# 31. 髙橋さんへの実機確認のお願い（W15 完了版・2026-09-13）

宛先: 髙橋さん / 差出: 小島（開発は Claude Code で進行）

---

## 0. 先に結論（お願いは1つだけになりました）

**でき上がった1つのファイルを、会社のパソコンで1回開いて動くかを見ていただくだけ**です。

これまでお願いしていた **PowerShell の「第2段ビルド」は不要になりました**。フォーム（画面の器）と参照設定の組み込みまで開発側の自動処理（`python3 build/build_rpn.py --final`）で完結するようになったためです（17章 Z-42 クローズ）。前回お渡しした第2段キットは破棄していただいて構いません。

所要は **15〜20分**を見込んでいます。

---

## 1. お渡しするもの

配布 zip（SharePoint / Teams 経由）に次を入れています。

| 中身 | 何か |
|---|---|
| `リスク提案ナビ.xlsm` | 本体。**これが完成品**です（フォームも参照設定も入っています） |
| `ナレッジブック.xlsx` | 業種リスク・メニュー・種目のマスタ。本体と同じフォルダに置きます |
| `リスク提案ナビを起動.bat` | 起動ボタン。**必ずこれから開いてください**（保存先 `data_dir.txt` をこれが書きます） |
| `ui/`（5ファイル） | HTML画面。本体と同じフォルダに置きます |
| `docs/24_実機テスト手順書_Windows.md` | 手順書 v7.6 |

**置き場所は `D:` 直下**（例 `D:\リスク提案ナビ`）でお願いします。信頼できる場所が `D:` だけという社内の前提に合わせています。

---

## 2. 見ていただきたいこと（この順に）

1. **開く**: `リスク提案ナビを起動.bat` をダブルクリック。赤い警告帯が出たら[コンテンツの有効化]。
2. **HTML画面が出るか**: 帯＋4区画の画面が出れば正常です。もしシート画面（`ナビ`）で起動したら、お知らせ欄（`hm_warning`）の文言を**一字一句そのまま**控えてください。
3. **自動テスト**: 画面左下の[テストを実行]。**FAIL 0 / SKIP 0** が正です。`SKIP(Mac)` が1本でも出たら、その行をそのまま控えてください。
4. **1社を通す**: 実在企業1社で、資料を貼る→[まとめて分析]→レポート出力まで。
5. **提案書（お客さま向け）**: 区画④で「内容を確認しました」にチェック＋確認者名を入れて[提案書（お客さま向け）を出す]。出た HTML を**エクスプローラーからダブルクリックして Microsoft Edge で開き**、印刷プレビューを見てください（画面の中の表示枠は IE11 相当で CSS が効きません）。A4横・22枚・発表者ノートが印刷に出ないこと、の3点だけ見ていただければ十分です。
6. **`content_filter` の代表3本**（今回の新しいお願い）: 「労災死亡」「PL賠償（異物混入）」「感染症BCP（集団感染で操業停止）」を含む現場メモで [まとめて分析] を1回ずつ。**3本とも最後まで通れば「E0207 なし」**とだけお知らせください。止まったものがあれば `データ\ログ\err_log.csv` の該当行をそのまま送ってください。

詳細な観察項目は手順書 §7 の表にまとめてあります。**全部やる必要はありません**。上の1〜6が通れば出荷判断ができます。

---

## 3. 送り返していただきたいもの（3点）

1. **`err_log.csv`**（`データ\ログ\`）— 空でも「空でした」とお知らせください
2. **案件JSON**（画面の「案件を書き出す」で出るファイル）— 実際の抽出品質を開発側のベンチにかけます
3. **`data_dir.txt` の中身1行**（メモ帳で開いてそのまま）

---

## 4. あわせて教えていただきたいこと（お手すきで）

| # | 質問 | なぜ聞くか |
|---|---|---|
| 1 | **リボンちゃんの新版のソース**をいただけますか | `Application.Run "ChatGPT"` の引数が変わっていないかを確定させたい（当方は11実引数で実装） |
| 2 | **`_ss` サフィックス**は今も要りますか | モデル名の指定方法が変わっていれば config の既定を直します |
| 3 | 本物の資料（実在企業の DR 出力）で1社、もう一度回していただけますか | mock ではなく**実際のディープリサーチの出力**で S1 の抽出品質を測りたい（当方の最大の懸念点です） |
| 4 | Windows の表示倍率は何%ですか | 前回の実測は 200% のみ。100/125/150% の見え方が未確認です |

---

## 5. 今こちらで確定していること（ご参考）

- 検問（自動チェック）は **35本**、純ロジックテストは **1,065本**。すべて緑でお渡ししています。
- 「作ったのに繋いでいない公開関数」は機械で数えて **0件**です。
- 顧客提示物（提案書）は**確認者名を入れないと1ファイルも出ません**。レポート側は未確認でも出ますが、免責が「AI生成・担当者確認前」に切り替わります。
- **実機でしか確かめられないもの**が、この依頼で残る全部です: HTML画面の描画（Z-43）、Outlook 下書きが AV にどう見えるか（Z-45）、`content_filter`（Z-50）。

---

## 6. 開発側に残っている「人の判断」1件（小島さん→PJ）

**現契約サマリの匿名化を既定でONのままにするか**（17章 Z-47 / 16章 OQ-6）。社内 AI 環境 v1.1 §7.1 では「解除可」となりました。既定を OFF にすると担当者の手間は減りますが、持株ログに本文が残ります（16章 OQ-13）。**実装はどちらでも1行**なので、PJ のご判断をいただければ切り替えます。現状は **ON（匿名化する）** のままです。
