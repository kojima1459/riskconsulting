# 35. 裁定書46: ナレッジ第2波の投入(班Eの候補 → ナレッジブック実データ。2026-09-14 W17)

> 班F(Sonnet)への依頼文(§0〜§5)と、司令塔の検収結果・手直し(§6)。班Eの候補表は `docs/kb/`、投入先は `build/sheets_kb.json`。

作業場所: `(作業用 worktree)`(git worktree。**この中だけ**で編集。`/home/user/riskconsulting` は触らない)。

## 0. 班の規約(CLAUDE.md。厳守)
- **git commit / push / checkout / restore / stash / merge / branch は一切行わない**(司令塔のみ)。編集して、検問を回して、報告するだけ。
- 裁定書の外の判断は**しない**。迷ったら concerns に書いて止める(その項目だけ未実施で可。他は進める)。
- 「資料にあることだけを書く」。人名・電話・メール・料率の具体額は KB に入れない。ナレッジ資料の原文はリポジトリ外(Google Drive)にあり、参照するのは班Eが `docs/kb/*.md` に写した候補表だけでよい。
- ソース(.bas)は UTF-8。1モジュール **30,000字以下**(`python3 -c "print(len(open(f,encoding='utf-8').read()))"` で測る)。1物理行 1,000バイト以下。
- 報告は「やったこと / 測った数値(コマンドと結果を貼る) / concerns」の3節。数値は必ず実測。

## 1. 投入する候補と裁定

値源: `docs/kb/` の班E候補表。投入先: `build/sheets_kb.json` の該当シートの `seed_rows`(既存行の書式・型・列順を厳密に模倣。列定義は変えない)。

| # | 候補 | 裁定 | 備考 |
|---|---|---|---|
| F-1 | `メニュー候補_ニューリスク.md` M-0101〜M-0120(20行) | **全行投入** | `source` 列は KB のメニュー一覧に無いので落とす。`target_categories` は enum `category` 内の値だけ(`;`区切り)。`target_industries` は候補が空なら空文字。`is_active` は JSON の `true`(既存行 M-0012 と同じ型) |
| F-2 | `種目候補_ニューリスク.md` L-12〜L-16(5行) | **全行投入**。L-12 の分割は**保留**(種目ナレッジシート受領後に司令塔が判断) | 既存 L-01〜L-11 の後ろへ |
| F-3 | `メニュー種目対応_候補.md`(20行) | **全行投入**。concerns の M-0117(PPA)は **L-16 と L-10 の2行**で登録(対応表は多対多なので二重登録が正) | 計21行追加 |
| F-4 | `判断基準候補_ニューリスク.md` UW-NR-01〜33(33行) | **全行投入。rule_id は `J-50`〜`J-82` へ改番**(UW-NR-01→J-50 … UW-NR-33→J-82。13章の既存形式 `J-NN` に合わせる。`UW-` 形式は不採用) | `workaround` は enum `rule_workaround` = `entry_path / benefit_form / underwriter / none` の**4値だけ**。候補にある **`scope`(4件)→`none`、`channel`(1件 UW-NR-07)→`entry_path`** に置き換える(理由: 「直扱いに限る」は入口の制約=entry_path。「対象外」は回避策なし=none)。置換した行番号を報告に列挙すること。rule_text から「(要確認…)」の注記は削る(注記の内容は報告の concerns へ写す) |
| F-5 | `成功事例候補.md` K-0101〜K-0112 | **投入しない(保留)**。`why_it_worked` が資料に無い行を KB に入れると S3/S4 のプロンプトが根拠のない「成功理由」を学ぶため | 何もしない。docs/kb に候補表は残す |
| F-6 | `対訳表候補_ニューリスク.md` 14対 | **下記 §2 のとおり投入**(replace 9対・warn 6対。「リコース型・ノンリコース型」は2対に分ける) | 実装場所は §2 |
| F-7 | 種目マスタ **L-01** `uw_info_needs` | 末尾に4項目を追加(`;`区切り): `事故頻度の時系列(直近3〜5年の件数と種類);従業員数と車両台数の推移;運転者の年齢層・免許証の色;運転者の違反歴と安全運転教育の受講状況`。最後の項目は既存「安全運転教育の実施状況」と重なるので、**既存の「安全運転教育の実施状況」を「運転者の違反歴と安全運転教育の受講状況」に置き換える**(項目数は +3 になる) | 値源: `docs/34_野田FB_自動車フリートGの観点と現設計の突合.md` 3-1 |

## 2. 対訳表の投入(F-6)。modValidate5 の新設

現状: `src/app/modValidate4.bas` は 29,976字で**満杯**(残24字)。`TabooPairs()` が対訳表の**唯一の値源**で、`tools/render_proposal.py` の `check_glossary_impl`(行675付近の `_impl_pairs`)が **modValidate4.bas の字面**から `AdPair` 行を読み取って `docs/design/提案書_wide/対訳表_社内語から顧客語.md` と全列を突き合わせている。

裁定(構造):
1. `src/app/modValidate5.bas` を**新設**(R4 純関数モジュール。ヘッダ注釈は modValidate4 に倣い「なぜ分けたか(容量)」「何を持つか」を書く)。`Public Function TabooPairList() As String` に **modValidate4.TabooPairs の全 AdPair 行を移し**、末尾に新規 15 対を足す。`AdPair` は modValidate5 の Private へ移す(modValidate4 側の `AdPair` は削除)。定数 `V4_REPLACE/V4_WARN/V4_TAB` 相当は modValidate5 で自前に持つ(名前は `V5_` 接頭辞)。
2. `modValidate4.TabooPairs()` は `TabooPairs = modValidate5.TabooPairList()` の**1行委譲**にする(公開 API 名は変えない。modTestsPure27/28 と modExportProposal が `modValidate4.TabooPairs` を呼ぶ)。
3. `tools/render_proposal.py` の `_impl_pairs`(と TermChars 以外で modValidate4.bas を読む箇所)を **modValidate5.bas の `TabooPairList()`** を読むように直す。`TermChars()` は modValidate4 に残す(動かさない)。RENDER_MODULES の手並べ一覧(行63〜66)に `modValidate5` を足す(閉包で自動追加されるが、注釈のとおり「手で足す」規約)。
4. `tools/vba_lint.py` の CONTRACT 一覧(行244付近 `"modPromptsS5", "modValidate4", "modPipeline5"`)に `modValidate5` を足す。
5. `docs/design/提案書_wide/対訳表_社内語から顧客語.md` に新規 15 対を**同じ書式で**追記(§1「保険の内部用語」へ。`mode` 列・理由・出典ファイル名は候補表から写す。**§5「判断に迷った語」に「てん補」単独(見送り。複合語「てん補期間」だけ対訳表化)を1行追加**)。§6.1 の「`warn` の対(全10対)」の件数を実測に直す。§7 と冒頭の実装参照 `modValidate4.TabooPairs` を `modValidate5.TabooPairList`(委譲元 `modValidate4.TabooPairs` を併記)に直す。
6. `docs/spec/15_プロンプトとJSONスキーマ.md` 行1642付近と `docs/spec/12_アーキテクチャとデータフロー.md` §2 のモジュール一覧(行204付近)に modValidate5 を追記(12章は冒頭の版履歴に1行足す。版番号は +0.1)。

新規 15 対(社内語 / 顧客語 / mode):

replace(9):
- てん補期間 / 保険金をお支払いする期間
- 縮小支払割合 / 損害額のうち保険金としてお支払いする割合
- 縮小率 / 経過期間に応じて保険金の支払割合を減らす仕組み
- 既発生債権 / 保険が始まる前から既にあった売掛金
- 保証委託契約 / お客さまと保証会社との間で交わす契約
- リコース型 / 売主が補償責任を負う型
- ノンリコース型 / 売主が補償責任を負わない型
- 1事故免責金額 / 個別の損害ごとにお客さま負担となる金額
- 1証券免責金額 / 契約全体を通じてお客さま負担となる金額

warn(6。置換せず警告だけ):
- アームズ・レングス・ルール / 利害関係のない対等な立場どうしでの交渉
- No DD, No cover / 十分な調査をしていない部分は保険の対象にしないという原則
- デミニミス / 個別の損害ごとにお客さま負担となる金額
- エクセス / 一定額を超えた部分だけを補償する免責の設定方法
- フランチャイズ免責 / 一定額を超えたら全額を補償する免責の設定方法
- 求償 / 保険金をお支払いした後に保険会社が本来の責任者へ請求すること

注意:
- **最長一致と語頭側重なり**(対訳表§6・§6.2・`modValidate3.HeadOverlap`): 「1事故免責金額」「1証券免責金額」は既存「免責金額」を含み、「ノンリコース型」は「リコース型」を含む。`python3 tools/render_proposal.py` の check_glossary が**連鎖・重なりを赤にしたら**、対訳表§6.2 の方式(宣言側で解消)に従い、どう解消したかを報告する。勝手に対を落とさない(落とすなら concerns)。
- 顧客語が別の社内語を含まないことを目視で確認する(冪等性)。
- 「デミニミス」の顧客語は候補表では「(1事故免責金額の別名)」だったが、上記のとおり1事故免責金額と同じ顧客語にする(司令塔裁定)。

## 3. 検問の拡張(F-8)と純テスト(F-9)

F-8 `tools/kb_check.py` に検査を足す(既存の (a)〜(d) の型に合わせ、`Checked.record` に実際に見た行数を積む。`--selftest` の変異注入も1条件につき1本足す):
- (e) メニュー一覧.`target_categories` の各値が enum `category` 内 / `menu_id` 重複なし
- (f) メニュー種目対応の `menu_id` がメニュー一覧に、`line_id` が種目マスタに実在 / 同じ対の重複なし
- (g) 種目マスタ `line_id` 重複なし
docstring の「何を見るか」も更新。

F-9 純テスト: 新規対の挙動を層(a)に足す(`modTestsPure28.bas` は 9,070字で空きあり。そこへ足す):
- てん補期間 が終端文脈で置換される(SoftenTaboo)
- 「1事故免責金額」が「免責金額」より先に最長一致で1回で置換され、結果に「免責金額」の顧客語「ご負担いただく金額」が**混ざらない**
- 「No DD, No cover」は置換されず TabooHit に出る
- 「ノンリコース型」→「売主が補償責任を負わない型」(リコース型の対に食われない)
件数を `wintest/tests_expected.txt` の `prod=1082` に足す(足した本数ぶんだけ。実測で決める)。

## 4. DoD(全部貼ること)
```
cd <wt_F>
python3 tools/kb_check.py            # OK
python3 tools/kb_check.py --selftest # OK
python3 build/build_rpn.py --kb      # OK(dist/ナレッジブック.xlsx)
python3 tools/sheet_check.py --kb    # OK
python3 tools/config_check.py        # OK(存在すれば)
python3 tools/vba_lint.py            # OK
python3 tools/orphan_check.py        # OK
python3 tools/render_proposal.py     # OK(check_glossary 含む3本)
python3 tools/run_lo_tests.py --pure --pure-timeout 240   # 純層 = tests_expected の prod と一致
python3 tools/gate_count.py          # OK
python3 tools/doc_gate.py            # OK
```
加えて: 各シートの seed_rows 件数(投入前→後)、modValidate4/5 の字数、対訳表の対数(replace/warn 別)を実測で報告。`git diff --stat` も貼る(見るだけ。commit しない)。

## 5. concerns の例(こう書く)
- 「UW-NR-07 の workaround を entry_path にしたが、資料の趣旨は『代理店経由不可』であり entry_path の語義(申込の入口)と少しずれる。司令塔確認」
のように、**何を・どう判断し・何が不安か**を1件1行で。

## 6. 検収結果と司令塔の手直し(2026-09-14)

### 6.1 投入結果(実測)

| シート | 前 | 後 |
|---|---|---|
| メニュー一覧 | 1 | 21 |
| 種目マスタ | 11 | 16(L-01 の `uw_info_needs` は +3 項目) |
| メニュー種目対応 | 1 | 22(M-0117 は L-16 と L-10 の2行) |
| 判断基準 | 30 | 63(`J-50`〜`J-82`) |
| 成功事例 | 1 | 1(保留。F-5) |
| 対訳表 | 51対(replace 41 / warn 10) | 66対(replace 50 / warn 16) |
| 純テスト(prod) | 1082 | 1086 |

- `modValidate4.bas` 29,976→27,485字、`modValidate5.bas`(新設)5,306字。`TabooPairs` は modValidate5 の `TabooPairList()` へ1行委譲。
- `kb_check.py` に (e) メニュー一覧の enum/ID重複、(f) 対応表の両IDの実在と対の重複、(g) 種目 ID 重複を追加(検査 4,196→4,456件)。
- `workaround` の丸め: `scope`→`none`(UW-NR-03/29/32/33 = J-52/J-78/J-81/J-82)、`channel`→`entry_path`(UW-NR-07 = J-56)。

### 6.2 班Fの concerns と裁定

1. **`render_proposal.py` の check_glossary が赤**(15章§5.6 の system 本文に新規15対の `社内語 -> 顧客語` が無い)。班Fは `modPromptsS5.bas` が依頼文の対象外のため止めた(正しい)。**司令塔が手直し**: 15章§5.6【3. 用語】と `modPromptsS5.bas` に15行を同時追加(`prompt_diff.py --strict` 差分0)。
2. **実効出力の走査が180秒ちょうどでタイムアウト**(対が51→66、入力行 9,180→11,880 で線形に伸びた)。**司令塔が手直し**: `render_proposal.py` の上限を名前付き定数 `RENDER_TIMEOUT_SEC = 420` にした(切れれば「HTMLが生成されませんでした」で赤になり、黙って通ることはない)。実測 4分58秒で緑。ゲート一括の所要時間は約5分増える。
3. DoD の `--pure` は `--mode pure` の誤記(司令塔の依頼文の誤り。班Fが読み替えて実行)。
4. J-56(個人ローン信用保険は直扱いに限る)の `workaround=entry_path` は字義がやや広い。**enum を増やさず現状維持**。種目ナレッジシート受領時に `rule_workaround` の enum 見直し(`channel` を足すか)を再判断する。

### 6.3 司令塔の抜き打ち変異(いずれも赤を確認後に復元)

- 対応表の `line_id` を `L-99` に → `kb_check` が「(f) メニュー種目対応: line_id 'L-99' が種目マスタに無い」で NG。
- `modValidate5` の「てん補期間」を `V5_WARN` に → 純テスト PASS 1084 / FAIL 2。

### 6.4 残り(次の波へ)

- 成功事例12件(K-0101〜K-0112)は `docs/kb/成功事例候補.md` に保留。成約理由が資料に無いため、髙橋さん・ニューリスクGの担当者に「なぜ決まったか」を1行ずつ足してもらってから投入する。
- L-12(費用・利益)の分割、予防系メニュー(ドラレコ・テレマ・安全運転診断)は種目ナレッジシート受領後。
- 野田さんの三式受領後に裁定書45(docs/34)を確定。

### 6.5 ゲート一括(37本)で見つかった2件(司令塔が手直し)

- **build-dev/prod/final・bin-roundtrip・bin-rt-final が赤**: 新設 `modValidate5` が `build/modules.json` に未登録(依頼文にも書いていなかった)。「作ったのに繋いでいない」型を build-dev の未登録検査が止めた。登録して緑。
- **ship ②キー走査が赤(16件)**: ナレッジ資料の出典に含まれる**ローマ字化ファイル名**(例 `Jkurejitto…202111`。44字・数字と大小文字を含む)を「Base64様の鍵」と誤検知。`tools/ship_check.py` に精度ガード(4)「英大文字+数字の割合が 18% 未満なら鍵ではない」を追加(乱数鍵 3,000,000 本で素通り 0 本・最小 18.2%。ローマ字16件の最大 15.9%)。`selftest_b64` を②の冒頭で毎回回す(乱数鍵 20,000 本の検算とローマ字の負例)。閾値を 0.6 に壊す変異で 1,404/2,000 本が素通りして赤になることを確認後に復元。
