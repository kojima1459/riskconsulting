# 22. 実装前監査記録（2026-08-29・実装GO直前の上流総点検）

## 経緯と結果

実装開始指示（発注者・2026-08-29）を受け、着手前に仕様書10～19章の総点検を実施した。

- **監査**: 6レンズ（整合性トレース／VBA・32bit実装可能性／エッジケース網羅／テスト厳格性／レゴ再利用性・変更容易性／プロンプト品質）×Opus、全指摘に実物引用を義務付け
- **反証**: 各レンズの指摘を別Opusが敵対的に再検証 → **72件が生存**（棄却0）
- **裁定→改訂**: 司令塔が裁定書で語彙・番号・関数契約を固定し、6編集者がファイル分割で仕様書v2.4へ改訂（新設: 18章HTMLレポートテンプレート仕様）
- **横断検証→残欠陥修正**: 検証者2名が残欠陥25件を検出→修正→最終検証7件→司令塔が外科修正で完了。機械検査（CP932・依存グラフ・引数順・enum同期）全緑

代表的な重大欠陥（すべて修正済み）: S3プロンプトに企業プロファイル未注入（幻覚確定構造）／S2にメニュー一覧未注入（ID実在検査の自己矛盾）／13章にS1～S4シート列定義なし／15章にCP932外文字（VBEで`?`化）／allianceバリアント本文の不在／mock異常系ゼロ・SKIPがPASS扱い（出来レーステスト穴）。

## 実装環境の実証（このリモート環境）

姉妹PJ（notebook）の3ゲートを実機実行し全緑を確認: `vba_lint.py`（ERROR 0）／`run_lo_tests.py`（LibreOfficeコンパイル全PASS）／`build --dev`（自己検証OK）。**必要な追加は `pip install olefile` のみ**。→ T-01のDoDはこの環境で機械判定できる。wintest（実Excel）のみWindows実機側。

## キー露出の最終確定（ハッシュ突合・2026-08-29）

- B6BE7の配布物にあったhex64 blobは**H-17レビューで差し替え済みのダミー**（復号すると"DUMMY-not-a-real-key-…"）。B6BE7最新の成果物に実キーは無かった（docs/21の旧記述は訂正済み）
- **実キーの露出現存は `product/nexus-agent` のdist 2ファイルのみ**（configシートの`azure_embed_key`・sheet6/sheet14）
- B6BE7系のgit履歴には差し替え前コミットが残る → **リポジトリ非公開の維持が唯一の防壁**（ローテ不能キーのため）

### 伝書鳩（姉妹PJセッションへの依頼・1件のみ）
> dist追跡停止の処置（B6BE7では実施済み: 86934a5/92daebf）を **product/nexus-agent ブランチにも横展開してほしい**。実キーが今も残るのはそちらのdistだけ。

## 発注者への確認事項（急ぎではない・実装は止まらない）

1. **「ニューリスク」の意味**（HTMLレポートSEC-09）: **解決済（発注者確認 2026-08-29）**――「サイバー・気候変動のような新種・新興リスク（エマージングリスク）」の意味と確定。S2スキーマに `emerging_risks` 配列（0～3件・10分類カテゴリ・時間軸・当てはまり根拠つき）を新設し、SEC-09はこれを表示する。第2ラウンドの新仮説（status=new）は別概念として維持（裁定書3で反映）
2. 本改訂で司令塔が確定させた運用値（実測後に見直し可）: research_requestsは最大6件／ナレッジ注入は入力全体の3割上限・1行400字切詰め／企業ドシエ共有前のPII警告は「確認した」選択を必須化

## 実装後記（W0～W4.4・2026-08-30）

- **実装5波**: W0（雛形・検問基盤）→W1（core移植）→W2（app層: 案件・ナレッジ・プロンプト・検証・パイプライン）→W3（ui層・HTMLレポート）→W4（最終レビュー・手順書一式）。成果は VBA 67モジュール（core 8 / app 30 / ui 12 / test 17）。
- **修正4波**: W4.1～W4.4（裁定書9～12）。各波は「敵対的レビュー→司令塔の裁定書→修正班→検証班」の4段で回し、**修正案を出す者と裁く者を分離**した。
- **検問方式**: 緑は1行・赤は雄弁の非対称出力（`tools/gate.py`）で全14ゲートを一括化。lint（ERROR 0）／LibreOffice コンパイル／純ロジック **612本**／dev・prod・kbビルド／シート台帳照合254項目／出荷検査／プロンプト差分／validate／enum同期／HTMLレンダの10系統が毎回走る。
- **テストの3層**: 層(a)＝純ロジック612本（LibreOfficeで無人実行）、層(b)＝実Excel E2Eスモーク **47本**（`modTestsExcel` + `modTestsExcel2`。自己照合 `TE_EXPECTED` つき）、層(c)＝実機手順書（docs/23・24）。
- **敵対的レビュー3周の収穫**: critical 3件・major 20件超を**出荷前に**捕獲した。代表例:
  - **B1**（W4.1）: 前の案件の画面が残ったまま次の案件で保存され、別案件の内容が確定する**クロス案件汚染**。`sN_case_id` の突合ガードで封じた。
  - **B2**（W4.1）: 受信箱の判定が `status` の自己遷移で必ず弾かれ、**判定が構造的に1件も保存できない**。入力列 `judge_to` を新設して解消。
  - **C1**（W4.2）: 投函本文が `InputBox` の255字上限で切れ、**長文の投函が不可能**。受信箱の「投函下書き行」方式へ差し替え。
  - **P1**（W4.2）: 選択行の取得が `ParkFocus` より後で、判断台帳の起票が**別の行を読む**。取得順の固定で解消。
- **W4.4（本波・裁定書12）**: 案件入力の採番判定を揮発変数から**ブックに残る `ci_case_id` の3値判定**（`(新規)`／有効ID／空はブロック）へ移し、仕様（13章§2.11）が宣言していた無条件保証と実装を一致させた。あわせて受信箱・判断台帳の入力列を日本語ラベルの列全体DVへ揃え、層(b)へ回帰網3本を追加した。
- **残り**: 実機テスト（T-41/T-46④⑤・Windows実Excel）のみ。これが緑になるまで配布はしない。
