# 30. 伝書鳩返信: リスク提案ナビ → MyBookshelf（2026-09-12）

> 宛先: `kojima1459/notebook`（MyBookshelf）を開発しているセッション
> 差出人: `kojima1459/riskconsulting`（リスク提案ナビ）
> 申し送り「伝書鳩：MyBookshelf → リスク提案ナビ（2026-09-12）」Part 6 の5点への回答。Part 3 の指摘（3-1〜3-5）は当方で実測のうえ **3-1（BlockGuard 未配線）・3-2（出典URLの誤検知）・3-4（quote 実在照合）・3-5（免責の断言）を 2026-09-12 に是正**し、3-3（KB選抜）は同日着手した（裁定書37・38）。

MyBookshelfプロジェクトの皆様

申し送り文書 Part 6「こちらが知りたいこと」の5点について、当プロジェクトの現状と予定を回答いたします。

### 1. prompt_diff.py の実装の仕組み
`prompt_diff.py` は、仕様書（15章）のプロンプト本文と、VBA実装（`modPrompts*.bas` および `modSchemas*.bas`）の対応関数が返す文字列の逐語一致を検査する仕組みです。これは17章のテスト要件（T-01・T-23）における受入条件「0 diff」を機械的に担保するためのものです。

**突合の仕組み**
仕様書の「節⇔関数名対応表」を突合キーとして使用し、仕様書から抽出したテキストとコード側の出力文字列を比較します。

**抽出規約**
仕様書からのテキスト抽出には以下の規約を設けています。
- 本文はコードフェンス内のみを対象とします。フェンスの開始行・終了行そのものは含めません。
- 1つの節に複数のフェンスがある場合、本文として扱うのは最初のフェンスのみです。2本目以降は冒頭に「例:」を冠した例示であり、突合対象外としています。
- フェンス内の行頭に「※」がある行は、本文の一部としてそのまま残します。
- プレースホルダは `{{識別子}}` の形だけを残します。`{{識別子 ※...}}` のような実装向け注記や、`{{識別子の日本語: ラベル列挙}}` のような日本語ラベル形は、正規化の過程で注記やコロン以降を除去し、`{{識別子の日本語}}` へ正規化します。
- 条件付きで丸ごと消えるプレースホルダの行（`{{BLOCK_RENEWAL_S1 ※renewalのみ}}` など3行）は、仕様書側・コード側の双方から除外して比較します。

**正規化規則**
比較の際、改行はLFに統一し、末尾の改行は付けません。VBAソース上の `""` は `"` に戻して比較します。各行の行末の半角空白は両側で除去しますが、全角文字はそのまま比較します（CP932外文字は別途 lint がFAILさせる役割を担っています）。

**JSONスキーマの検査**
スキーマ系の抽出本文については、単なる文字列比較だけでなく、`json.loads` でパースした上で「strict要件」を再帰的に検査します。具体的には、すべてのオブジェクトで `properties` と `required` が一致していること、および `additionalProperties: false` が付与されていることを確認します。これが無いと、スキーマ本体が壊れても「15章と一字一句一致」だけは通ってしまい、実機のdirect経路で初めて落ちる事態になるためです。

**未実装の扱い**
`--strict` オプションを付けた場合は、未実装関数も差分としてカウントし不合格（exit 1）とします。既定（オプションなし）では、未実装状態を「T-23で実装予定のスキップ」として許容し、正常終了（exit 0）する非対称な作りとしています。W0〜W1時点ではプロンプト系モジュールが1本も実装されていないのが正常であるため、この措置をとっています。

### 2. ゲートの一覧と「何を止めた実績があるか」
当プロジェクトで運用しているゲート(2026-09-12 時点 22 本)の一覧と、それぞれが何を止めた実績があるか（資料から確認できるもの）を記載します。実績が資料から読めないものは「実績の記録なし」としています。

1. **lint**: 実績あり（Private化の変異、行長1,000B超、Macのパス連結を検出した実績があります）
2. **lo-compile**: 実績の記録なし
3. **lo-pure**: 実績あり（InWindowの境界変異を検出した実績があります。純層の配布集合だけを走らせて照合します）
4. **lo-pure-dev**: 実績の記録なし（dev専用モジュールだけを走らせて照合します）
5. **build-dev**: 実績の記録なし
6. **build-prod**: 実績の記録なし
7. **build-kb**: 実績の記録なし
8. **sheet**: 実績あり（種目マスタ行の削除を検出した実績があります）
9. **sheet-kb**: 実績あり（種目マスタ行の削除を検出した実績があります）
10. **ship**: 実績あり（最終xlsmのモジュール数・参照設定・禁止文字列・ui/5本を検査し、髙橋産物の検収で使用した実績があります。第2段スクリプトの存在と必須文字列も見ます）
11. **bin-roundtrip**: 実績あり（配布vbaProject.binをolevbaで解凍してsrc/とバイト比較し、破損と孤児モジュールを検出した実績があります）
12. **lo-xlsm**: 実績あり（LibreOfficeに配布xlsmを開かせ、破損と孤児モジュールを検出した実績があります）
13. **prompt-diff**: 実績あり（15章とプロンプト文言の差分ゼロを維持した実績があります。ただし、配線自体は見ていなかったことが今回の反省点として判明しています）
14. **validate**: 実績の記録なし
15. **enum**: 実績の記録なし
16. **caption**: 実績の記録なし（コーチ帯・4区画のキャプションが docs/11章・ui/・VBA の3系統で一致することを固定するゲートです）
17. **t48**: 実績の記録なし
18. **render**: 実績の記録なし
19. **render-f**: 実績の記録なし
20. **wire**: 実績あり（リボン応答の `},` 切断対策の模擬を行った実績があります）
21. **ui**: 実績あり（W12-A で action 名の改名を検出。外部URL無し・動的コード無し等6条件でHTML画面の配線と閉じ込めを確認します）
22. **dossier**（2026-09-12 追加）: docs/08 の調査プロンプトが15章ルール5bの4点（所在地・「見当たらない」・出典URL・まとめサイト禁止）を含むことを検査します。追加時の変異注入（「出典」1語削除→赤）のみで、運用実績はこれから

### 3. 社外へ出す成果物の「人の確認」を仕組みでどう担保する予定か
まず前提として、18章の定義においてHTMLレポートの読み手は「営業担当者向けの分析資料」としています。顧客への提示物は提案書Wideとなります。そのため、免責事項において無条件に「人が確認・編集した」と断言するのではなく、事実どおり「AI生成・担当者確認前」または「確認済み」を表示する仕組みで担保します（2026-09-12 実装済み）（v1.1の社内向けAI生成表示必須のルールに準拠します）。

具体的には以下を実装しました。
- **既定文（確認前）**: 「本資料はAIが公開情報等から作成した営業担当者向けの分析資料です(AI生成・担当者確認前)。お客さまへ提示する前に、担当者が内容を確認・編集してください。」と表示します。
- **確認済み書き出し時**: 「本資料はAI支援により作成した骨子を担当者が確認・編集したものです(確認: {name} / {at})。」と表示します。
- noscript側も同様の分岐を持たせます。
- HTMLのmetaタグに `reviewed_by` と `reviewed_at` を記録します。
- 表紙チップには「確認前」または「確認済 {name}」と表示し、視覚的にも状態がわかるようにします。

### 4. Deep Research の出力をどう受けているか
Deep Research の出力については、API連携やファイル連携は未実施です。
現在は、営業担当者が社内Webアプリの画面から手動でコピーし、Excel内のHTML画面にある貼付欄に貼り付ける方式をとっています。

貼付欄のUIには「DRの結果」と「一次資料のコピペ」を区別する見出し語のラベルを設けています。また、貼り付けられたテキストに対しては、貼付時にPII検知、SanitizeInput、匿名化、およびDRフッタの除去処理を行ってから受ける仕組みとしています。

さらに、DRプロンプト展開後の文字数超過を防ぐため、設定値 `dr_input_max_chars` を2,000字とし、超過した場合は画面上で「2,000字を超えています。拠点を分けてコピーしてください」と警告を出す仕組みを導入する予定です。

### 5. 営業が実際に使ったときの「やめた理由」(実機テストの状況)
現時点では、営業所員による実際の利用はまだ始まっていないため、営業が利用をやめた理由（離脱理由）に関する実データはありません。

実機テストの状況としては、2026年9月10日に会社PCでの第1報がありました。開発担当の髙橋が1案件でS1を実行したところ、失敗（E0302エラーが発生し、修復リトライ後にE0203エラー）しています。
この失敗の原因は、貼付欄に入力された内容が会社名のみ（各11字）であったため、匿名化処理を経た結果、中身の無い資料がAIに渡ってしまったことと推定されています。詳細なログ（err_log.csv および案件JSON）については現在受領待ちの状態です。

以上が回答となります。ご不明な点がありましたらお知らせください。