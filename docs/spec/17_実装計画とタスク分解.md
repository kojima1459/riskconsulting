# 17. 実装計画とタスク分解 v2.6

v2.6（W7・裁定書25「W7センターピン整合」）: §3へ **W7 の表**を新設し、**T-55（班B・core）/ T-56（班C・app/ui/出力）/ T-57（班B+班C・検問と回帰の同期）** を登記した。**T-50〜T-52 / T-54 は Phase 1.5 の先行定義として既に使用済み**、**T-53 は欠番（再利用禁止）** であるため、W7 は T-50番台の空き番である **T-55 から**採番した（§3の「番号を再利用しない」規約に従う）。仕様側（10/11/12/13/15/18/19章）の改訂は班Aが先に確定済みであり、本表の実装はそれを**写す**作業である（15章の本文は `tools/prompt_diff.py` が突合するため、文面を実装側で言い換えてはならない）。

v2.5.5（裁定書14: W5「社内環境対応+UI/UX改善」班Cの引継ぎ）: §7のバックログへ **Z-9（`EnsureButton`/`EnsureButtonEx` の戻り値が全呼出で捨てられ、描画失敗が記録されない）**・**Z-10（`modBoot` 申し送り `input_rule_omissions` の未処理）**・**Z-12（HOME以外のボタン横重なり=`FitCol` 横展開。Z-5と同時波で）**・**Z-13（`modKnowledge` 残23字・`modUIHome` 残368字の分割。次に触る波は着手前裁定必須）** を登録した（裁定書14 班Bのconcerns裁定 B-c7/B-c1/B-c3/B-c4）。

v2.5.4（裁定書13: W4.5 クローズパッチ）: §7のバックログへ **Z-6（判断台帳の確定行の日本語表示化。Phase 1 は容認し実機FBで判断）** を登録し（W3）、§7末尾の裁定書12の解消範囲を **V1・V3～V9**（V2 は欠番）へ訂正した（W7）。

v2.5（裁定書9: W4.1 最終修正ウェーブ）: W4表に **T-42b（裁定書9の修正ウェーブと敵対的再レビュー）** を追加してT-42の残（B1～B22・A-1～A-6の消化と再レビュー1周）を明示し、§5-1の検問列に `tools/enum_check.py` と `tools/render_report.py`（standard/--faithful）を加えて**まとめては `python3 tools/gate.py`（当時全14ゲート一括）で回す**ことを標準と定め、§6の後にバックログ節を新設して **A-4（config `deep_transport` 未結線）** を唯一の残件として記録した。

v2.4.1（裁定書5: W1整合）: T-22のDoDに「15章§8.1のmock11応答を modValidate で全件検証（警告0件）」を追記（§8.1受入条件1の「T-22後に再検証」の実体）、T-24のDoDに「run_log detail に extra_json / fw_normalized / E-49除去件数が記録されることを mock E2E で確認」を追記（層(a)では検査不能なため）、T-42の観点②に「CallStep/CallChat の ok が DecideOk の戻り値以外で決まっていないこと」を追加した。

v2.4（ニューリスク=エマージング確定・W0実装報告の裁定）: T-22の検証ケース内訳をS2:17件・計64件へ更新（§4-2の照合も計64件）、T-16のDoDに protection_policy 全シート適用＋ParkFocus と case_data の data_key 入力規則（隠しレンジ参照）を追加、T-46②に誤検知ガードの宣言義務・③に `docs/` 配下の仕様入力xlsxの対象外を追記、T-47のエントリ関数名を `modTestsExcel.RunAllExcelTests()` に確定、§4-1に `tests_expected` の実装形（`modTestRunner.SetExpectedCount(n)` のハーネス注入・未設定実行はランナー自身がFAIL）を明記した。
v2.4変更概要: 実装前監査72件の裁定を反映。T-16（modBoot起動シーケンス）/T-29（modCompanyFile＋modPii）/T-35（HTMLテンプレ・テーマ定数）/T-47（modTestsExcel＝テスト層bの実体）を新設してT-53をPhase 1.5表から削除し、T-42を読取専用の敵対的レビュー（観点6本・裁定ファイル必須）へ全面書換、T-46を走査仕様つき5項目の出荷前検問に改め、T-01の移植・成果物に`run_lo_tests.py`/`prompt_diff.py`を追加、§4-1にテストランナー要件・§4-2に検証ルール表の照合を新設し、§5-1の出荷手順と各タスクのDoDを更新した。（検証指摘の修正）依存欄の範囲記法とワークストリーム一括指定を廃してID明示列挙に統一、T-16をW2末尾へ移して実体結線検査に改め、T-33bをPhase 1.5表へ移し、T-14b（ニセリボンちゃん配管検証）を新設した。

## 1. 開発体制（PoC「開発憲法」踏襲）

- 司令塔（Fable）: 仕様裁定・発注・検収。実装しない
- 実装: Opus（ゲートウェイ・検証・パイプライン・COM等の難所）／Sonnet（移植・シートI/O・UI・定数化・テスト）
- 検問方式: タスク着手時に空コミット→受入条件ごとに即コミット。停止時は最後の検問から再投入
- 各実装波の完了後に敵対的レビュー1周＋Fix後もう1周
- 実装エージェント必読: docs/spec/10～17＋19、docs/06/07、PoCの移植元ファイル（12章の対応表）。**仕様との食い違いは勝手に直さず司令塔へ報告**（裁量は変数名・文言字数のみ）
- **開発憲法の正典（追随先・2026-08-28実物確定）**: 対コード規約は `product/nexus-agent:docs/dev/CONTRIBUTING.md`（§2.1 レイヤリングR1／§2.5 1モジュール約30,000字上限＝30,000字契約の正／§3 変更後に必ず通す3ゲート＝検問方式の正）。対人の作法は別レイヤー（nexus系の `CLAUDE.md` 相当）に置き、コード規約と混ぜない。B6BE7の `.claude/skills/final-gates/SKILL.md` は検問の実装形。本製品はCONTRIBUTING.md準拠。
- **30,000字契約の分割バックログ（超過する前に切り出す順番。CONTRIBUTING.md §2.5）**: 警告帯（28,000字）に入ったモジュールは、次にそのモジュール本体へ手を入れる波の**前**に切り出す。現時点の待ち行列（W3.1時点。**着手はいずれも「そのモジュール本体へ次に手を入れる波の前」**）:
  1. **`modPipeline`（29,988字・残り12字。W4.2で run_log `operator` 結線を入れて残余がほぼ尽きた）から入力打切り系（E-03 の9貼付欄の読取と切詰め＝`LoadPasted` / `Sanitized` 相当の約2,300字）を `modPipeline3` へ切り出す**（W3の持越し。W2cで `qualityOverride`・`SetStepOutcome` 結線・`modPipeline2` への1行フックを足して余白がほぼ無いため。判定核16本は `modPipeline` に残す＝14章§6の公開契約面を動かさない）。W3では本体に触れなかったため未着手のまま持ち越している。
  2. **解決済み（W7・T-56）**: `modCaseStore`（29,997字・残り3字）は本体へ手を入れる波（T-56 の `input_finance` 追加と `FreezeRound` の写し込み）で規約どおり先に切り出し、**`modCaseStore3`** を新設して `data_key` の一覧（`DataKeys`）とラウンド確定の付帯処理（`ApplyRoundFocus`＝13章§2.1 `adopted_story_nos` / `focus_line_ids`）を移した（29,837字 / 8,267字）。12章§2・`build/modules.json`・`tools/vba_lint.py` の3箇所へ登記済み。
  3. **`modTestsPure5`（29,952字・残り48字）**。1行でも足すと契約を割るので、次に触る波では先に切り出す。
  - **解決済み（W4.3・裁定書11）**: `modUICase3`（29,782字・残り218字）は本体へ手を入れる波（W4.3 Q1の overflow 永続ガード）で規約どおり先に切り出し、追加収集の[コピー]（`CopyResearchRow` / `PromptColOn`）を `modUICase4` へ移した（28,962字 / 11,664字）。`modTestsExcel` も層(b)へ2件足す前に `modTestsExcel2` を新設した（本数の自己照合は `RunAllExcelTests` 側で合流）。
  - **解決済み（W3.1）**: `modTestsPure10` は本体へ手を入れる波（W3.1）で規約どおり先に切り出し、エスケープ契約20本と共有素材を `modTestsPure11` へ移した（23,507字 / 9,752字）。`modHtmlTemplate1` も18章§4.4の25,000字規約に従い `LabelJs` を `modHtmlTemplate6` へ出した（23,143字 / 3,469字）。
- **実機検問（B6BE7が34ラウンドかけて実証した教訓）**: LibreOffice合格を出荷条件にしない。W4結合・実機で **wintest（実Excel自動テスト・COM経由）のPASSを最終ゲートに必須化**する。テストは次の3層に分離し、「(b)が通るまで出荷しない」。
  - **(a) 純VBA**: `modTestsPure1..n`。どこでも実行可。実行と集計は `modTestRunner.RunAllPureTests`（SKIPをPASSに数えない。§4-1のランナー要件が正）
  - **(b) Excel固有**: `modTestsExcel`（＋30,000字契約の分割先 `modTestsExcel2`。入口は `RunAllExcelTests` の1本）。wintest実機（実Excel・COM経由）のみで走る。**層(b)の実体はT-47で作る**（`run_excel_tests.ps1` が純ロジックを実Excelで流すだけでは層(b)にならない）
  - **(c) 実機前静的**: `tools/vba_lint.py`（規約・命名・レイヤリング・CP932検査）＋ `tools/run_lo_tests.py`（LibreOfficeでの全モジュール構文コンパイル検証＋純ロジック実行）。**(c)緑はコミット条件であって出荷条件ではない。出荷条件は(b)**。HTMLテンプレ系に触れた変更は `tools/render_report.py`(standard/--faithful)も(c)に含める(18章の逐語照合の唯一の検問)。**検問の実行は `tools/gate.py`(一括ラッパー・成功時1行/失敗時のみ詳細)を標準とする**(テスト数千本規模でもログがレビュー側の負荷にならない)。(c)はLinux側の開発でタイプミス1つを実機まで持ち越さないための門であり、実機の代用ではない（姉妹PJはLibreOffice緑のまま出荷して34ラウンド手戻りした）
  - 移植は `git checkout product/nexus-agent -- wintest tools/vba_lint.py tools/run_lo_tests.py`（**dist/ を巻き込まない**＝キー入りxlsm混入防止・パス厳密指定）。

## 2. フェーズ

```
W0 準備(雛形・シート) → W1 core移植＋起動シーケンス(modBoot) → W2 app層(知識・検証・プロンプト・パイプライン・企業ファイル)
→ W3 UI・出力・受信箱 → W4 結合・実機・チューニング → リリース(Phase 1)
Phase 1.5(12月～): PL-05/06ツール化・機構ライブラリ投入
Phase 2(来期): PL-07/08（判断台帳100件超が着手条件）
```
並行人間タスク（T-90系）はW1から開始。

## 3. タスク分解（Phase 1）

凡例: 担当 O=Opus / S=Sonnet / H=人間。DoD=受入条件（テスト含む）。ソースは `src/{core,app,ui,test}` 層別管理＋PoC方式のbuild/lint。

### W0 準備
| ID | タスク | 担当 | 依存 | DoD |
|---|---|---|---|---|
| T-01 | リポジトリ雛形＋移植（`git checkout product/nexus-agent -- wintest tools/vba_lint.py tools/run_lo_tests.py`）＋buildスクリプト移植。成果物に **`tools/prompt_diff.py`**（15章§10.1の抽出規約で15章から定数本文を抽出し、`src/**/*.bas` の対応関数の戻り値と突合。15章§10.2の節⇔関数名対応表を突合キーに使う）を新設 | S | - | lint緑・**`run_lo_tests.py` が全モジュール構文PASS**・空ブックビルド成功・`run_excel_tests.ps1` が `modTestRunner.RunAllPureTests` を実行してPASS/FAILを返す・`prompt_diff.py` が実行でき差分件数を終了コードで返す |
| T-02 | 本体ブック雛形: 11章の全シート（ヘッダ=13章物理名・保護・非表示・入力規則）＋config既定値投入 | S | T-01 | 13章の全シート・全列・全configキーと一致（照合スクリプトで検査） |
| T-03 | ナレッジブック雛形: 13章§3の全シート＋業種マスタ30行投入 | S | T-01 | 同上照合 |

### W1 core移植
| ID | タスク | 担当 | 依存 | DoD |
|---|---|---|---|---|
| T-10 | modConfig/modLog/modUtil/modUtilText/modTypes 移植（run_log・E07xx・TCaseCtx追加）＋**SetCellSafe新設**（16章NFR-S7: 外部由来テキストの唯一の書き込み口。E-46数式ガード） | S | T-02 | modTestsPureで config読取/ログローテ/SafeLeft/分割結合ユーティリティ緑。**SetCellSafeが先頭 `=` `+` `-` `@` を `'` 前置で無害化・NULバイト除去・32,000字切詰めする単体テスト緑** |
| T-11 | modJsonLite（ExtractJsonBlock/GetStr/GetLong/GetBoolJ/GetArrayItems/Escape/Unescape） | O | T-10 | 15章全スキーマのサンプルJSONを正しく抽出・分解。**14章§5の「ExtractJsonBlock 入力パターン表」の7パターン（①前後に説明文 ②コードフェンス囲み ③フェンス閉じ忘れ ④末尾途切れ〔閉じ括弧欠落〕 ⑤JSON2連結 ⑥全角波括弧・全角引用符 ⑦文字列値内の生改行とエスケープ済み引用符）を1テスト1パターンで実装し、各々が表の「戻り値」列（正常抽出 or 空文字列）と一致すること** |
| T-12 | modGatewayRPN（CallStep 3経路分岐/RibbonAvailable/RunLimitCheck/**帯域外成否規約 `ByRef ok`**（14章§6）/run_log記録） | O | T-10 | mockでs1～s4/pfの決定的応答。リボン無し環境でE0201。多経路の分岐網羅テスト。**LLMが `#ERR:` で始まる文字列を返しても ok=True のままであること（偽装エラーを成否判定に混入させない）を検査するテストを含む** |
| T-13 | modGatewayDirect（chat/completions+strict+リトライ+キー読込。14章§3） | O | T-12 | 実キーでs1実行（H検収）。キー無しE0205。429模擬でバックオフ動作 |
| T-14 | **modMockLlm**（本体内mockトランスポート。`src/test/` 配置・modGatewayRPN のmock分岐が呼ぶ唯一の相手。**`wintest/mock_ribbon/modMockRibbon.bas`＝ニセリボンちゃん（本体外の.xlamスタブ）とは別物**）: 浜松スイーツファクトリーで S1～S4×new/renewal＋PF＋**S2C/S3C**の決定的応答（**7 step種・11応答**。15章§8.1の表が正）＋**障害注入**（config `mock_fault`。15章§8.2の broken_json / broken_json_once / enum_violation / ghost_id / count_violation / empty / limit / fake_err） | S | T-03,T-12 | **mock実行前に、mock用ナレッジ行（M-0012 / L-03 / S-0004 / K-0003 / P9 / MC-0107）をナレッジ雛形（T-03）へ投入しておく**（実在チェックの相手が無いとmock応答が実在しないIDとして落ちるため）。**全11応答が modValidate に「自分のバリアント文脈」で合格**（MK-\*-NEW は case_type=new 文脈、MK-\*-RNW は renewal 文脈で15章§11のケースIDが警告含め1件も発火しない。文脈またぎの組合せは対象外。15章§8.1受入条件1が正。T-22後に再検証）。quality_mode=deep で `S2→MK-S2C-HIT→改訂` と `S2→MK-S2C-CLEAN→改訂スキップ` の両経路が流れる（S3Cも同様）。`mock_fault` の8値それぞれで15章§8.2の表どおりの応答を返し（broken_json_once は初回のみ破損・ResetFaultOnce でリセット）、`mock_fault` が空のときは異常応答を一切返さない |

### W2 app層
| ID | タスク | 担当 | 依存 | DoD |
|---|---|---|---|---|
| T-20 | modCaseStore（採番/case_type/分割保存/状態遷移/InvalidateDownstream/RepairStates/**FreezeRound**） | O | T-10 | 40,000字往復一致・E-10/12/22/23緑。**`FreezeRound(caseId)` が S2（edited優先で解決した1本）を `s2_prev_json` へ退避し、案件一覧の `round_no` を+1して新しい値を返す**（退避後に `s2_prev_json` が15章 S2 userの `{{prevS2Json}}` として取り出せること）。**S3が検証不合格でstatus=errorになった案件が、再起動時のRepairStatesで s3_done へ昇格しないこと**（`sN_json` は合格済のみ／`failed_step` は維持） |
| T-21 | modKnowledge（読込/検査E-34/スナップショット/フィルタ/整形/実在チェック×5種/AppendServiceGap退避/**MenusSummaryFor・MechsText・LastInjectedIds/ResetInjectedIds**） | O | T-03,T-10 | E-08/09/13/34緑。整形出力が15章の書式とdiffゼロ。**注入した整形テキストに現れるIDの集合と `LastInjectedIds()` の返す ";" 区切りID列が完全一致**すること（`ResetInjectedIds()` でStep開始時に空になる）。**機構ライブラリのシートが不在または0行のとき `MechsText()` が `(登録なし)` を返す**（E-09準拠。Phase 1の既定経路） |
| T-22 | modValidate（CheckS1～S4/PF＋CheckS2C/S3C。15章の検証ルール表を分岐網羅＋**NormalizeLlmJson**〔検証前段の配列重複排除。16章E-49〕） | O | T-11,T-21 | 正常5本合格。**15章§11の全ケースID（v2.6 現在: V-S1-01～13／V-S2-01～18〔V-S2-12 は欠番・V-S2-12b を含む〕／V-S3-01～21／V-S4-01～06／V-PF-01～07／V-S2C-01～05／V-S3C-01～05＝**計75件**）を1テスト1ケースで実装**し、テスト名にケースIDを含め、戻り値の各行が `[ケースID] ` で始まることをアサートする（§4-2の照合スクリプトが件数一致を検査する）。**`NormalizeLlmJson` が同名項目の重複を1件に畳み、除去件数を `removedCount` に返すこと**（E0303）。**15章§8.1のmock11応答を `modValidate` で全件検証し、各応答が自分のバリアント文脈（MK-\*-NEW は case_type=new、MK-\*-RNW は renewal、共通応答は両文脈）で警告を含め0件であること**（15章§8.1受入条件1の「T-22後に再検証」の実体はこのDoD。T-14の時点では modValidate が未実装のため検証できない） |
| T-23 | modPromptsCore/Blocks/Ops/modSchemas（15章と一字一句一致・**Const禁止の純関数方式**〔`Public Function SchemaS1() As String` を `s = s & "..." & vbLf` で組み立てて返す〕・30,000字契約遵守の分割） | S | T-10 | **`tools/prompt_diff.py` が0 diff**（15章§10.1の抽出規約と正規化規則: 改行はvbLf・末尾改行なし・VBAソース上の `""` は `"` に戻して比較・`{{...※...}}` の注記は除去・`※renewalのみ` の条件付き行は両側から除外）。schemaはJSONとしてパース可能。**`tools/vba_lint.py` の check_cp932_safe が0件**（プロンプト・スキーマ本文にCP932外の文字が1文字も無いこと。VBE注入で `?` 化するため） |
| T-24 | modPipeline（RunAll/RunStep/**参照優先 `sN_edited > sNr_json > sN_json`**/検証前段の `NormalizeLlmJson` 通し/修復リトライ/状態管理/入力打切り〔16章E-03〕/run_log） | O | T-20,T-21,T-22,T-23 | mock E2E: new・renewal両案件でs4_doneまで。E-06緑。**異常系は config `mock_fault` で観測可能な事実として検査する**: `broken_json_once` で run_log に validate_result=repaired が1行残り s4_done まで到達／`broken_json` で E0302・status=error となり sN_json_failed に生応答が残る／`ghost_id` で E0301・status=error となりS1/S2の結果は保持されS3から再開できる／`fake_err` で err_log にE0201が記録されず正常終了する。**run_log の `injected_kb_ids` が非空**であること。**同一項目が2回出るmock応答で重複が1件に畳まれ、除去件数がrun_logのdetailに残る**（E0303）。**run_log の detail に `extra_json`（14章§5パターン⑤でJSONを2本目以降破棄した件数）・`fw_normalized`（同前処理P0の全角記号置換件数）・E-49の除去件数が記録されることを mock E2E で確認する**（この3つはいずれもログ書込を伴うため層(a)＝modTestsPure では検査できない。ここが唯一の検問である） |
| T-25 | modInboxStore＋modPlayOps.RunPreflight（受信箱CRUD/一括診断/統制語彙必須E-41/関心度集計） | O | T-22,T-23 | mockでUC5一巡。E-40/41緑 |
| T-26 | modJudgeStore（判断台帳CRUD） | S | T-10 | 入力→保存→再表示一致。decision enum検証 |
| T-27 | modSparring（PL-04: CallChat・";;;"履歴連結・sparring_max_turns制限・履歴のcase_data保存/復元・受信箱送信） | O | T-20,T-23,T-25 | mockで対話3往復→保存→再開→履歴復元。E-44緑 |
| T-28 | 入念モード（S2C/S3C批判→改訂のパイプ組込・deep_transport分岐・s2c/s3c保存・改訂版の `s2r_json`/`s3r_json` 保存・充足度/批判のrun_log記録） | O | T-24,T-14 | mock deepでS2→S2C→改訂の3段が流れ、批判指摘ゼロ時（MK-S2C-CLEAN）は改訂スキップ。CheckS2C/S3C異常系テスト緑（15章§11のV-S2C-01～05・V-S3C-01～05）。**16章E-35: S2C/S3Cが修復1回で直らないとき批判をスキップして生成版を確定しStepは成功扱い**（run_logにstep=s2c/validate_result=failed、HOMEに警告）。**16章E-36: 改訂結果が本体スキーマ不合格のとき改訂を破棄し検証合格済みの改訂前JSONを採用**（Step失敗にしない。run_logにstep=s2r/failed）。**残項（W2c検証 MINOR4・W4結合＝T-40の前に結線）**: config `deep_transport` の経路上書きが未結線。14章§6の `CallStep` に呼び出し単位で経路を上書きする口が無いため、`DeepRouteOf` は解決だけを行い、指定がある間 `RunDeep` が usage_log に事実を残している（黙って標準経路で通してはいない）。結線には `CallStep` への経路引数の追加が要るので、14章§6の裁定とセットで行う |
| T-29 | **modCompanyFile**＋**modPii**: 企業ドシエファイル（1社1.xlsx）のI/O（13章§2.8。PoC modPack/modPackExport転用・ファイル名は `modUtilText.SanitizeFileName`）＋保存・外部送信・レポート出力前のPII走査（FR-45・16章E-05/E-31） | O | T-10,T-20 | **企業ファイル書出→再取込で dossier_rounds の S1～S3 JSON と dossier_notes の原文が往復一致**（32,000字分割を含む・欠落ゼロ）。**modPii の走査結果が dossier_meta のPII走査結果欄に記録される**（警告であり停止ではない）。案件開始時の「開く」で前回ラウンドの s1/s2 と notes が案件に復元される |
| T-16 | **modBoot**（起動シーケンス。`Workbook_Open` から呼ぶ唯一の入口。12章§2.1の7手順を固定順で実装）: ①ガードシート `はじめにお読みください` を非表示 ②config読込（13章§2.3の既定値を登録してからconfigシートで上書き。読込失敗は**E0608**警告で既定値のまま続行） ③`modCaseStore.RepairStates()` ④enum入力規則の隠しレンジ複製（**ナレッジ未接続でも19章レジストリの内蔵定数から復元して続行**） ⑤`modKnowledge.LoadKnowledge()`＋スナップショット保存（失敗はE0401警告のみで続行しスナップショットを使う） ⑥新サービス候補のローカル退避分の再送（失敗は無音で次回持越し） ⑦`modGatewayRPN.RunLimitCheck()`（**Trueでも起動は止めず案内のみ**） | S | T-10,T-12,T-20,T-21 | 7手順が**この順で1回ずつ**実行されることをトレースで検査。各手順に失敗を注入したとき12章§2.1の表どおりに挙動する（②欠損=既定値で続行しerr_log記録〔E0608〕／③失敗=修復0件で続行しE0603／④ナレッジ未接続=内蔵定数で復元し続行／⑤失敗=E0401警告のみで続行／⑥失敗=無音で持越し／⑦True=起動継続＋HOME案内）。**②より前にconfigを読む処理、③より前に案件シートへ書く処理が存在しないこと**をコード検査。**③～⑦はスタブではなく実体（modCaseStore/modKnowledge/modGatewayRPN）に結線した状態で検査する**（各手順が実体の公開関数を1回ずつ呼ぶことをトレースで確認し、スタブ差し替えでの合格を認めない）。**起動時に `protection_policy`（Locked ＋ `EnableSelection=xlUnlockedCells`）を全シートへ適用し、`ParkFocus` が機能すること**（`EnableSelection` は .xlsm に永続化されない実行時プロパティであり、ビルドは Locked だけを焼く。11章§5・16章 E-51）。**`case_data` の `data_key` 入力規則が隠しレンジ参照で有効化されること**（値が255字上限を超えインライン入力規則にできないためW0ビルドが省略した1件を、手順④の隠しレンジ複製で解消する） |

### W3 UI・出力
| ID | タスク | 担当 | 依存 | DoD |
|---|---|---|---|---|
| T-30 | modUIHome/modUIProgress（HOME・プレイ起動・進捗・ui_lock・赤帯） | S | T-24 | 11章HOMEワイヤー通り。E-11/27緑。**`modUIProgress.ParkFocus`（14章§6・16章E-51）を実装した時点で、`modBoot` が暫定で持つ private `ParkFocusAtBoot` を委譲へ差し替える**（フォーカス退避の実装を2箇所に残さない。裁定書7 B-10） |
| T-31 | modUICase（案件入力〔コンテキスト・**追加ドシエ**・**付保の見立て**・**追加収集（research_requests）**・匿名化ボタンE-31・連番「続き」欄を含む〕/S1～S4/FB/判断台帳の描画・編集読取・enum変換・E-01～05） | O | T-24,T-26 | 各シート編集→当該Step再実行が機能。E-05/31で正しくブロック/置換。**S2/S3の全スキーマ項目が往復編集できる**（JSON→シート→JSON で欠落ゼロ。S2は status/frequency_score/impact_score/insurability/transferability/line_note/control_note/loss_scale_note/preventions、S3は do_not_propose/unmatched_risks を含む。列とスキーマの写像は13章§2.13/§2.14の表、配列・入れ子のセル格納と逆シリアライズは13章§2.2の規約が正）。**追加ドシエ（input_dossier）・付保の見立て（input_coverage_note）の保存往復一致**、および**research_requests の各行の[コピー]操作でクリップボードに当該行が入る**こと |
| T-32 | modUIInbox（受信箱一覧・投函・診断表示・判定入力） | S | T-25 | 11章受信箱ワイヤー通り。E-41ブロック動作 |
| T-34 | modUISparring（壁打ちシート: 履歴表示・発話入力・「受信箱へ」送信ボタン・再開） | S | T-27 | 11章壁打ちワイヤー通り |
| T-35 | **modHtmlTemplate1..n / modHtmlTheme**: HTML骨格・セクション部品とテーマCSSを純文字列モジュールとして実装（18章〔HTMLレポートテンプレート仕様〕が正。Excelトークン・Run呼出し禁止=12章R4。**modExportHtml内へのテンプレ直書き禁止**） | S | T-24 | 18章のセクションID一覧・CSS変数名の閉じた一覧に準拠。**全モジュールが30,000字以内**であることを文字数検査（超過時は `modHtmlTemplate1..n` の分割を増やす）。**あわせて `modHtmlTemplate*` は18章§4.4の分割規約どおり1モジュール25,000字以内であること**（`tools/vba_lint.py` が `modHtmlTemplate*` 専用のERROR閾値として機械検査する。18章§4.4が本DoDへ検査を委ねているため、閾値の数値もここへ明記して委譲の輪を閉じる。25,000字は30,000字契約に対する余白であり、超過時は次番モジュールへ**関数単位で**切り出す）。**JsStringSafe は「すべての `<` を `\u003C` へ」（18章§5.3(1) v1.1）であり、生成HTMLの `var DATA=JSON.parse("…")` の文字列リテラル内に生の `<` が1文字も無いことを `tools/render_report.py` が機械検査する**（`</` 限定のエスケープでは `<!--<script>` を含むLLM出力でページが白紙化する）。config `html_theme` の値を切り替えるだけで配色が変わり、セクションを1つ追加する変更が2ファイル以内に収まる。**v1.2（裁定書21・11章§3.8）追加分**: 18章§5.1のCSS変数の閉じた一覧が**39個**であること・テーマ3本（`standard`／`mono`／`ds`）がそれを過不足なく定義すること・**SEC-17 growth**（`s3.growth_ideas[]`）が登録表と描画後DOMの双方にあること・**上部ナビと印刷用目次のアンカーが表示対象セクションと1対1**であること（11章§3.8.3(1)(2)）・18章§3.7の免責1文が逐語で入っていることを `tools/render_report.py` が機械検査する |
| T-33 | **modExportHtml（v2.3で主力出力に昇格）**: S1+S2+S3のJSONを固定HTMLテンプレート（10章FR-37の標準型構成・2軸リスクマップ5×5・移転可能性色分け・テーマCSS分離）に流し込み自己完結HTML1ファイルを生成＋modExportHearing（A4 1枚）。出力先は config `html_out_dir`、ファイル名は `modUtilText.SanitizeFileName` | O | T-24,T-35 | mockデータからHTML生成しブラウザで開き、リスクマップ・カバレッジ表・提案・ヒアリングの全セクション表示（H検収）。印刷プレビュー確認。**JSONは「1本のJS文字列リテラル＋JSON.parse」形式で埋め、`JsStringSafe`／`HtmlSafe` を必ず通す**（16章E-47。素のJSリテラル直書きが grep でゼロ）。生成失敗はE0502（16章E-48） |

### W4 結合・品質・リリース
| ID | タスク | 担当 | 依存 | DoD |
|---|---|---|---|---|
| T-40 | E2E結合（mock）: **UC1～UC5・UC7・UC8・UC9**のシナリオ手順書作成＋実施。**UC7（PL-04壁打ち）の必須確認点**=案件から壁打ち起動→`dossier_tier` が t3_sparring へ自動昇格→3往復→`sparring_max_turns` 超過時に古い履歴が切り捨てられて渡される（case_data には全履歴が残る）→受信箱へ `source_kind=field_voice` で送信→受信箱側でプリフライト診断が回る。modTestsPure は§4-1のランナー要件を満たすこと | S | T-13,T-16,T-28,T-29,T-30,T-31,T-32,T-33,T-34,T-35 | 手順書どおり全UC緑。modTestsPure が **FAIL 0件 かつ SKIP 0件 かつ 実行本数=tests_expected** |
| T-14b | **ニセリボンちゃん配管検証**: `wintest/mock_ribbon/modMockRibbon.bas` を実機Excelにアドイン（`.xlam`）として登録し、**12引数の `Application.Run` 配管そのもの**を検証する（引数の型と順序＝PoC確定シグネチャ〔Text, roleSystem, Temperature, MaxTokens, Wait, optModel, prevU, prevA, toolN, reasoning_effort, verbosity〕・**アドイン名の部分一致検出**・**LimitCheck の真偽解釈**・**toolN の中身**が受領側に届いていること。14章§3(b)・12章§2が正） | H+S | T-12 | 実機Excelでニセリボンちゃんを登録した状態で `modGatewayRPN.CallStep` が ribbon 経路を通り、①12引数が宣言順どおり型どおりに渡る ②アドイン部分一致でリボンを検出する ③`LimitCheck` が True/False いずれでも仕様どおり解釈される（Trueで起動を止めない）④送信した toolN の内容が応答へ反映される、の4点を確認 |
| T-41 | 実機テスト（ribbon）: 実在1社×new・1社×renewal完走 | H | T-40,T-14b | 4Step完走・**HTMLレポート生成（ブラウザで開き5×5リスクマップとカバレッジ表が描画される）**・**ヒアリングシート生成**・run_log の validate_result 集計（ok/repaired/failed の件数）確認 |
| T-42 | **敵対的レビュー1周＋Fix＋再レビュー**（読取専用レビュー。**実装コードを書いた主体は評者になれない**＝実装セッションとは別セッションで、レビュー中はコードを書かない）。レビュー観点は次の6本に固定する: ①データ喪失・破壊経路（削除・上書き・ロック解放漏れ）②失敗を成功として報告する箇所（戻り値・`ByRef ok` の取り違え・`On Error Resume Next` の握り潰し）。**あわせて `modGatewayRPN.CallStep` / `CallChat` の `ok` が `modGatewayRPN.DecideOk` の戻り値以外で決まっていないこと**を確認する（`ok` への代入は「関数冒頭の防御的初期化 `ok = False`」と「`ok = DecideOk(...)`」の2種類だけで、それ以外の `ok = True` / `ok = False` や `"#ERR:"` などの本文プレフィクスによる分岐が grep でゼロ。14章§6の帯域外成否規約の唯一の判定点を迂回させない）③外部由来テキストの3つの書込口（セル・CSV・HTML/JS）が `SetCellSafe` / `HtmlSafe` / `JsStringSafe` を迂回していないか（16章NFR-S7の①②③）④LLM出力ID（メニューID・ナレッジID）のホワイトリスト照合漏れ（16章NFR-S7の設計原則②・E-43）⑤エラー時に案件状態を壊す経路（10章NFR-R3違反。完了済みStepの結果保持と `last_ok_step`/`failed_step` の整合）⑥15章プロンプト・スキーマ／13章列名／19章enumとの逐語齟齬 | **O（読取専用）** | T-40 | 指摘を **B1..Bn の連番で全件列挙した裁定ファイル**を `docs/` に作成し、各件に「修正／受容（理由つき）」を司令塔が明記。修正指定分は再レビューで解消を確認。**「指摘ゼロ」での完了は不可**（最低でも観点6本それぞれについて受容判断の記録が残ること） |
| T-42b | **裁定書9（W4.1）の修正ウェーブ＋敵対的再レビュー1周**。T-42のレビュー指摘 B1～B22 と申し送り A-1～A-6 の裁定を、(1)仕様（11/13/14/16/17/19章）(2)実装（src・build）(3)手順書（docs/23・24・25）(4)テスト の4面へ反映する。**命名は裁定書9§1の N1～N8 に限る**（`modPipeline2.LastDeepOutcome` / `modCaseStore.PromoteTier` / `modCompanyFile2.DossierSaveAndClose` のBoolean化 / 名前付きレンジ `s1_case_id`～`s4_case_id` / 受信箱の `judge_to` 列 / 名前付きレンジ `ib_body_draft`〔裁定書10 C1で廃止＝投函下書き行方式。13章§2.6〕 / HOMEボタン `btn_round_freeze` / `modExportHearing.AnswerMemoCount`）。仕様の確定が先、実装はその後 | O+S | T-42 | ①B1～B22 が「修正済み」または「裁定で受容」のいずれかに確定 ②A-1/A-2/A-3/A-5/A-6 が解消し、残件は **A-4 のみ**（§6のバックログへ記録） ③`python3 tools/gate.py` 全ゲート緑（当時14本） ④変異テスト3点（B1のガード・B3の順序・B11のガード）で「わざと壊す→落ちる→復元→diffクリーン」を実証 ⑤敵対的再レビュー1周で**指摘ゼロまたは全対応**（新規指摘は勝手に直さず司令塔へ返す） |
| T-43 | プロンプト実戦チューニング: 実在3社→ベテラン3軸採点→15章改訂→回帰（評価入力セット保存） | H+O | T-41 | 平均3.5/5以上 or 改善課題リスト化 |
| T-47 | **modTestsExcel**（テスト3層の**層(b)の実体**。Excel固有E2Eスモーク。wintest実機でのみ走る） | S | T-40 | 次の全項目が実Excelで緑: `ws.EnableSelection = xlUnlockedCells`＋Locked設定下での `SetCellSafe` 書込／case_data 40,000字の分割保存・結合往復（1セル32,767字の実機確認）／ナレッジブックのUNC接続とスナップショット退避（E-08）／起動時 `RepairStates`（E-12）／`ui_lock` 多重クリック（E-11）と `llm_wait_sec+120` 秒超のロック失効（E0602）／32bitでの巨大文字列連結（E-26）／`DisableProcessWindowsGhosting` 有効時に長時間呼出でも画面がゴースト化しないこと（E-50）。SKIP 0件。**エントリ関数名は `modTestsExcel.RunAllExcelTests()` に確定**（14章§6のtest層。wintest実機からの唯一の入口とし、`run_excel_tests.ps1` はこの1本を呼ぶ）。**本数は `TE_EXPECTED = 45`**（`modTestsExcel` 34本＋`modTestsExcel2` 11本。`T47-00_層(b)本数の自己照合` が実行のたびに突き合わせる）。v3.2.1（裁定書22 仕上げ）で、v3.1 の予約行方式（`ci_paste_*` / `modUICase3.DrawCaseInput` / `SaveCaseInput` / `CaseSave`）を前提にしていた旧 Q9/Q1/V1 群 12本を**新経路（保管＋プレビュー）の Q9N 5本・V1N 2本へ書き換え**、`overflow` の永続ガード（Q1）は**事象そのものが v3.2 で消えた**ため削除した（跡地の理由は `modTestsExcel2` の「跡地」節が持つ）。**層(b)のテストは、前提とする名前付きレンジ・公開関数・シートが現行ブックに実在することを、仕様を変えた波ごとに1本ずつ点検すること**（前提不成立のまま残ったテストは、実機で赤くなるまで誰も気付けない） |
| T-48 | **ブック内テスト実行**（`modTestsRunnerUi.RunAllTestsFromBook`。**利用者向けの同等検問**。裁定書14 裁定5） | S | T-47 | `操作ガイド`⑤の[テストを実行]（OnAction=`modTestsRunnerUi.RunAllTestsFromBook`。引数なしなので Alt+F8 からも実行できる）で、**ps1と同一の4条件**＝FAIL 0件／SKIP 0件／純層の実行本数=期待本数（v3.2.1 現在 **663本**。`wintest/tests_expected.txt` が正）／層(b) 1本以上（v3.2.1 現在 **45本**。`modTestsExcel.TE_EXPECTED` が正）、をVBA側で判定して合否1行を MsgBox に出し、全文を `gd_test_result`（縦20行）へ書く（1行目=合否サマリ、2行目以降=`ReportText` の先頭から。FAILがあるときはFAIL行を先に。20行を超えた分は最終行を「（以下省略・残りN行）」。**最終行は打切り表示で置換されるため、そこにあった本文1行も欠落数Nに数える**）。期待本数は**ビルドが `wintest/tests_expected.txt` から config `tests_expected` へ焼き込む**（欠落・非整数はビルド失敗。v3.4・裁定書27 W9-A で `vba_src!E2` から移設）。実行時にこのキーが空・非数値なら**テストを実行せずNG**で終える（fail-closed）。実行前に「数分かかる・`err_log` に検査の行が残る」確認ダイアログを出し、[いいえ]で中止する。計数は `modTestRunner.PassCount` / `FailCount` / `SkipCount` / `ExecutedCount`（読み出しのみ。集計の仕方とR4は不変）から取る。実行順は `SetExpectedCount` → `RunAllPureTests` → 純層本数を控える → `RunAllExcelTests`（`ResetTests` を呼ばない設計のため必ず純層の後）。**`wintest/run_excel_tests.ps1` と§5-1の出荷手順は開発側の正として存続**し、本タスクは社内PC（ターミナル・PowerShell・Pythonが使えない）での同等検問＝24章の正とする |
| T-46 | **出荷前検問（自動化・毎リリース必須）**: ①**外部由来テキストの直書き検査**=対象 `src/**/*.bas` から `modUtilText`（防御関数の定義本体）を除外し、検出パターン `\.Value2?\s*=` ／ `\.Formula\w*\s*=` ／ `(Cells\|Range\|Offset)\([^)]*\)\s*=`（VBAで最頻の既定プロパティ代入 `ws.Cells(r,c) = s` を取りこぼさないため。表中の `\|` は正規表現の選択記号）でヒットを取る。定数・ヘッダの書込は行末に `' SAFE:const` の注記を必須とし、**注記の無いヒットのみFAIL**（HTML経路は「差し込む値が `HtmlSafe`/`JsStringSafe` を経由しない埋め込みが grep でゼロ」を同検問に含める。16章NFR-S7と同一定義） ②**キー走査**=16章NFR-S2の走査仕様4項目（対象=全trackedファイル＋成果物 `*.xlsm`/`*.xlsx` を `unzip -p` で全パート展開／検出パターン `sk-[A-Za-z0-9_-]{20,}`・連続32文字以上の `[0-9a-f]`・連続40文字以上のBase64様文字列・文字列リテラルに `OBF`・難読・`_KEY` を含む定数定義／`build/` 配下が `api_key.txt` または環境変数からキーを読む行が無いことのgrep検査／検出時はファイル名と行番号のみ出力しキー値は出力しない）でヒットゼロ。**検出パターンには誤検知ガード（文字種混在条件等）を加えてよい。ただし緩和は `ship_check.py` のコメントで宣言し、全小文字16進など鍵形状の検出力を落とさないこと** ③`git check-ignore` で `dist/` と `*.xlsm/*.xlsx` 成果物が実際に除外されていることを確認（**`docs/` 配下の仕様入力 xlsx〔種目ナレッジ確認シート等〕は成果物ではなく本項の対象外**。tracked のまま正しい） ④**wintest（実Excel・COM経由）全PASS**（`modTestsPure` と `modTestsExcel` の**両方**。SKIP 0件） ⑤**modTestsPure が FAIL 0件 かつ SKIP 0件 かつ 実行本数=`tests_expected`**（§4-1のランナー要件。SKIPをPASSに数えない） | S | T-42,T-47 | 5項目すべてPASS。1つでも落ちたら出荷しない（スクリプト化して `run_excel_tests.ps1` と同列の検問に置く）。①は `vba_lint.py` のルールとしても実装し(c)層で毎コミット走らせる |
| T-44 | 利用ガイド1枚＋15分スモークチェックリスト＋配布パッケージ | S | T-42 | 第三者が15分でセットアップ（H検収） |
| T-45 | リリース: 署名(OQ-2)→共有フォルダ→PJ展開→5～10社PoC開始 | H | T-43,T-44,T-46 | PoC開始 |
| **T-49** | **状態: 実装済み(検証待ち)。W6.1(裁定書22)で検証班の指摘を全件処置済み**（W6第1弾: 24章§5 T-41の実機2社完走が未実施のため、コード実装とゲート緑は済んでいるが最終検証が残る）。**W6第1弾「1画面ナビ」**（11章v3.2 §8.4 の第1弾。裁定書19/20/21）。①`ナビ`シート新設と `hm_*` / `ci_*` の**付け替え**（改名なし。11章§8.1）②コーチ帯（最上段をウィンドウ枠固定・STEP自動決定10行・[← 戻る][次へ →]・強調枠 `nv_focus`）③区画①の調べる文8本（`modUIResearch`。雛形は使い方タブの非表示行が唯一の値源）④**区画②の保管＋プレビュー貼付**（`modUICase6`。[ここに貼る]は `case_data` へ直接保存し、画面には状態行とプレビュー5行だけ）⑤既存ハンドラの結線（中身は1行も変えない）⑥**待ち時間の2段**（ステータスバー＋事前描画カード。11章§4.2）⑦`使い方`タブ7章立て（旧`操作ガイド`）⑧**既定の可視タブを2枚へ**（他は実行時に可視化＋[ナビへ戻る]） | S | Z-13 | **全16ゲート緑**。純層 612→630→**663**本（W6第1弾で `StripDrFooter` 7／`PreviewLines` 4／`SplitFieldNotes` 7、W6.1 で `FillTemplate` 11／`AreaTable`・`HandlerName` 3／`StepText`・`StepAnchor` 2／`StepFor` 10／`FitsInRows` 2 の計28本。`wintest/tests_expected.txt` を同時更新）・層(b) 47→50→**45**本（W6.1 でナビの貼付3本を足し、仕上げで v3.1 前提の旧12本を新経路7本へ書き換えた。`TE_EXPECTED` を同時更新）。`sheet_check` が13章§2.9の可視性表・§2.10の名前付きレンジ**61本**・§2.18の `gd_` **15本**（W6.1 で `gd_btn_adv_act` を追加）と一致。`caption_check` の値源が `modUINav` の配置表（`UN_ROW_*`）へ移り、使い方タブの早見（`GUIDE_NAV_BUTTONS`）・13章§2.10(f)・初回ツアーと逐語一致。openpyxl 読み戻しで `ナビ` の `freeze_panes` と `state=visible`、`ナビ`/`使い方`以外が `hidden`/`veryHidden` であることを確認。**`modUICase3` / `modCaseStore` / `modPipeline` / `modPipeline2` には1行も足さない**（区画②は新設 `modUICase6` だけで完結する。11章§8.1(c)） |
| **T-49b** | **状態: 完了**。**W6.1「検証班指摘の全件処置」**（裁定書22）。major 4件（`{{企業規模}}` 露出＝`FillTemplate` の差込表化／現場メモ60行超の fail-closed 化／③の段別進捗／新設モジュールの回帰網）・minor 9件（貼付時刻の持ち越し・直貼り失敗の理由分け・`DrawOk` ガード・例文消去の仕様側訂正・`hasBody` の一本化・波ダッシュ統一・`NoticeBusy` のトースト・PII検査の前倒し・トースト秒数の可変化）・info 2件（`U6_PREVIEW_HEAD` 撤去・15章§2.0 の集合一致検問）・逐語逸脱 D6/D7/D8/D9/D10/D12/D13・**使い方タブ⑦への上級ボタン3本の配線**（[第2ラウンドを始める][企業ファイルへ保存][企業ファイルを開く]。v3.2 で押す口が消えていた）・`modUIToast.ShowResearchPrompts` の廃止 | S | T-49 | **全16ゲート緑**（検問を2つ拡張: `enum_check` へ15章§2.0⇔`NT_FOOTER_WORDS` の集合一致〔fail-closed〕、`vba_lint` へ `build/build_rpn.py`・`build/sheets_main.json` の**文字列**に対する危険6字検査〔ERROR〕。`caption_check` へ⑦上級の動作ボタン3本の3系統照合を追加）。純層 635→**663**本・層(b) 47→**45**本（ナビ貼付3本を追加し、v3.1 の予約行方式を前提にしていた旧 Q9/Q1/V1 群 12本を新経路の Q9N 5本・V1N 2本へ書き換え。Q1 は対象事象の消滅により削除）。新設 `modUICase7`（`modUICase6` の分割先）・`modTestsPure14`。申し送りは Z-16（取り消し線の語彙）・Z-17（`modUIGeom` の改名）へ登録 |

### W7 センターピン整合（裁定書25。仕様=班A確定済み → 実装）

**前提**: 上流の正は 15章 v2.6 / 13章 v2.6 / 18章 v1.3 / 11章 v3.3 / 12章 v2.6 / 10章 v2.5 / 19章 v2.6。**この7章に書かれていない変更をコードで足さない**（迷ったら司令塔へ返す）。**30,000字契約**: `modPipeline` / `modCaseStore` / `modUICase3` は満杯であり1行も足せない。新設モジュールで受ける（`modPrompts*` 系も着手前に残字数を確認すること）。

| ID | タスク | 担当 | 依存 | DoD |
|---|---|---|---|---|
| T-55 | **W7 core（班B）**: 15章 v2.6 を `modSchemas` / `modValidate` / `modPrompts*` へ写す。①`SchemaS1()` へ `financials`（6キー・`source` は6値enum）と `current_coverage[].certainty`（confirmed/assumed）を追加し **required に全列挙・`additionalProperties:false` を維持** ②`SchemaS2()` の `insurability` を `line_note` / `gap_note` / `control_note` の3キー（＋`transferability`）へ ③`SchemaS3()` へ `talk_script`（`opening` maxLength 80 / `flow` minItems 3・maxItems 5 / `closing` / `taboo`）を required で追加 ④`modPromptsBlocks` へ **`BlockNewS2()`**（15章§1.2b）と **`BlockRound2Focus()`**（同§1.2c）を新設（無引数のテンプレート関数。差し込みは `modPromptsOps` の組立層） ⑤`BuildS1System` 第10ルール・`BuildS1User` の【決算・財務】ブロックと出力JSON例 ⑥`BuildS2System` ルール10/11/13の改訂・`BuildS2User` の `{{incidentsText}}` 枠と2ブロックの挿入・※行の差し替え ⑦`BuildS3System` 第12ルール・`BuildS3User` の生成指示1行と `talk_script` のJSON例・※2行 ⑧`BuildS4System` の「PowerPoint骨子」→「提案書骨子」 ⑨`modValidate`: **V-S1-12 / V-S1-13 / V-S2-12b / V-S2-18 / V-S3-19 / V-S3-20 / V-S3-21 を新設、V-S2-12 を削除、V-S1-04 と V-S2-16 を改訂** | O | （班A仕様確定） | ①`tools/prompt_diff.py` の差分**0件**（15章の本文と `.bas` の戻り値が一字一句一致。§10.2 の対応表は `BlockNewS2` / `BlockRound2Focus` を含む**33関数**）。**`BLOCK_RENEWAL_S1` / `BLOCK_RENEWAL_S2` から「新規案件では空配列にする。」の1文が消えた**ことも同差分に含まれる ①b **`tools/validate_check.py` を枝番と欠番へ対応させる**（現状の `CASE_ID` / `ROW_11` / `ROW_RULE` は `V-S2-12b` を読めず `V-S2-12` を欠番として扱えない。範囲展開で得た `V-S2-12` を `V-S2-12b` へ読み替えるか欠番リストを持たせる。**この対応まで `validate` ゲートは赤である**＝仕様の誤りではない。15章§11の枝番の注記が正） ②`modTestsPure` に**新設・改訂した各検証ルールへ正例1本・負例1本以上**（V-S1-12 / V-S1-13 / V-S2-12b / V-S2-18 / V-S3-19 / V-S3-20 / V-S3-21 と、条件を変えた V-S1-04 / V-S2-16）。**変異テスト**（わざと条件を反転させると落ち、戻すとdiffクリーン）を V-S2-12b と V-S3-19 の2点で実証 ③17章§4-2 の照合が **15章§11 の計75件**（CheckS1 13 / CheckS2 18〔V-S2-12 は欠番〕/ CheckS3 21）と一致 ④15章§8.1 の mock 11応答を更新（MK-S1-NEW/RNW へ `financials`・`certainty`、MK-S2-NEW へ `uninsured` の gaps 2件と `gap_note`、MK-S3 へ `talk_script`）し、**各mockが自分の文脈で警告0件**（§8.1 受入条件1） ⑤`vba_lint.py` の `check_cp932_safe` / `check_docs_prompt_cp932` 0件 ⑥`python3 tools/gate.py` 全ゲート緑 |
| T-56 | **W7 app/ui/出力（班C）**: 13章 v2.6・11章 v3.3・18章 v1.3 を写す。①`modCaseStore`（満杯のため**分割先の新設モジュールで受ける**）: `data_key` へ `input_finance` を追加（全29値）・案件一覧へ `adopted_story_nos` / `focus_line_ids` の2列・`FreezeRound` で `used_proposals` → `adopted_story_nos` → `focus_line_ids`（採用storyの `line_ids` から導出・**種目マスタ実在チェック**）の写し込み ②`modKnowledge*`: `事故事例` シートの読込と `IncidentsFor(industryCode)`・1行整形（15章§3の書式）・`LoadKnowledge` の `category` 10分類検査（E-34）／種目マスタ2列（`deep_dive_points` / `uw_info_needs`）／成功事例2列（`outcome` / `why_it_failed`）／15章§0.7 の切詰め順（**成功事例の次に事故事例**・config `kb_incident_rows` 既定5） ③ビルド台帳: `build/sheets_kb.json`（事故事例8列・種目マスタ2列・成功事例2列）／`build/sheets_main.json`（貼付欄の7本目・案件一覧2列） ④13章§2.12 の `s1_basic` へ `fin_*` 6列・`s1_current_coverage` へ `certainty` 列、§2.14 へ `s3_talk_script` ブロック（**`sN_edited` の逆シリアライズで新キーが落ちないこと**が受入の要）／§2.10(c) の名前付きレンジ **61→65本**（`ci_count_finance` / `ci_prev_finance` / `ci_raw_finance` / `ci_sent_finance`）／config `kb_incident_rows`（既定5）の追加 ⑤`modUINavDraw` / `modUICase6`: **7欄目の描画は既存6欄の複製で最小**（`ci_prev_finance` / `ci_raw_finance` / `ci_sent_finance` / `ci_count_finance` と3ボタン）・**「いまの契約」の出る条件を常時表示へ**・11章§3.3.3(a) の説明1行と例文2行を**逐語**で ⑥`modUICase6` の現場メモ分解: 【付保の見立て】節を `input_coverage_note` へも保存（**`input_memo` からは落とさない**＝二重保存。13章§2.11(d)） ⑦`modHtmlTemplate3`: SEC-08 を**リスク単位の8列表**＋現契約表＋gaps表の3枚へ（18章§3.8。`insurability` があれば必ず描く） ⑧**`modHtmlTemplate8` 新設**: SEC-18 talk と `TalkCss`（18章§3.9・§4.4） ⑨`SectionsJs` へ SEC-18 の登録行（SEC-10 の直後）／`LabelJs` へ `certainty` / `financials.source` / `case.outcome` の日本語ラベル ⑩**検問スクリプトの追随（`tools/`。班Cが同時に直す）**: `render_report.py` の `SECTIONS` を**18行**へ（SEC-18 追加）／`parse_growth_note` の切り出し範囲を `"### 3.7 "`〜`"## 4. "` から **`"### 3.8"` まで**へ狭め（現状は §3.9 の固定文まで拾い「§3.7 SEC-17 の免責固定文」と誤ラベルする）、§3.9 用の抽出を1本足す／`enum_check.py` の分類表へ **`current_coverage.certainty` / `financials.source` / `case.outcome`** の3行を REQUIRED か EXCLUDED で登録（未登録だと fail-closed で赤のまま）／`sheet_check.py` の名前付きレンジ期待値を **65本**へ | S | T-55 | ①`sheet_check` が13章§2.11(a) の7欄・案件一覧の新2列・`事故事例` シートの8列と一致 ②`enum_check` が19章§3の新3 enum（`certainty` / `financials.source` / `case.outcome`）と `modUICase.EnumPairsCsv` の集合一致（**fail-closed**） ③`caption_check` が7欄目の説明・例文を11章§3.3.3(a) と逐語一致 ④`render_report.py`: **新規案件（`current_coverage`=0件・`gaps`=2件）のDATAで SEC-08 が描画され**、`s3.talk_script` を与えたとき SEC-18 が本文と目次の両方に出て、与えないとき**両方から消える**こと。18章§3.9 の固定文1本と§3.8の注記1本を逐語照合 ⑤`vba_lint.py` の `modHtmlTemplate*` 25,000字ERROR に抵触しない（`modHtmlTemplate8` を新設したのはこのため） ⑥層(b)へ `input_finance` の保存・結合の往復1本を追加し `TE_EXPECTED` を同時更新 ⑦`python3 tools/gate.py` 全ゲート緑 |
| T-57 | **W7 検問と回帰の同期（班B+班C）**: 純層の期待本数（`wintest/tests_expected.txt` と config `tests_expected`）・層(b)の `TE_EXPECTED`・§4-2の検証ケース件数（**75件**）・`tools/gate.py` のゲート一覧を、T-55/T-56 の増分に合わせて**同一コミットで**更新する。あわせて T-40 の mock E2E を新規案件で1周回し、**新規案件のレポートに SEC-08（カバレッジ表・未充足リスク一覧）と SEC-18（経営層への話し方）が出ること**を目視確認する | S | T-55,T-56 | ①`modTestsPure` が **FAIL 0件・SKIP 0件・実行本数=`tests_expected`** ②`python3 tools/gate.py` 全ゲート緑 ③mock E2E（`case_type=new`）で `s4_done` まで完走し、生成HTMLをブラウザで開いて SEC-08 のリスク単位表と SEC-18 が描画される（**この2点が W7 の受入の中心**。裁定書25 S1・S2） |
| **T-58** | **W9-A 配布方式B: 完成品 `vbaProject.bin` を生成する**（裁定書27。Z-19 の実施）。①`build/ovba_write.py` を新設（MS-OVBA準拠のライター＋CFB書き出し。`VBA/dir` の `PROJECTINFORMATION` / `PROJECTREFERENCES` は `build/template_skeleton.xlsm` から**写し**、`PROJECTMODULES` と各モジュールストリーム・`PROJECT` の `Document=`/`Module=`/`Class=` 行と `[Workspace]`・`PROJECTwm` を**生成**する。全モジュール `MODULEOFFSET=0`／`_VBA_PROJECT` は Version `0xFFFF`＋キャッシュゼロ埋め＝p-code無効）②`build/build_rpn.py` に `--vba-mode baked|installer`（**既定 baked**）。baked では `vba_src` シートを作らず、自己インストーラの外科パッチも当てない。`installer` は1リリース限りの開発用フォールバック③ブック内テストの期待本数の値源を `vba_src!E2` から **config `tests_expected`** へ移設（13章§2.3・`modTestsRunnerUi`）。`vba_src!E3`（焼き付けマーカー）は廃止④新ゲート **`bin-roundtrip`**（`tools/bin_roundtrip.py`）と **`lo-xlsm`**（`tools/lo_xlsm.py`）を `tools/gate.py` へ登録（16→**18ゲート**）⑤`tools/ship_check.py` へ⑥「配布物のAV表面積と経路の固定」を fail-closed で追加（`llm_transport=ribbon` / `direct_api_base` 不在 / mock 不在 / `vba_src` 不在 / bin に禁止文字列 `VBProject`・`AddFromString`・`ExecuteExcel4Macro`・`WScript.Shell`・`new:{` が無い） | M | Z-19 | ①`bin-roundtrip`: 配布 `.xlsm` の bin を **oletools.olevba**（第三者実装）で解凍し、各モジュールが `src/` の `.bas`（ヘッダ除去・CRLF・CP932）と**バイト一致**／モジュール数と集合が `build/modules.json` と一致／document module 集合が `{ThisWorkbook}` ちょうど（`Sheet1` 等は焼かない。集合一致条件は緩めない）／`vba_src` シート不在／禁止文字列不在／全 `MODULEOFFSET=0`。②`lo-xlsm`: 配布 `.xlsm` を `soffice --headless` に開かせ、ドキュメントの `VBAProject` ライブラリに**全モジュールが載る**こと（LOが黙って落としたモジュールを検出する）と、document module 集合が `{ThisWorkbook}` ちょうどであることと、**配布 bin から取り出したソース**で既存 `lo-compile` と同じ隔離ライブラリ方式のコンパイルが全モジュール成功すること。実行前提として **LibreOffice 24.2系 + libreoffice-calc**（xlsm import フィルタ）を検出し、無ければ「ライブラリ0本」を緑にせず理由を明示して環境不備（exit 2）で止める。③CFB ディレクトリ名ソートは非ASCIIのストリーム名を fail-closed で拒否する（[MS-CFB] 2.6.4 の大文字化規則が非ASCII文字で Python の `str.upper()` と一致する保証が無いため）。④`python3 tools/gate.py` **全18ゲート緑**。⑤実機（Excel）確認は利用者環境で行う（Linux側では代替できない。24章§8 は「置くだけ」へ改訂） |

| **T-59（班1）** | **W10 データ分離の土台（裁定書28。ランチャー・data_dir・ログcsv・設定.txt）**: ①`build/build_rpn.py` が `dist/リスク提案ナビを起動.bat` を生成（CRLF・CP932・ASCIIコメント。`SRC=%~dp0` / `DST=D:\リスク提案ナビ`（D: 無しは `%TEMP%` へ退避し `echo` で警告）/ `mkdir` / `xcopy /D /Y` で本体とナレッジブック / `%DST%\data_dir.txt` に `%SRC%データ` / `start "" excel.exe /x`）②`tools/ship_check.py` へ⑦「ランチャーの形」（存在・CRLF・CP932・`start ""` 形式・excel のフルパス無し・裁定の4手順）③`modUtil.DataDirCandidates` の先頭に **pointer 引数**を足し、解決順を `data_dir.txt` → config → `%OneDriveCommercial%` → `%OneDrive%` → Documents(warn) にする（読取は `modUtil.ReadDataDirPointer`。本体の置き場所は `modUtil.SetBookDir` で `modBoot` が預ける）④ナレッジブック探索順を「本体と同じフォルダ → data_dir の親」へ（候補列挙は純関数 `modUtil.FileCandidatesIn`）⑤`modLog` の `err_log`/`run_log` 追記時に `<data_dir>\ログ\*.csv` へも1行追記（1行組立ては純関数 `CsvLineOf`/`CsvField`。失敗は握って本体のシートにだけ残す）⑥`<data_dir>\設定.txt`（key=value・`#`コメント・UTF-8）を起動手順(2)で読み、許可キー8つだけを `modConfig` へ上書き（パーサは純関数 `modConfig.ParseSettingsText`）⑦docs/24 の利用者手順＝ランチャーのダブルクリック・SmartScreen 解除・bat が動かない端末の代替、§8 の発行者手順＝配布フォルダの差し替え | M | T-58 | ①`python3 tools/gate.py` 全18ゲート緑 ②純層 `wintest/tests_expected.txt` **761 → 772**（`modTestsPure19` 11本: pointer 有/無/空 3本・探索順1本・csv 無害化3本・設定.txt パーサ4本）③`tools/ship_check.py` ⑦が PASS ④13章§2.3（設定.txt・解決順）・§2.4（csv 複製）・16章 E-59/E-60/E-61 の登記 |
| **T-59（班2）** | **W10 データ分離の本体（裁定書28。企業ファイルへの自動保存と起動時再構成）**: `modCompanyFile*` / `modCaseStore*` / `modBoot` の起動時再構成（`<data_dir>\企業\` を走査して案件一覧を組み直す）・企業ファイルのスキーマ拡張とバージョン列（前方互換）・自動保存（貼付の保管／S1〜S4の完了／商談の記録・判断台帳の追記／第2ラウンド確定のたびに無言で保存。失敗は toast「OneDriveに保存できませんでした。次回の保存で再試行します」＋err_log）・13章§2.8・11章（保存済み表示 hh:mm） | M | T-59（班1） | ①層(b) 企業ファイル往復（保存→読込で全項目一致）②起動時再構成で案件一覧が組み直る③`python3 tools/gate.py` 全18ゲート緑 ④純層 **772 → 779**（`modTestsPure20` 7本。班1が同名 `modTestsPure19` を使ったため統合で `modTestsPure20` へ改名した） |
| **T-59（統合）** | **W10 の統合（班1＋班2）**: ①`tools/run_lo_tests.py` の `PURE_ALLOWLIST` を和集合で解消（班1の `modTestsPure19` と班2の `modTestsPure20` を両方登記）②RunAll 連鎖を `Pure18 → Pure19（班1 11本）→ Pure20（班2 7本）` に繋ぐ③**PII と自動保存の裁定**: 自分の `data_dir` への自動保存は「共有・送信」ではないので PII 検知があっても保存し、`dossier_case.pii_flag`（`TRUE`/`FALSE`）に印を残す（16章 E-05(7) を共有・送信・配布に対する規定へ改訂・13章§2.8 同期。写像は純関数 `modCompanyFile3.CaseSheetCols` / `PiiFlagOf`）④自動保存失敗の発生コードは既存 `E0603`（`save_failed`）の流用と 16章 E-59 に明記⑤`build/modules.json` / `tools/vba_lint.py` の登記⑥docs/24 §7 に実機観察2項目 | S | T-59（班1）・T-59（班2） | ①`python3 tools/gate.py` 全18ゲート緑 ②純層 **779 → 780**（`modTestsPure20` に pii_flag の写像1本）③`sheet_check` が緑（班2の `hm_saved_at` と班1の config 行が両立）④`modBoot` が 30,000字未満⑤変異注入: `dossier_case` の `pii_flag` 列を写像（`CaseSheetCols`）から落とすと純層が落ちる |
| **T-60** | **W10.1 Macベンチの緑化とパス連結の一元化（裁定書29）**: 2026-09-05 の利用者Mac実測で層(b)が7本落ちた（純層 780=780 は全合格）。原因は (a) テストの一時フォルダが `Environ$("TEMP")` 決め打ちで、Macには TEMP が無い（TMPDIR）ため SaveAs失敗 Err=1004・UTF8書出失敗・企業ファイル往復の前提不成立 (b) 製品側にパス区切りの決め打ち連結が6箇所残っていた（`modBootData` 63/86・`modCompanyFile` 90・`modCompanyFile3` 150・`modExportHtml` 375/383。`modUtil.PathSep` があるのに通っていない） (c) 貼り付けの書式名（「Unicode テキスト」/「Unicode Text」）がMac版Excelに無く `PasteSpecial` が全滅、の3つ。**製品の対象はWindowsであり製品挙動は変えない**。①core へ `modUtilPath` を新設（`JoinPathWith`（純）/ `JoinPath` / `TempDir` / `FileNameOf`（純）。`modUtil` が30,000字契約で満杯のため分割。**Excelトークンは1つも持たない**＝R4の例外を作らない。Mac判定 `IsMacExcel` は層(b)のSKIP判定にしか使わないので core ではなく**テスト層の `modTestsExcel3`** が持つ）②製品側6箇所を `JoinPath` へ、`modCompanyFile` の `BaseNameOf` を `modUtilPath.FileNameOf` へ置換（Windowsでは同一文字列）③テスト側3箇所（`modTestsExcel.TempFilePath` / W9-03 / `modTestsExcel3` の W10）を `TempDir` + `JoinPath` へ④層(b) W9-01 と W61-15 に **Mac限定のSKIP**（`modTestsExcel3.IsMacExcel` かつ貼り付け不成立のときだけ緑。detail は `SKIP(Mac): 貼り付け書式名がMac版Excelに無い`。**Windowsでは新しいSKIP経路を作らない**）⑤`tools/vba_lint.py` へ2規則（パス連結の `& "\"` / `"\" &` 禁止＝`src/test` と `modUtil`/`modUtilPath`/`modJsonLite`（JSONエスケープ）を除く／`Environ$("TEMP")`・`("TMP")`・`("TMPDIR")` 禁止＝`modUtil`/`modUtilPath` を除き `src/test` にも効かせる）と正負の自己テスト⑥`build/modules.json` / `tools/run_lo_tests.py` の `PURE_ALLOWLIST` / 12章§2・§4 の登記 | S | T-59（統合） | ①`python3 tools/gate.py` 全18ゲート緑 ②純層 **780 → 786**（`modTestsPure20` へ W101 群6本: `JoinPathWith` 4本＝末尾区切り重複なし・dir空→""・sep "/"・tail先頭区切り、`FileNameOf` 2本＝`\` と `/`）③層(b)は **53本のまま**（SKIPは本数を変えない）④変異注入: 製品側1箇所を `& "\" &` へ戻すと lint が赤／`JoinPathWith` の `TrimTrailingSep` を外すと純層が赤 |
| **T-61** | **W11-b 保存先の解決から環境変数を全撤去＋起動batの受け渡し（裁定書31）**: 2026-09-05 の会社PC実測で、`%OneDrive%` が**別の利用者（win11admin）のフォルダ**を指し `%OneDriveCommercial%` は未定義だった。環境変数で OneDrive を探す方式は**不成立**であり、`.xlsm` を直接開くと他人の OneDrive へ企業データを書きに行きうる。①`modUtil` の data_dir 解決を **(1)`data_dir.txt` → (2)config `data_dir`（`%…%` を含む値は展開せず捨てる）→ (3)本体と同じフォルダの `\データ`** の3段へ書き換え、`DD_VAR_*` / `ExpandDirVars` / `IsUnderOneDrive` / `DataDirNotOneDrive` を撤去（`DataDirCandidates` の署名を `(pointerRaw, configRaw, bookDir)` へ。`DataDirLastResortOf` / `DataDirIsLastResort` を新設）②config `data_dir` の既定を**空**にし（`build/sheets_main.json`・13章§2.3）、`modBoot` の `BOOT_DATA_DIR_DEFAULT` を撤去③warn の判定を「OneDrive 配下でない」から「**(3)で解決した**」へ変え、文言を逐語「保存先が確認できません。『リスク提案ナビを起動』から開き直してください。このまま使うと、保存したものはこのパソコンの再起動で消えることがあります。」へ④`tools/ship_check.py` に **⑧ 手順書のコードブロック == `dist` の `.bat`**（CRLF→LF 正規化のうえバイト比較）と自己テスト（正例1・負例3）を新設⑤docs/24 §8.1c-1「起動batを会社PCへ持ち込む」（主経路＝zip を SharePoint に置く／保険＝メモ帳・ANSI・ファイル名まで。**メールは .bat も zip も通らない**）⑥13章§2.3・14章§6・16章 E-60・docs/24 §7・docs/25・docs/28 §3 の同期 | S | T-60 | ①`python3 tools/gate.py` 全19ゲート緑 ②純層 **771 → 773**（`modTestsPure18` の W9C2 3本と `modTestsPure19` の W10A 3本を撤去し、`modTestsPure19` へ W11B 群8本＝解決順・`%…%` を捨てる（負）・両方空なら逃げ場・末尾区切りの重複なし・空白だけのポインタ・本体フォルダ空で候補ゼロ（負）・`DataDirIsLastResort` の正負）③層(b)は **54本のまま**（触っていない）④変異注入: `DataDirCandidates` へ env 経路を1本戻すと純層が赤／docs/24 のコードブロックの1行を変えると ship ⑧ が赤／config の `%…%` を展開する実装へ戻すと純層が赤 |
| **T-62** | **W11-c リボンの抽出切断への対策（裁定書33）**: リボンちゃん ver202606 の実ソース（`log.bas` の `parseText` / `ExtractText`、`GPT.bas` の `UnEscapeJSON`）を読み、200応答の本文が **始点 `content":"` から終点 `","` / `"},` / `"`+LF の最も手前まで**で切り出されることを確定した。当方が返させるJSONに `"},` があると**そこで切られる**（16章 E-63）。①15章§1.3 BLOCK_GUARD と §7 RepairSuffix へ整形JSON規則を1文ずつ追加（`modPromptsBlocks.BlockGuard` / `modPromptsOps.RepairSuffix` と逐語一致）②`modMockLlm` / `2` / `3` の応答を整形（`"}` の `"` と `}` の間に改行。JSONの意味は変えない）し、新ゲート **`wire`**（`tools/ribbon_wire_check.py`。mock 応答に `"},` があれば赤・自己テスト3件つき）を `tools/gate.py` へ登録（19→**20ゲート**）③`src/test/modRibbonSim.bas`（相手側3関数の逐語模擬。純関数）と `src/test/modTestsPure21.bas`（W11C 7本）を新設④`src/core/modRibbonWire.bas`（`LooksRibbonCut` / `CutNote`）を新設し、`modPipeline.FailStep` / `modPipeline2.CallGuarded` / `modPlayOps.CallGuarded` の3箇所で err_log の detail 末尾へ ` ribbon_cut?` を添える（**新コード無し**）⑤14章§2 の「Azure OpenAI応答の素通し」を訂正（3置換と終点規則を逐語で明記）・16章 **E-63** を新設・12章§2 と `build/modules.json` へ3本を登記 | S | T-61 | ①`python3 tools/gate.py` 全20ゲート緑 ②純層 **773 → 780**（`modTestsPure21` の W11C 7本＝1行詰めが切れる／整形は切れない／`": "`→`":"`／`\uXXXX` と `\"` の UnEscape／mock 11本の往復一致／mock_fault 6本の往復一致／`LooksRibbonCut` の正例2・負例2）③層(b)は **54本のまま**（`wintest/mock_ribbon` は触らない＝配管検証専用） ④変異注入: mock の改行を1箇所外すと `wire` と W11C-05 が赤／`SimParseText` の終点候補から `"},` を外すと W11C-01 が赤／`BlockGuard` の追加文を1字変えると `prompt-diff` が赤 |

**T-58 の追記（W9.2・実機第3報 2026-09-03）**: Mac の実Excel で配布物を開いた直後に **「実行時エラー 5: プロシージャの呼び出し、または引数が無効です」（[OK] のみの生ダイアログ）** が出た。原因の候補を1つずつ潰し、T-58 の成果物へ次を反映した。**(1)** 焼き付ける `ThisWorkbook` の `Workbook_Open` を `Application.Run "'" & ThisWorkbook.Name & "'!modBoot.Boot"` から **直接呼出 `modBoot.Boot`** へ（ホストによる文字列でのブック名解決を1経路まるごと消す）。**(2)** `modUIToast` の `Application.OnTime` の手続き名からブック名修飾を外し（`"modUIToast.HideToast"`）、`clsAppEvents.App_WorkbookBeforeClose` から `CancelToast` を `RestoreScreen` の前に呼ぶよう結線した。**「OnTime はブック名で修飾する」という規約は 16章・17章のどこにも存在しない**ことを確認済み（改訂すべき既存規約は無かった）。骨抜き防止として `tools/vba_lint.py` に **`check_workbook_qualified_proc`**（`ThisWorkbook.Name` と `!` を含む文字列リテラルの連結を ERROR）を新設し、修飾へ戻す変異が lint で必ず赤くなることを実証した。**(3)** パス区切りの決め打ちを撤去（`modUtil.PathSep()` 新設＋純関数 `modUtil.SplitPathParts(t, sep)` へ分割ロジックを切り出し、`modBoot` は `Application.PathSeparator`、`modUIGuide.NoticeKbMissingOnce` は既定プレースホルダ `\\...\` を `Dir$` に掛けない、`modUIHome2.OutDir` にハンドラ＋`E0501`）。純層に `modTestsPure18` の **W92 群3本**（`\` / `/` / UNC先頭）を追加し `wintest/tests_expected.txt` は **761**。**(4)** 起動経路の最外周に網を張った（16章 **E-58**・`E0603`）。**(5)** `modUISparring` の絵文字サロゲート組立（`ChrW(&HD83D&) & ChrW(&HDCA1&)`）を撤去しボタン名から電球を落とした（11章§8.6 の趣旨）。**(6)** `build/ovba_write.py` が template の `dir` 先頭ブロックを丸写しする際に、**MSForms（`{0D452EE1-E08F-101A-852E-02608C4D0BB4}` / fm20.tlb）への REFERENCE を [MS-OVBA] 2.3.4.2.2 の単位で除去**するようにした（W9-B で `DataObject` を撤去済みで不要／template を作った開発者の絶対パス `/Users/...` も同時に消える）。`tools/ship_check.py` ⑥へ「配布 bin の `dir` に `/Users/` と MSForms 参照が無い」を追加。

**T-58 の追記（W9.3・実機第4報 2026-09-03）**: 完成品 bin に**クラスモジュールを1本含めるだけ**で Mac の実Excel が読み込み時に「実行時エラー 5」を出す件を切り分け、**Z-24** に記録した（原因＝`dir` の `MODULEPRIVATE`(0x0028) 欠落とクラス属性行5行。`build/ovba_write.py` を修正して実機で解消を確認）。**この配布の裁定は「クラスをやめる」**である: `src/ui/clsAppEvents.cls` を削除し、ブックイベント（`Workbook_Activate` / `Workbook_Deactivate` / `Workbook_BeforeClose`）は**ビルドが焼く `ThisWorkbook` 文書モジュール**が受ける（値源は `build/build_rpn.py` の `_BAKED_THISWORKBOOK_TEXT`。**ASCII のみ**）。`ThisWorkbook` の属性行は Excel 製と同じ**8行**とし、値源を `build/ovba_write.py` の `_ATTR_DOCUMENT` 1箇所に寄せた（`build_rpn` 側の6行ベタ書きを廃止）。`modBoot` の `HookAppEvents` / `gAppEvents` / `AppEventsReady` を撤去。層(b)の `T47B-W81-03`（`AppEventsReady`）は**置換できなかったため削除**し `TE_EXPECTED` を **52 → 51** とした（VBA から「`ThisWorkbook` に `Workbook_Activate` があるか」を読むには VBE のプロジェクト参照を使うしかなく、その語は配布物の禁止文字列である。**代わりに `tools/bin_roundtrip.py` [4b] が配布物の側から「スタブに4本のイベントが含まれる／クラスモジュールが0本」を検査する**＝検査は消さず、層を移した）。

**T-58 の追記（W9.4: LO が見逃す VBA 規則(UDT ByVal)を lint 化・実機第5報 2026-09-03）**: Mac の実Excel で操作ガイドの[テストを実行]を押すと **「ユーザー定義型を ByVal で渡すことはできません」** のコンパイルエラーが `modPromptsOps.AsmS1User(ByVal ctx As TCaseCtx, ...)` で出た。VBAの規則: ユーザー定義型(`Type` 宣言した型)と配列は手続き引数として `ByVal` で渡せない(`ByRef` 必須)。LibreOffice Basic はこれを許すため層(c)の `lo-compile` が素通りし、他の裁定書27系の実機事故と同じ「LO死角」だった。**(1)** `modPromptsOps.AsmS1User`/`AsmS2User`/`AsmS3User`/`AsmS4User`/`AsmS3CriticUser`/`CtxBlockText` の `ByVal ctx As TCaseCtx`(6件)と `modJudgeStore.NewJudgement` の `ByVal rec As TJudgement`(1件)を `ByRef` へ修正した(呼び出し側はすべて変数を渡していたため無修正)。14章§6の該当シグネチャも `ByRef` へ同期した。**(2)** `tools/vba_lint.py` に **`check_udt_byval_param`** を新設し、`tools/gate.py` の 18ゲート内の lint 実行で毎回強制する。src 全体から `Type` 宣言名(公開・非公開とも)を集め、`ByVal <name> As <UDT>` と `ByVal <name>() As ...`(配列。要素型は問わない)を ERROR にする。正負例4本の自己テスト(`_selftest_udt_byval_param`)を run_lint 冒頭へ組み込み、ルールを空振りさせる改変を赤で止める。

**T-57 の統合で実際に入った変更（実装後の追記）**: (1) `modPipeline3.IncidentsFor` のスタブを `modKnowledge.IncidentsFor(industryCode, maxRows)` へ差し替え（0行時の文言はKB側が返し、呼出側は既定文言を持たない）。(2) 15章§0.7 の切詰めを**5段→6段**（事故事例＝順2）にし、計画 `TrimPlan`（modKnowledgeFmt から移設）と 適用 `LoadKbSlots`（modPipeline / modPipeline2 に写経されていた `LoadKb`/`FetchKb` を畳んだ唯一の実装）を **新モジュール `modPipeline4`** へ集めた（`modPipeline` が29,970字で満杯だったため）。`TChkCtx` / `TDeepCtx` へ `incidentsText` を通し、`AsmS2User` へは**切詰め済みの値**が渡る。(3) 純層は `modTestsPure17`（6段の半減順に正負1本ずつ＝12本）を新設し、`modTestsPure4` の G20 を6段へ更新（9→10本）。班Cの `modTestsPure15` は `modTestsPure16`（テスト名 W7D/W7E/W7F）へ改名し、`Pure14→Pure15→Pure16→Pure17` の連鎖にした。`wintest/tests_expected.txt` は **738**。(4) `tools/render_report.py` の `RENDER_MODULES` へ **`modMockLlm3`** を追加（T-55 で `modMockLlm.ResponseById` が MK-S2-NEW / MK-S2-RNW を `modMockLlm3` へ委譲するようになったのに検問側の一覧が追随しておらず、Basicライブラリに関数が無いまま soffice が止まって**render / render-f が exit=124 のタイムアウトで赤**になっていた。原因を直したのであって検問は緩めていない＝タイムアウト値は180秒のまま・修正後は1.2秒で緑）。(5) `modBoot.RegisterDefault` へ config `kb_incident_rows`（既定5。13章§2.3）を追加。 |

**W7 で触ってはいけないもの**: SEC番号・V番号・FR番号の既存の採番（改番禁止）／18章§3.5 の免責フッタ4行（「保険料の試算は本資料の対象外です（要見積）。」は据え置き）／config `ppt_max_slides_t2`（据え置き）／`SplitForCells` の32,000字契約と `JoinField` の区切りなし連結／区画②の保管＋プレビュー方式そのもの。

### Phase 1.5 タスク（先行定義）
| ID | タスク | 担当 |
|---|---|---|
| T-50 | 15章§9のWT/FGプロンプトを全文へ昇格→modPromptsOps/modSchemas拡張→CheckWT/FG | O |
| T-51 | 機構ライブラリ初期投入（第3弾795件の整形インポート） | S+H |
| T-52 | PL-05/06の実行UI（modPlayOps拡張・編集会用の採択画面） | S |
| T-33b | modExportPpt（5枚生成/テンプレ・白紙/AI注記/E-20・21）※**Phase 1.5に後送（高橋FB⑥: 編集可能形式は最後の最後）**。依存 T-33。DoD=mockデータからPPT生成しPowerPointで開ける | O |
| T-54 | modExtractor系移植（PDF3経路・Word相乗り）: 有報PDF・決算公告PDFの直接取込（FR-47）※Phase 1.5 | O |

※ **T-53（modCompanyFile）は本表から削除**した。FR-45はMustでありPhase 1本体の実装対象のため、W2の **T-29** として担当・依存・DoDつきで再定義している。番号 T-53 は欠番とし再利用しない。**W7（裁定書25）の3タスクは、この表の T-50〜T-52 / T-54 と欠番 T-53 を避けて T-55 から採番した**（v2.6）。

### 人間タスク（並行）
| ID | タスク | 期日目安 |
|---|---|---|
| T-90 | シード装填（docs/04・05・07の現物化）: 業種30/カタログ80-150行(AI下書き→各Gレビュー)/型22件/判断基準30行/リスクライブラリ100行/成功事例5件/適用先8社/パターン15行/研究テーマ20行。**docs/08ドシエ収集プロンプトの実機検証**（実在1社でD-1～D-4とD-9を流し品質確認）。**受入条件: リスクライブラリの全行の `category` が19章§3のリスクユニバース10分類のいずれかであること**（旧6分類で作った行は13章§3.1の読替表で変換してから投入。注入知識と出力語彙のズレを残さない。LoadKnowledge の E-34 検査でも同じenumを検査する） | W3完了まで |
| T-91 | 各G 60分ヒアリング（docs/04§5アジェンダ: カタログレビュー・勝ち事例・座組・引受けない条件3つ・困りごと） | 9月中 |
| T-92 | **OQ-1～12**の確認（16章§3。OQ-8は解決済みのため対象外） | W4まで（OQ-2/6/7優先。OQ-7〔リボンの1呼び出し入力上限〕が未確定のうちは `t2_max_context_chars`・`sparring_max_turns` を暫定値のまま先行させる） |
| T-93 | 9月キックオフ: デモ（docs/03改良プロンプト＋STUDIOウォッチ実演〔docs/07§実演記録〕）＋構想v2提示 | 9月 |
| T-94 | 判断台帳の記録運用開始（ツール完成前は暫定Excelで） | 9月～ |

## 4. テスト計画

1. **ユニット（modTestsPure）**: JSON抽出・検証（分岐網羅）・分割保存・整形・打切り・enum変換・採番。`run_excel_tests.ps1` で一括実行（実行と集計は `modTestRunner.RunAllPureTests`）

   **テストランナー要件（PoC modTestRunner が実害から作り直した仕様。移植後もこの3点を落とさない）**
   - ① `ReportText` は **PASS / FAIL / SKIP を別集計**で出す。**SKIPをPASSにも総本数にも数えない**（移植元では「実行環境の制限で本体を1行も実行できないテスト群」が `Check "…スキップ", True` としてPASSに計上され、件数が水増しされていた）
   - ② **期待テスト総本数 `tests_expected`** を `wintest/tests_expected.txt`（1行目に10進整数のみ）へ記録し、実行本数がそれと一致しなければ**ランナー自身がFAILを返す**（0件実行の「全緑」を成立させない）。テストを増減したコミットで同ファイルを更新するのは実装者の義務とし、更新漏れは即FAILとして現れる
     - **実装形**: ランナーは `modTestRunner.SetExpectedCount(n)` を公開し、**ハーネス（`run_excel_tests.ps1` / `tools/run_lo_tests.py`）が `wintest/tests_expected.txt` を読んで注入する**（VBA側からファイルI/Oを行わないため。W0実装済みの形を承認する）。**未設定のまま実行した場合はランナー自身がFAILを返す**（期待本数の注入漏れを「全緑」に見せない）
   - ③ SKIPが1件でもあれば、**レポートの冒頭と末尾に理由込みで全件列挙**する（何が走らなかったかがレポートだけで分かる）
   - 以後、本書で「modTestsPure全緑」とは **FAIL 0件 かつ SKIP 0件 かつ 実行本数=`tests_expected`** を指す（T-40・T-46⑤・§5-1の出荷手順と同一定義）
2. **一致検査**: 15章⇔modPrompts*/modSchemas のdiffゼロ検査（`tools/prompt_diff.py`。T-23）。13章⇔シート実体の照合スクリプト（T-02/03）。**19章⇔enum変換表の照合**（`tools/enum_check.py`。T-31。19章§3の全行を「変換表に載せる行／載せない行＋理由」へ**漏れなく分類**できることと、載せる行が `modUICase.EnumPairsCsv()` と機械値・日本語ラベル・**並び順**まで一致することを検査する。同関数の本体は静的に評価するので、表をループや条件分岐で組み立て直すと照合が落ちる＝骨抜きにできない）。**15章§11の検証ルール表⇔modValidate の照合**=表の**全ケースID（V-S1/S2/S3/S4/PF/S2C/S3C・v2.6 現在 計75件）に対して `modTestsPure` にテストが1本ずつ存在し、テスト名にケースIDを含み、ケース数と実装テスト数が一致すること**を照合スクリプトで検査する（modValidate の戻り値は各行が `[ケースID] ` で始まるため機械照合できる。実装者がケースを選ぶ余地をなくす）
3. **結合（mock）**: UC単位のシナリオ回帰（対象=**UC1～UC5・UC7・UC8・UC9**。T-40と同一集合）。決定的応答のため毎リリース実施。異常系は config `mock_fault`（15章§8.2の8値）で注入する
4. **実機（ribbon）**: new/renewal各1社完走＋validate_result集計
5. **品質評価**: ベテラン3軸ルーブリック。評価入力セットで改訂時回帰

## 5. リリース・運用

**回し方（v2.5）**: 検問はまとめて **`python3 tools/gate.py`（全20ゲート一括）** で回すのが標準であり、個別ツールの直接実行はデバッグ時に限る。一括の内訳には `tools/enum_check.py`（19章⇔`EnumPairsCsv` の一致検査。§4-2）と、HTMLテンプレ系に触れた変更で必須の `tools/render_report.py`（**standard と --faithful の両方**。18章の逐語照合の唯一の検問。§1）を必ず含める。下の**§5-1** はその内訳と順序を定めたものである（項番は従来どおり）。**`wire`（`tools/ribbon_wire_check.py`。W11-c・裁定書33 C-2）は 20本目のゲート**で、mock 応答（`src/test/modMockLlm*.bas`）にリボンの抽出切断を招く `"},` が1つでもあれば赤にする（15章のJSONフェンスと `modSchemas` は同じ走査を **WARN** で数えるだけ＝送信側なので切断は起きない。`--strict-docs` で ERROR へ昇格できる。16章 E-63）。

1. **`vba_lint.py` 緑 → `run_lo_tests.py` 緑（全モジュール構文コンパイル）→ modTestsPure（FAIL 0件・SKIP 0件・実行本数=`tests_expected`）→ ビルド → T-46の出荷前検問5項目PASS（①`SetCellSafe`/`HtmlSafe`迂回ゼロ〔`' SAFE:const` 注記なしヒットゼロ〕／②16章NFR-S2の走査仕様4項目でキー検出ゼロ〔成果物は `unzip -p` 展開後を走査〕／③`dist`・成果物除外の実効確認／④実Excel wintest 全PASS〔modTestsPure＋modTestsExcel・SKIP 0件〕／⑤modTestsPureの本数条件）→ app_version更新 → 15分スモーク → 署名(OQ-2) → 共有フォルダ配置**。**検問を1つでも飛ばした版は配布しない**（姉妹PJはLibreOffice合格のまま出荷して34ラウンド手戻り／配布物にキーを載せた）。なお `vba_lint.py`・`run_lo_tests.py` は**コミット条件**であり、これらが緑でも実機wintest（層b）を飛ばしてよい理由にはならない（§1）
2. 利用者は旧版を自分でコピー置換（Phase 1は「案件使い切り」運用。移行機能はv2.1検討）
3. ナレッジブック更新は管理者随時（本体再配布不要）
4. 月次運用: 編集会（受信箱・型候補の採択）／run_log集計（repaired・failed率、所要時間、警告率）／新サービス候補の商品部報告。四半期: 型生成バッチ＋勝ち筋仮説レビュー

## 6. マイルストーン

| 時期 | イベント |
|---|---|
| 9月上旬 | キックオフ（T-93）。W0～W1着手。ヒアリング開始（T-91）。判断台帳運用開始（T-94） |
| 9月末 | mock E2E完走（T-40相当の中間確認） |
| 10月中旬 | 実機2社完走（T-41）・シード装填完了（T-90） |
| 10月末 | リリース（T-45）・5～10社PoC開始 |
| 11月末 | PoC評価（10章§9 KPI）→Phase 1.5移行判断 |
| 12月～ | PL-05/06ツール化・月次編集会定着 |
| 来期 | PL-07/08設計着手判断（判断台帳100件超）・全社基盤化検討 |

## 7. バックログ（Phase 1 では着手しない残件）

| ID | 内容 | 判断 |
|---|---|---|
| A-4 | config `deep_transport` が未結線。`modPipeline2.DeepRouteOf` は値の解決だけを行い、入念モード（S2C/S3C・S2R/S3R）の呼び出しを direct 経路へ切り替える口が無い（14章§6の `CallStep` に呼び出し単位の経路上書き引数が無いため）。指定がある間は `RunDeep` が usage_log に事実を残して既定経路で続行する | **見送り（裁定書9 A-4）**。`CallStep` の経路上書き口の新設とセットで別途裁定する。それまでは `deep_transport` を**空のまま運用**する（docs/23 の手順もこの前提で書かれている） |
| Z-1 | `modCaseStore` が 29,997/30,000字（残り3字）で、次の修正が1件も入らない。案件一覧の属性列アクセスまたは `FreezeRound` 系を `modCaseStore3` へ切り出す（§1の分割バックログ2番と同一件）。`modPipeline`（29,988字・残り12字）も同じ帯にある | **W4.2で記録のみ（裁定書10補遺P8）**。分割は公開契約（14章§6）を動かさない範囲で、次に当該モジュール本体へ手を入れる波の**前**に行う |
| Z-2 | **Notice（MsgBox）のテスト時抑止口**。ui層の `Notice` はモーダルを出すため、`modUIInbox.InboxPost` など案内を伴う経路を層(b)テストから無人実行できない（裁定書10補遺P7）。裁定書11 Q1 では案件入力の不一致ガードを **hm_warning への直書き**（13章§2.12 B1 と同作法）にして1本だけ回避したが、恒久解は「テスト実行中は MsgBox を出さない口」の新設 | **W4.3で記録のみ（裁定書11 Q10）**。抑止口は公開契約（14章§6）の新設になるため、E2E経路をテストへ載せる波とセットで裁定する |
| Z-3 | **16章のエラーコード表へ `draft_row_promoted`（E0603 detail）を登記**する（裁定書10補遺P2で実装済み・表に未記載）。裁定書11 Q3(b) で足した `draft_row_remarked`（E0603 detail。下書き行マーカーの回復）も同じ表へ登記する | **W4.3で記録のみ（裁定書11 Q10）**。16章の表を触る波でまとめて反映する |
| Z-4 | **`EnsureDraftRow` の行挿入の実機確認**。`ws.Rows(2).Insert` で用意した下書き行に入力規則が付くこと（裁定書11 Q3(a) の `modUICase.RebindFlatValidation` の効き）と、`judge_to` / `source_kind` の日本語DVが121行目以降でも出ること（同 Q5）は、実Excel でしか確かめられない | **W4.3で記録のみ（裁定書11 Q10）**。次の wintest 実機実行のチェック項目に含める |
| Z-5 | **`modUICase3` の分割**。W4.4（裁定書12 V1）の3値判定を入れた結果 **29,847/30,000字（残り153字）** となり、次に案件入力へ触る波は事実上1行も足せない。`modUICase2`→`modUICase5`・`modUICase3`→`modUICase4` と同じ作法で、貼付欄の定義（`PasteTable` / `SplitPaste` / `JoinField` / `MissingRequired` / `ScanPasteFields`）を1モジュールへ切り出すのが自然。**W6第1弾追記**: 区画②が `modUICase6` へ新設されたことで、`modUICase3.CaseSave` / `SaveCaseInput` / `MissingRequired` / `ScanPasteFields` 等の**旧貼付欄（v3.1以前の予約行方式）を読む一連の経路は、`btn_ci_save` の配線が `modUICase6.SaveNav` へ切り替わったため、現在いずれの図形からも呼ばれない未配線コードとして残存している**（11章§3.1.2）。分割の要否とは別に、**この未配線コード自体を撤去するかどうかは第2弾で裁定・実施すること**（第1弾は「触らない」以上のことをしていない。撤去すれば29,847字の残153字問題そのものが解消する可能性が高い） | **W4.4で記録のみ**。切り出しは公開名の新設（14章§6の登記）を伴うため、案件入力を触る次の波の冒頭で裁定してから行う |
| Z-6 | **判断台帳の確定行の日本語表示化**。`decision` / `result` は入力列を日本語ラベルのDVへ変えた（裁定書12 V7）が、`modJudgeStore.JgPut` が書く確定行の値は機械値（`raise` / `won` 等）のままで、利用者が選んだ「増率」が読み返すと「raise」に見える（受信箱 `source_kind` と同じ非対称。13章§2.7に明記済み）。DVは既存値を検査しないため赤くはならず実害は無いが、判断台帳は利用者が読み返す台帳そのものである。確定行も日本語で持つ／書くときだけ日本語へ変換する等の選択は store 改修を伴う | **Phase 1 は容認（裁定書13 W3）**。実機フィードバック（読み返しで困るか）を見てから改修可否を判断する |
| Z-7 | **`gOverBuf` の揮発残留と新規モードの跨ぎ**。overflow 描画の直後に[＋新規案件]で新案件を作ると、`SavePasteFields` が `IsOverflowed` により当該欄を新案件でもスキップし続け、新しく貼った内容がその欄だけ保存されない（保存案内に「続き欄に収まらないため保存しなかった欄があります」が必ず出るためサイレントではない）。`gOverBuf` は `modUICase3` の Private で、リセット口を足す余白が無い（残153字） | **W4.5で記録のみ（裁定書13後の最終検証指摘）**。Z-5 の分割とセットで、公開リセット口（または新規モード開始時のリセット）を裁定してから対応する |
| Z-8 | **新規モードのクリア線引き**。`ClearCaseInput` は `ci_quality_mode` / `ci_anonymize`（利用者設定）と `ci_recipe_01`～`ci_recipe_14`（収集レシピの自己確認チェック）をクリアしない。前案件のチェックが新規画面に残るが保存内容には影響しない（13章§2.11 の「空画面」規定との表示上の不整合）。あわせて `U4_RESEARCH_ROOM` = `U3_RESEARCH_ROOM` の定数二重管理（描く側と消す側が別モジュール）も Z-5 分割時に一本化する | **W4.5で記録のみ**。レシピ欄をクリアするかは実機FBで判断（クリアする場合は14行の追加のみ） |
| Z-11 | **`操作ガイド`の型（案内型）の正式化**。本シートは入力欄も1行目ヘッダも持たない「案内専用」だが、13章§2.9 の型は帳票型／テーブル型の2つしかないため `role=form` を借りて登録している（裁定書15 A-c9 で Phase 1 は容認）。`tools/sheet_check.py` も form の照合経路に相乗りしており、案内シート特有の検査（本文が実装の文字列と一致しているか等）は持てない | **Phase 1 は容認（裁定書15 A-c9）**。案内型（guide型）を13章§2.9の型表へ正式に足すのは、シート型を触る次の波でまとめて裁定する |
| Z-9 | **`modUISheet.EnsureButton` / `EnsureButtonEx` の戻り値が全呼出元で捨てられている**（裁定書14調査の発見H）。図形の生成に失敗しても呼び出し側は気づけず、ボタンが描画されないまま起動が続く。戻り値を見て `err_log` へ記録する経路が無い（**`FitCol` が0を返してボタンが黙って生えない無言失敗も、同じく `err_log` 記録の対象に含める**） | **W5で記録のみ（裁定書14 裁定9・班Bのconcerns裁定 B-c7）**。呼出元を横断する改修になるため、UI描画まわりへ次に手を入れる波でまとめて裁定する |
| Z-10 | **`modBoot` の申し送りコメント `input_rule_omissions` が未処理のまま残っている**（裁定書14調査の発見J）。起動シーケンスのどこかで「入力規則を張らなかった欄」を集計・案内する意図のコメントだけがあり、実装が追随していない | **W5で記録のみ（裁定書14 裁定9・班Bのconcerns裁定 B-c7）**。`modBoot` の起動シーケンスへ次に手を入れる波で、コメントの意図どおり実装するか削るかを裁定する |
| Z-12 | **HOME以外のボタン横重なり残存**（受信箱／フィードバック／判断台帳／壁打ち／S1～S4のrow1）。追補1でHOMEは `FitCol` による重なりゼロの配置へ直したが、他画面のボタン配置はまだ列幅の仮定に依存しており、同じ横展開をしていない（裁定書14 班Bのconcerns裁定 B-c1） | **W5で記録のみ**。`modUICase3` の分割（Z-5）と同時波で、`FitCol` 流儀を横展開する |
| Z-13 | ~~**`modKnowledge` 残23字・`modUIHome` 残368字**（30,000字契約に対する残り）。次にどちらかへ手を入れる修正が1件でも入ると分割が避けられない帯にある（裁定書14 班Bのconcerns裁定 B-c3/B-c4）~~ | **完了（W6第1弾の前提として挙動不変で分割）**。`modUIHome`（29,205字）→ `modUIHome`（12,629字＝画面）＋ `modUIHome2`（18,552字＝OnActionハンドラ群）、`modKnowledge`（29,978字）→ `modKnowledge`（22,295字＝シートに触る側）＋ `modKnowledge2`（9,017字＝シートを触らない純関数）。**移設のみで挙動・シグネチャ・文言は不変**（公開Subの集合は移設前後で欠落ゼロ）。登記は12章§2のモジュール一覧・14章§6の「登記の移設」表・13章§2.10（OnActionの修飾名）・`build/modules.json`・`tools/vba_lint.py`（MODULE_REGISTRY／CONTRACT）へ同期済み。全16ゲート緑・純層612本／層(b)不変 |

| Z-15 | **ワーカー方式（第2のExcelインスタンスに呼出させ、親が進捗バーを動かす）** | **保留（v3.2で降格）**。裁定書21・利用者回答4「裏Excelへの執着は不要・要検討課題へ」により第1弾から外した。11章§4.2 は (1)ステータスバー＋(2)事前描画カードの2段で**完結**しており、Z-15 が永久に着手されなくても仕様に欠けは無い。着手するときの手順8項目と判定基準3つ（親がゴースト化せず進捗バーが動く／結果が親へ確実に戻る／ゾンビプロセスが残らない）は11章§8.7が持つ。**同じ案が繰り返し提案されるのを防ぐため節ごと消さない**。着手する場合は11章§4.2・§7.2(d)・§8.4を**先に**改訂してから実装すること |
| Z-16 | **取り消し線（`Font.Strikethrough`）で「もう押せない／もう要らない」を表す案**（W6.1 検証班 i3）。いまは「押せないボタンは置かない」（原則①）で通しているが、`案件一覧`・`受信箱`の行や、区画②の隠した欄など「消さずに残すが、いまは対象外」の表現手段が1つも無い | **W6.1で記録のみ（裁定書22 i3）。第2弾で裁定する**。取り消し線は Excel の `Font.Strikethrough` 1つで書けるが、**どの状態を取り消し線で表すかの語彙が11章に無い**（色分けは§3.3.8が上限の3色を使い切っている）。第2弾で 11章§3.3.8 の色分けと合わせて「使ってよい強調の一覧」を決めてから実装すること（先に実装すると画面ごとに違う意味の取り消し線が生える） |
| Z-17 | **`modUIGeom` の改名**（W6.1 検証班 i4）。名前は「幾何」だが、実体は帯・ボタンの並び・カードの高さに加えて**表示時間（`CardWaitMsFor` / `ToastSecondsFor`）**という時間の算数も持っており、core層の中で役割名と中身がずれている | **W6.1で記録のみ（裁定書22 i4）**。改名は `MODULE_REGISTRY` / `CONTRACT` / `PURE_ALLOWLIST` / `build/modules.json` / 12章§2 / 14章§6 / 全呼出元へ波及するため、**単独の波では割に合わない**。次に `modUIGeom` を分割する必要が出た波（30,000字契約に触れたとき）で、分割と同時に改名を裁定する |
| Z-18 | **docs/08 の `{{ }}` プレースホルダと差込表（`modUIResearch.PlaceholderTable`）の不一致を機械で赤化する検問**。現状は純層テスト（`modTestsPure14` の W61A）が **docs/08 の実文を写した雛形**を持ち「`{{` が残らない」ことを固定しているだけなので、docs/08 に新しいプレースホルダが増えても、写しを更新しない限りテストは緑のまま通ってしまう（差込表に穴が足りないことに気付けない） | **W6.2で記録のみ（裁定書23追補3）**。docs/08 のコードフェンスから `{{ }}` を機械抽出し、`PlaceholderTable` の集合と突合する検問（`tools/` の既存チェッカと同形）を足す波で実施する。実装するまでは、docs/08 を触ったら W61A の写しも同時に更新すること |
| Z-19 | **B案: OVBAライターで完成品 `vbaProject.bin` を生成し、焼き付け工程そのものを不要化する**（裁定書23）。いまの配布は C案「焼き付け配布」で、開発側が新しいビルドを受け取るたびに**会社PC（Windows・VBOM有効）で1回開いて閉じる**手間が要る（docs/24 §8）。ビルド側で CFB を書き出し、`dir` / `PROJECT` / `PROJECTwm` ストリームを自前で生成して**モジュールが最初から入った** `vbaProject.bin` を作れば、この1回の手間が消え、`vba_src` からの注入経路自体が不要になる | **完了 → T-58（裁定書27 W9-A）で実施**。後回しにしていた理由（工事が大きい・検証は実Excelでしかできない）は変わらないが、**2026-09-02 に旧方式（`vba_src` ＋ `AddFromString` 注入）が社内AVのAMSIに検知されて採用禁止になった**ため、B案が「手間を消す投資」ではなく**唯一の配布手段**になった。Linux側の検証は `bin-roundtrip`（olevba での読み戻しバイト比較）と `lo-xlsm`（LibreOfficeに実際に開かせる）の2本で担保し、実Excelの確認は利用者環境で行う |
| Z-20 | **Macは非対応**（裁定書23 C-3）。`\`（整数除算演算子）がMacの日本語コードページで化けてコンパイルエラーになる件は **W6.2 で根治済み**（8行を `Fix(a / b)` 系へ書き換え、`tools/vba_lint.py` に「文字列リテラル・コメント外の `\` を禁止」ルールと自己テストを追加）。**W9-B（裁定書27）で、Windows前提の4系統（クリップボードの `DataObject` 遅延バインド／メモ帳の起動／`ADODB.Stream`・`FileSystemObject`／`Declare PtrSafe`）は配布物から全撤去した**（クリップボード＝`paste_buf` シートを介した Excel自身の貼り付け・コピー、[中身を見る]＝ブック内の `中身` シート、ファイル書き出し＝純VBAの `Open For Binary` ＋自前UTF-8エンコード、待機＝`Application.Wait`）。したがって「Windows前提のAPI依存」を理由とする非対応の根拠は**もう無い**が、**動作を確かめているのはWindowsだけ**であり、リボン経由のLLM呼び出し（社内アドイン）がMacに存在しないという別の理由が残る | **W9-Bで根拠を更新（裁定書27）。Mac対応の可否そのものは引き続き未裁定**。いまは「**Macで開くと画面の確認くらいはできる**」程度に留め、製品としては非対応のままとする（docs/24 §8.1b に同旨）。対応するなら、残る依存はリボン経路1本なので、その代替（direct経路の復活＝Z-22と表裏）を決める波を別に立てる |
| Z-22 | **`modGatewayDirect` の配布除外（`build/modules.json` の `ship:false`）**（裁定書27 W9-B5）。HTTPのCOM生成はAVが重く見る形なので、direct経路のモジュールごと配布ビルドから落とすのが本筋である | **実施済み（W11-a・裁定書30 裁定1）**。保留の理由だった3件をまとめて片づけた: ①参照元 `modGatewayRPN.DirectStep` は薄い接続モジュール `modGatewayLink.CallDirect` を呼ぶ形にし、**同名で2ソース**（prod=`src/core/modGatewayLink.bas`＝`E0209` を返すだけ／dev=`src/core/dev/modGatewayLink.bas`＝`modGatewayDirect` へ転送）をビルドがモードで選ぶ（`build/modules.json` の `dev_src` が唯一の値源。`Application.Run` や文字列ディスパッチによる実行時の切替は**しない**）②`modTestsPure2` G11 の17本は dev専用の `src/test/dev/modTestsPureDev.bas`（`ship:false`）へ移設し、配布物に載る `modTestRunner` からは接続モジュール `modTestsPureHook`（同じく同名2ソース）経由で呼ぶ③`wintest/tests_expected.txt` を `prod=` / `dev_only=` の2行にし、prod ビルドは `prod`、dev ビルドは `prod+dev_only` を `config!tests_expected` へ焼く。ゲートは `lo-pure`（配布集合＝`prod` と一致）と **`lo-pure-dev`（dev専用モジュールだけ＝`dev_only` と一致）** の2本。配布物の担保は `tools/ship_check.py` ⑥ に prod 限定の禁止文字列（HTTPのCOM生成に使う3語）を足して fail-closed にした |
| Z-23 | **`E0606`（数式ガード発火）が `中身` シート・`paste_buf` シートで鳴りやすい**（裁定書27 W9-B・班2 concern 4）。`modUtilText.SetCellSafe` は書き込む文字列の先頭が `=` `+` `-` `@` のときに `E0606` を err_log へ記録するが、[中身を見る]で保管庫の全文を `中身` シートへ流し込む経路では、本文が箇条書き（`- ...`）で始まるだけで発火する | **実施済み（W11-a・裁定書30 裁定4）**。対処案(b)を採った: **閲覧・受け皿シートへの流し込みは無害化はするが `E0606` を記録しない**。判定は純関数 `modUtilText.ShouldLogFormulaGuard(whereNote)` に閉じ（除外は `modUICase7/body_title` / `body_text` / `clip_copy` の3つだけ）、純層テスト正負2本で押さえた。16章 E-46 に例外の1行を書いた（実装だけを先に変えていない） |
| Z-24 | **クラスモジュールの焼き込みが Mac の実Excel と不一致だった**（W9.3・実機第4報 2026-09-03）。完成品 bin にクラスモジュール（`clsAppEvents`）を1本含めるだけで、Mac の実Excel が読み込み時に **「実行時エラー 5」の生ダイアログ**を出した。切り分け（`v0`〜`v7`）で、標準モジュール93本＋`ThisWorkbook` のみなら正常／中身を `Public App As Object` だけにしたクラスでも再現、と判明し、**クラスモジュールの焼き方そのもの**が疑われた。Mac Excel が保存したクラス入りサンプル（`Class1` を1本足しただけのブック）と突合したところ、差分は2点だった: **(1)** `dir` の MODULE レコードが Excel では `0x0022(MODULETYPE)` → **`0x0028(MODULEPRIVATE・Size=0)`** → `0x002B(Terminator)` の並びなのに、我々は `0x0028` を書いていなかった（`document` / 標準モジュールには Excel も付けない）。**(2)** クラスの属性行が Excel では8行（`VB_Name` / **`VB_Base = "0{FCFB3D2A-A0FA-1068-A738-08002B3371B5}"`** / `VB_GlobalNameSpace` / `VB_Creatable` / `VB_PredeclaredId` / `VB_Exposed` / `VB_TemplateDerived` / **`VB_Customizable = False`**）なのに、我々は5行しか書いておらず `VB_Base` を欠いていた（`document` の `VB_Customizable` は `True`）。なお同サンプルは**全モジュールの改行が LF 単独**だった（Windows 製は CRLF）が、CRLF の切り分けブックが実機で通っているため改行は原因ではない | **解決（W9.3・Mac実機 v7 で確認）**。根拠＝Mac Excel 製サンプル。`build/ovba_write.py` を上の(1)(2)どおりに直した版が Mac の実Excel で正常に開いた。退行防止として `tools/bin_roundtrip.py` に検査 **[6]**（class＝属性8行＋`VB_Base`＋`0x0028` / document＝`0x0028` 無し・`VB_Customizable = True` / std＝`Attribute VB_Name` の1行だけ）と、**その場でクラス入り bin を組み立てる正例・負例の自己テスト**を入れた（配布物はクラスを持たないため、自己テストが無いとクラス分岐が一度も走らず腐る）。**この配布（W9.3）ではブックイベントの受け口を `clsAppEvents` から `ThisWorkbook` 文書モジュールへ移し、配布物からクラスモジュールを外した**（可動部品を減らすため。クラス対応そのものは他プロダクトが使う） |
| Z-25 | **Macベンチはコンパイル＋論理の検証用であり、製品の対象ではない**（裁定書29 W10.1）。Mac版Excelには貼り付けの書式名（「Unicode テキスト」/「Unicode Text」）が無く `PasteSpecial` が全滅するため、層(b)の `T47-W9-01`（クリップボード往復）と `T47B-W61-15`（案件未選択へ貼ると採番→保存）は Mac では成立しない。**製品（`modUICase7`）は変えない**（会社PC＝Windowsでは正しく動いており、Mac向けの書式名を足すと配布物の貼り付け経路が2本になる）| **W10.1 で SKIP 化して決着**。`modUtilPath.IsMacExcel()`（`Application.OperatingSystem` が "Macintosh" で始まる）かつ**貼り付けが不成立のときだけ** SKIP 扱いで緑にし、detail に `SKIP(Mac): 貼り付け書式名がMac版Excelに無い` と明記する。**Windowsでは `IsMacExcel` が False なのでこの枝に入らない**＝会社PCの検問に新しい逃げ道は増えない（落ちたら FAIL のまま）。会社PCでの実行では `SKIP(Mac)` は0本になるはずで、1本でも出たら判定が壊れている合図である |
| Z-26 | **`modTestsExcel` は満杯**（W10.1 終了時点で 29,855字／30,000字契約に対し残り145字）。層(b)の1本目はフィクスチャの後始末・共通ヘルパ（`ECheck` / `TempFilePath` / `SheetByName` / `Hdr1` ほか）を抱えており、W9-01 の Mac限定SKIP を入れるだけで帯域を使い切った | **次に層(b)へ追記する波で `modTestsExcel4` を新設**して受けること（`modTestsExcel2` / `modTestsExcel3` と同じ作法＝入口は `modTestsExcel.RunAllExcelTests` から呼ぶ `RunExcelTests4`、戻り値は打った Check の本数で `TE_EXPECTED` の自己照合へ足す）。**`modTestsExcel` へ行を足す形の修正はしない**（30,000字を超えるとビルドが止まる） |
| Z-27 | **`dossier_case.pii_flag` の読取が未結線**（W11-a で確認）。手動の[企業ファイルへ保存]（共有向け）は書き出しの前に本文を再走査しているため**実害は無い** | **記録のみ（裁定書30 裁定6・コード変更なし）**。共有・送信の経路が増えたときは、その経路の前段で `dossier_case.pii_flag` を読んで 16章 E-05(7) の確認を出すこと |
| Z-28 | **[テストを実行]の後始末**（W10.1 Mac実測）。テスト終了後に赤い警告帯「画面の案件と保存先が合いません…」と `hm_warning` が残っていた（層(b) `modTestsExcel2` の W61 (3) が `SaveNav` の不一致警告をわざと出させたままだったため） | **実施済み（W11-a・裁定書30 裁定2）**。W61 (3) の末尾で `hm_warning` を空へ戻し `modUIToast.CancelToast` / `HideToast` で帯を消す。さらに `modTestsRunnerUi.RunAllTestsFromBook` の終端（と異常終了の後始末）でも保険として同じ後始末を呼ぶ。層(b)に1本（T47B-W61-17）を足して「(3)の後に `hm_warning` が空」を機械で押さえた |
| Z-29 | **使い方タブの `note()` 行が潰れて読めない**（W10.1 スクショ実測）。幅の狭いB列に `wrap_text` で書くため1行の高さに収まらなかった | **実施済み（W11-a・裁定書30 裁定3）**。`build/build_rpn.py` の `note()` を `prompt()` と同じく **B:C 結合**で書くようにした（高さの式＝行数×14+5 は変えていない。名前付きレンジは無いので sheet ゲートは不変） |
| Z-30 | **`ShouldLogFormulaGuard` の `whereNote` リテラルが二重管理**（W11-a・裁定書30 裁定4）。`modUtilText.ShouldLogFormulaGuard` は除外先を `"modUICase7/body_title"` / `body_text` / `clip_copy` の**文字列リテラル**で判定するが、その値を作っているのは `modUICase7` の定数（`U7_SRC` ＋ サフィックス）である。片方を変えるともう片方が黙って効かなくなる（純層テストは同じリテラルを持つので、テストも一緒にずれる） | **記録のみ（W11-a）**。定数を core 側へ寄せると core → ui の依存ができるため、いまは影響の小さいリテラル判定を採った。`modUICase7` の `whereNote` を変える修正が入るときは、`modUtilText` と `modTestsPure` の G1、16章 E-46 の3箇所を同時に直すこと |
| Z-31 | **30,000字契約の残り帯域が逼迫しているモジュール**（W11-a 終了時点）: `modGatewayRPN` 残84字・`modTestsExcel` 残134字・`modTestsExcel2` 残395字。`modUtilText` は 27,965字で `tools/vba_lint.py` の28,000字 WARN 帯の直前にいる。**W11-b 終了時点の追記**: `modBoot` は 29,643字（**残357字**）で、warn 文言の差し替えで帯域をほぼ使い切った。`modExportHtml` は 29,837字（残163字・WARN 帯）| **記録のみ（W11-a）**。次にこれらへ1行でも足す波では、先に分割すること。層(b)は Z-26 のとおり`modTestsExcel4` の新設で受ける。`modGatewayRPN` は E0209 の文面を `ErrMessageFor` に持てなかった（裁定書30 裁定1(c) の逐語は `modGatewayLink` 側が持っている）ので、分割の必要が出たらそこも同時に整理する |
| Z-32 | **環境変数で OneDrive を探す方式は会社PCで不成立**（2026-09-05 実測・裁定書31）。`%OneDrive%` は**別の利用者（win11admin）のフォルダ**を指し、`%OneDriveCommercial%` は未定義だった。環境変数を頼りにすると、他人のフォルダへ企業データを書きに行く | **記録（W11-b で撤去済み・T-61）**。**`data_dir.txt`（起動bat が書く）が唯一の正**であり、config `data_dir` は利用者の明示値（`%…%` を含む値は無効）、最後の逃げ場は本体と同じフォルダの `\データ`（到達したら warn）。**この3段以外の値源（環境変数・レジストリ・既定パスの推測）を再び足さないこと**。**コード上の環境変数リテラルも0にした**（`modExportHtml.EX_OUT_DEFAULT` / `modUIHome2.UH_DIR_DEFAULT` の `%OneDriveCommercial%\リスク提案ナビ\データ` を空へ、未使用だった `modExportHtml.ExpandEnvText`（`%USERPROFILE%` / `%HOME%` の展開）を削除）。退行は `tools/vba_lint.py` の規則 `check_env_datadir_literal`（`Environ$("OneDrive")` / `("OneDriveCommercial")` / `("USERPROFILE")` を**全モジュール・除外なし**で ERROR。正例1・負例1の自己テストつき）が止める。足したくなったら、まず会社PCで環境変数の実測を取り直して裁定を受ける |
| Z-33 | **`data_dir` に Excel の禁止文字 `[ ] < > ? : | *` が含まれるとき、起動時に warn を出す（未実装）**（W11-c・裁定書33 C-6）。髙橋さんの実機で、本体を `[dev]` を含むフォルダに置いたところ企業ファイルの `SaveAs` が **1004** で落ちた（Excel はブック名・パスの一部にこれらの文字を許さない）。いまは保存失敗（16章 E-59・E0603）としてしか見えず、利用者には原因が分からない | **記録のみ（W11-c）**。会社PCでの実測結果（どの文字でどう落ちるか）を待ってから、`modBoot` の起動手順へ warn を1枚足すかを決める。**先に会社PCの結果**を取ること（`modBoot` は残357字＝Z-31 のとおり帯域が無いので、足すときは置き場所も同時に決める） |
| Z-34 | **検問 `wire` の走査(b)(c)（15章のJSONフェンス 71件・`modSchemas` 65件の `"},`）は既定 WARN のまま**（W11-c・裁定書33 C-2 の班 concerns 1 を司令塔が追認）。これらは**送る側**の見本・型注記であり、リボンの終点規則は応答にしか掛からないので切断は起きない。ただし「送る側の見本が1行詰め」だとモデルが真似る余地は残る | **記録のみ（W11-c）**。15章のスキーマ節と `modSchemas` を整形し直す波（`modSchemas` は 26,288字で分割必須）を組むときに `tools/gate.py` の `wire` へ `--strict-docs` を足して ERROR へ昇格する。それまで WARN 136件は既知のノイズとして扱う |

上表（Z-7 / Z-8 を含む）以外の W4.5 時点の申し送りは無い（裁定書9 の A-1/A-2/A-3/A-5/A-6 は T-42b で、裁定書11 の Q1～Q9 は W4.3 で、裁定書12 の V1・V3～V9 は W4.4 で〔V2 は欠番〕、裁定書13 の W1～W7 は W4.5 で解消した。W3 だけは本表の Z-6 として容認・記録した）。Phase 1.5 の予定タスクは§3の「Phase 1.5 タスク（先行定義）」が正。
