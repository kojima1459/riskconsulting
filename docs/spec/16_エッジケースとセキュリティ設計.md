# 16. エッジケースとセキュリティ設計 v2.4

v2.4変更概要: 実装前監査72件の裁定を反映。§1の全表に code 列を追加して未採番行を採番し、E-03（入力打切り）・E-06/E-12（データ体制）・E-11（ui_lock）を全面改訂、E-04/E-05/E-22の範囲を拡張、E-35/E-36/E-47/E-48/E-49/E-50/E-51 を新設、NFR-S2の走査仕様とNFR-S7の3書き込み口を明文化した。

エラーコード体系: E01xx=入力／E02xx=LLM経路／E03xx=JSON・検証／E04xx=ナレッジ／E05xx=出力／E06xx=環境・状態／E07xx=受信箱・ウォッチ。全て err_log 記録＋非エンジニア向け日本語メッセージ（対処つき）。
個別コードの正は §1 各表の `code` 列であり、19章§4のエラーコード表と一字一句一致させる（矛盾時は本章が正）。`code` 列が `(なし)` の行は、利用者への案内・run_log/usage_log 記録のみで err_log にコード付きの行を立てないケースである。

## 1. エッジケース一覧

### 入力系（E-01〜）
| ID | ケース | code | 検知 | 対応 |
|---|---|---|---|---|
| E-01 | 必須入力空（企業名/業種/HP、renewal時は現契約） | E0101 | modUICase実行前 | 該当欄を示し実行しない |
| E-02 | 入力が薄い | (なし) | 2段構え | 実行前: HP<200字で警告（続行可）。実行後: S1のinput_quality.overall=lowで「一般論に近い出力になります＋advice（何をどこから足すか）」を警告表示（続行可・run_log記録。15章§2.0収集レシピ・CheckS1充足度ゲート） |
| E-03 | 入力合計が上限超（入力打切り） | E0102 | **modPipeline**（各Stepのプロンプト組立の**前段**で判定する。modPrompts* は純文字列モジュールであり打切り・警告・ログの責務を持たない=12章§4） | 本節直後の「E-03 入力打切りの詳細規則」が正 |
| E-04 | 制御文字・私用領域文字／データ境界記号の偽装 | (なし) | modUtilText.SanitizeInput | 制御文字・私用領域文字を除去して続行。あわせて外部由来テキスト本文中の `■■■` を `[境界記号]` へ置換する（15章のデータ境界記号を貼付テキストが偽装するのを防ぐ。E-43の多層防御の1枚。除去・置換の件数はrun_logのdetailに件数のみ記録し本文は残さない=NFR-S3） |
| E-05 | 個人情報らしき文字列（@付き・電話番号・「様」付き人名パターン） | E0103 | 実行前走査。対象は **(1)案件入力の全貼付欄（9欄＋続き欄） (2)受信箱body (3)壁打ちの発話送信前 (4)判断台帳の situation / key_reason 保存前 (5)フィードバックの customer_quote 保存前**。走査本体は `modPii` に一元化し、`modUICase`（貼付欄・フィードバック保存）・`modUIInbox`（投函）・`modSparring`（送信）・`modJudgeStore`（保存）がそれぞれ前段で必ず通す（12章§4） | (1)〜(4)は警告し**実行・保存をブロック**（該当箇所を指示）。**(5) customer_quote のみ例外**: 顧客の逐語引用がFR-11の要件のため、ブロックせず「警告＋伏字の差し替え案（検知箇所を `{{PERSON}}` に置換した文面）を提示して利用者が採否を選ぶ」方式とし、採用時のみ置換して保存する。いずれの場合も E0103 を err_log に記録し、detail には検知種別と箇所（列名・文字位置）のみを書き本文は書かない（NFR-S3） |
| E-31 | 現契約サマリに顧客固有の識別情報 | E0104 | 案件入力の「匿名化」ボタン（anonymize_default=TRUE） | **プレースホルダ方式**: 企業名（および入力欄の社名表記ゆれ）→`{{COMPANY}}`、証券番号らしき英数列→`{{POLICY_NO}}` に置換して**全Stepを実名なしで実行**し、シート表示・レポート生成時にVBAが復元置換。LLMには実名と契約情報の組を渡さない（docs/09 F-04）。OQ-6で投入可が確定したら匿名化を任意化 |
| E-24 | 業種マスタに無い業種 | (なし) | 案件入力 | 最も近い業種を選択＋営業メモに実業種を記載する運用。`00 共通` 選択可 |
| E-26 | 32bitメモリ制約（巨大文字列連結） | (なし) | 実装規約 | max_context_chars上限＋バッファ連結方式（modUtilText） |

#### E-03 入力打切りの詳細規則（実装はこの順序どおりに行う）

1. **上限の選択**: 案件一覧の `dossier_tier` で決める。`t1_quick` → config `max_context_chars`（既定40,000字）。`t2_full` / `t3_sparring` → config `t2_max_context_chars`（既定100,000字）。判定単位は「1回のLLM呼び出しに載せる貼付テキストの合計字数」であり、ナレッジ注入分は別枠（下記5）で数える。
2. **打切り対象と優先順**（先に切るものから順に。1つ切るごとに再計測し、上限を下回った時点で停止する）: **追加ドシエ（input_dossier）→ 前回更新メモ（input_prev_renewal）→ 有報リスク章（input_yuho）→ 営業メモ（input_memo）→ HPテキスト（input_hp）**。
3. **打切らない4欄**: **現契約サマリ（input_contract）・現場メモ（input_field_notes）・付保の見立て（input_coverage_note）・ヒアリング回答（input_hearing_answers）**。いずれも他で代替できない一次情報であり、FR-34（現場メモの原文パススルー）・FR-35（ヒアリング回答=第2ラウンドの主入力）が原文性を要件にしているため、機械的に削らない。
4. **この4欄だけで上限を超える場合**: 打切らず、実行**前**に E0102 を警告として出し（「入力が上限を超えています。現場メモ・ヒアリング回答は自動では削れません。要点を残して短くしてください」＋各欄の現在字数）、利用者に削減を求める。利用者が続行を選んだ場合のみ、4欄を後ろから順に切り「(一部省略)」を挿入する（この経路もE0102をusage_logへ記録）。
5. **ナレッジ側の切詰め**: 貼付テキストの打切りとは別に、注入ナレッジは 15章§0.7 の順（成功事例→型→メニュー→種目→リスクライブラリ）で行数を削る。切詰めが発生したら run_log の detail に `truncated:<欄名>` を列挙して記録する（黙って削らない）。
6. 切詰めた欄には必ず末尾に `（一部省略）` を挿入し、E0102 を usage_log へ記録する。記録するのは欄名と削減後字数のみで、本文は記録しない（NFR-S3）。

### LLM経路系（E-14〜）
| ID | ケース | code | 対応 |
|---|---|---|---|
| E-14 | リボン未検出 | E0201 | 「AIリボンが見つかりません。導入手順は…」directへ自動フォールバック**しない** |
| E-15 | リボン利用上限（LimitCheck=True/上限文字列） | E0204 | 「本日のAI利用枠上限です」。案件状態は保持。**入念モード（deep）では**生成・批判・改訂のどの段で上限に当たっても、直前に検証合格した成果物までを確定保存し、`status` と `last_ok_step` はその段で確定する（批判・改訂の途中結果は破棄してよい。E-35/E-36と同じ扱い） |
| E-16 | リボン空応答/長時間無応答 | E0202 | 自動リトライなし（Wait既定1200秒のため）。「時間をおいて再実行」 |
| E-17 | direct: 429/5xx | E0203 | 指数バックオフ最大3回 |
| E-18 | direct: キーファイル無し | E0205 | 「direct経路は開発者専用です」 |
| E-19 | direct: refusal / finish_reason≠stop | E0206 | |
| E-27 | mock/directのまま利用者が本番使用 | (なし) | HOME赤帯常時表示「デモ/開発経路で動作中」 |
| E-50 | **リボン呼出中の無応答・画面ゴースト化**（`Application.Run` は同期ブロックのため、最大 `llm_wait_sec`（既定1200秒）の間VBAは制御を返さず、Windowsは約5秒で「応答なし」と判定して画面を白くゴースト化する。姉妹PoCが実機で観測・対処済み） | (なし) | 次の4点を必須とする。**(a) 呼出の前に確定表示**: `modUIProgress.SetStage` が呼出の**前**に「Step名／開始時刻／最大待ち時間／画面が白くなっても処理は続いている旨」をHOMEへ書き切る（呼出中は画面更新できない前提で文面を作る。書込先は13章§2.10 の hm_progress_step / hm_progress_started_at / hm_progress_max_wait / hm_progress_note）。**(b) ゴースト化抑止**: 姉妹PoCで実証済みの user32 `DisableProcessWindowsGhosting` のみを例外的に採用する（表示系・引数なし・user32限定。`#If VBA7 Then` の32/64bit両対応宣言で **ui層に置く**）。config `keep_window_alive`（既定TRUE）でオプトアウトできる。**(c) 一括実行はStep間で必ず `DoEvents` を挟む**（ui_lock中のクリックは SetStage への案内表示のみとし、モーダルダイアログは出さない）。**(d) `Application.ScreenUpdating = False` を使う場合は必ずエラーハンドラで `True` へ復帰させる**。復帰は `modUIProgress.ExitUiLock` に一元化し、個々のハンドラでの復帰漏れを構造的に無効化する |

### JSON・検証系（E-06〜）
| ID | ケース | code | 対応 |
|---|---|---|---|
| E-06 | JSON不正/スキーマ不合格 | E0302 | 修復リトライ1回（config `json_repair_retry`）→なお失敗なら step失敗・状態 `error`。**失敗した生応答は `sN_json` を汚さず `sN_json_failed`（s1〜s4）へ保存する**。`sN_json` に入るのは検証に合格した出力だけであり、これがE-12の状態再導出の前提になる。あわせて案件一覧の `failed_step` に当該Step（`s1`/`s2`/`s3`/`s4`）を書き、`last_ok_step` は更新しない。批判・改訂パスの不合格は E-35/E-36 のとおり本体Stepを失敗にしないため `failed_step` を立てない（13章§2.1が enum に持つ `s2c`/`s3c` は、批判・改訂の呼出そのものが利用上限等で中断し、当該ラウンドの本体出力を確定できないまま処理を打切った場合にのみ使う）。PF（受信箱）は従来どおり `pf_json` に保存する |
| E-07 | ID幻覚（menu/line/scheme/case_lib/pattern） | E0301 | CheckS3/PF等で不合格→修復→なお不合格は停止。**黙殺除去禁止** |
| E-32 | new案件でgaps/current_coverageが非空 | (なし) | CheckS1/S2で検知（S1は警告、S2は不合格→修復。15章の空配列規約） |
| E-33 | renewal案件でcurrent_coverage空 | (なし) | CheckS1不合格→修復→失敗時「現契約サマリの記載を確認してください」 |
| E-35 | **入念モードの批判パス不合格**（S2C/S3CがCheckS2C/CheckS3Cに不合格） | (なし) | 修復リトライ1回で直らなければ、**批判をスキップして生成版をそのStepの確定出力とする（Stepは成功扱い。`status`・`last_ok_step` は通常どおり進める）**。批判の生出力は `s2c_json` / `s3c_json` に残し、run_log に `step=s2c`（または `s3c`）・`validate_result=failed` を記録する。HOMEの `hm_warning` に「入念モードの審査を省略しました（生成版で続行）」を表示する。批判の失敗で本体Stepを巻き添えにしない |
| E-36 | **入念モードの改訂結果が本体スキーマ不合格**（改訂版がCheckS2/CheckS3に不合格） | (なし) | 修復リトライ1回で直らなければ、**改訂を破棄し、検証に合格済みの改訂前JSON（`s2_json`／`s3_json`）を確定として採用する（Step失敗にしない）**。`s2r_json` / `s3r_json` には**本体スキーマ検証に合格した改訂版だけ**を格納する（不合格の改訂版は `sN_json_failed` へ）。run_log に `step=s2r`（または `s3r`）・`validate_result=failed` を記録し、HOMEの `hm_warning` に「入念モードの改訂を破棄しました（改訂前で続行）」を表示する |
| E-49 | **長文応答の尾部劣化**（応答末尾で同名項目が重複出力され、重複側に他項目の内容が誤混入する。14章§3の実測で観測） | E0303 | `modValidate.NormalizeLlmJson(stepName, json, ByRef removedCount)` を**検証の前段**で `modPipeline` が全step・両経路に通す（S1だけの対策にしない）。配列要素を `modUtilText.NormalizeForHash` で正規化したうえで fnv1a64 のハッシュ一致により重複排除する。`removedCount > 0` のときは run_log の detail に除去件数と対象配列名を記録し、E0303 を警告として残す（**黙殺しない**）。除去後のJSONに対して通常のCheck関数を実行する |

### ナレッジ系（E-08〜）
| ID | ケース | code | 対応 |
|---|---|---|---|
| E-08 | ナレッジブック接続不可 | E0401 | 前回スナップショット（本体内隠しシート）で「前回時点の知識で続行」選択可。無ければ実行不可。起動時は警告のみで起動を止めない（12章§2.1 modBoot手順⑤） |
| E-09 | 該当業種のナレッジ0行 | (なし) | 「(登録なし)」を明示注入して続行。usage_log記録（整備優先度シグナル） |
| E-13 | 新サービス候補追記時にナレッジブックがロック | (なし) | ローカル退避→次回起動時再送（12章§2.1 modBoot手順⑥）。案件処理は成功扱い |
| E-34 | ナレッジブックのID重複・必須列欠落 | E0402 | LoadKnowledge時に検査し、管理者向けに行番号列挙（利用者には「管理者に連絡」） |

### 状態・同時実行系（E-10〜）
| ID | ケース | code | 対応 |
|---|---|---|---|
| E-10 | 中間編集後の上流再実行 | E0601 | `InvalidateDownstream` で下流のJSON・シート・状態を無効化＋確認ダイアログ。無効化した data_key を run_log の detail に列挙する |
| E-11 | 実行中の多重クリック・案件切替 | E0602 | `modUIProgress.TryEnterUiLock(stepName)` でブロックする。**ui_lock の保持場所は modUIProgress のモジュール変数**（保持者Step名＋取得時の `Timer` 値）であり、**シート・configへ永続化しない**＝Excelプロセスの終了で自然消滅する。保持時間が `llm_wait_sec + 120` 秒を超えたロックは失効とみなして自動解除し、E0602 を記録したうえで新しい取得を許す（想定外の実行時エラーで `ExitUiLock` に到達しなかった場合の回復路）。解除は正常・異常のどちらの終了でも必ずエラーハンドラから `ExitUiLock` を呼んで行い、`ExitUiLock` は `Application.ScreenUpdating = True` の復帰も内包する（E-50(d)）。起動時 `RepairStates` は ui_lock を**無条件に初期化**する |
| E-12 | 強制終了による状態不整合 | E0603 | 起動時 `modCaseStore.RepairStates()` が次の規則で再導出する。**(1)** 参照するのは `s1_json`〜`s4_json`（=検証合格済みのみ。E-06）と案件一覧の `last_ok_step` の**突合**であり、両者が示す最大Stepの小さいほうを採る。**(2)** `failed_step` が非空の案件は `status` を `error` のまま維持し、上位ステータスへ昇格させない（`failed_step` の値自体も書き換えない=失敗の記憶を消さない）。**(3)** 導出対象から除外するキー: `s2c_json` / `s3c_json`（批判）・`s2r_json` / `s3r_json`（改訂）・`sN_json_failed`（不合格の生応答）・`sN_edited`（人手編集）・`s2_prev_json`（前ラウンド退避）・`input_*`。**(4)** ui_lock は無条件初期化。**(5)** 修復件数を戻り値として返し、0件でも起動は続行する |
| E-22 | セル32,767字制限 | E0604 | **保存側**: case_dataは32,000字で分割保存（結合は modCaseStore が透過処理）。**表示側**: 表示セルは先頭2,000字＋注記で、全文はcase_data。**受信箱body**: 32,000字で打切り＋警告。**入力側（v2.4拡張）**: 案件入力の貼付欄も1セル32,767字が物理上限であり、32,000字を超える資料は**連番の「続き」欄**（`ci_paste_<欄>_2`=「続き1」／`_3`=「続き2」。13章§2.11）へ分けて貼る。組立時に `modUICase` が本欄・続き欄を**末尾番号の昇順に vbLf で連結**して1本のテキストにする（空欄はスキップ）。続き欄を持つのは HP・有報・現契約サマリ・追加ドシエの4欄で、続き欄を持たない5欄が超過した場合は「続き欄のある欄へ移すか要点を残して削る」旨を案内する。各欄と合計の現在字数は案件入力の文字数カウンタ（`ci_count_*`）に常時表示する（11章§2） |
| E-23 | 案件ID同日連番枯渇/重複 | E0605 | 999まで対応・重複時採番リトライ |
| E-51 | **処理中のセル編集モードによるVBA停止／シート保護方針** | (なし) | **シートProtectは使わない**（姉妹PoC裁定準拠: 全面Protectは図形・表の再描画のたびにUnprotect/Protectが必要になり、描画失敗時に保護状態が壊れる）。代替として **(a)** 入力対象外セルの `Locked = True` ＋ **(b)** `ws.EnableSelection = xlUnlockedCells`（編集可能セル以外を選択させない）＋ **(c)** 全アクション完了時の `ParkFocus`（フォーカスを編集不可の待避セルへ戻し、編集モードへの偶発進入を減らす。ui層 `modUIProgress` が持つ）で構成する。実行開始時にも `ParkFocus` を通してから処理へ入る。将来どうしても保護が必要になった場合に限り、`SetCellSafe` 内部で Unprotect→書込→`On Error` 込みの**無条件 Protect** を行う形に限定する（保護の掛け外しを他の場所に散らさない）。ui_lock をシートへ永続化しないこと（E-11）と合わせて、ロックも保護もブック側に残さない設計とする |
| E-52 | **configシートの欠損・読込失敗**（シート不在・キー行の欠落・値の型不正） | E0608 | **既定値で続行し製品を止めない**。`modBoot` 手順②（12章§2.1）は13章§2.3の既定値表を先に登録してから configシートの値で上書きするため、読込に失敗しても既定値のまま起動できる。失敗したキー名（値は書かない）を detail に添えて **E0608 を err_log へ警告記録**し、HOMEには赤帯を出さない（利用者の操作で直せない事象のため）。全キーの読込に失敗した場合も同様に続行する |

### 出力系（E-20〜）
| ID | ケース | code | 対応 |
|---|---|---|---|
| E-20 | PPT生成失敗（COM/テンプレ破損/保存先権限） | E0501 | S4結果は保持され再出力可。テンプレ不在は白紙生成へフォールバック。**PPT出力自体はPhase 1.5**（10章FR-07。Phase 1で本行が発火するのはPhase 1.5先行検証時のみ） |
| E-21 | 出力先フォルダ不存在 | (なし) | 自動作成→不可ならDocuments直下へフォールバックし通知。対象は `html_out_dir`（HTMLレポート）・`ppt_out_dir`（Phase 1.5）・企業ドシエファイルの保存先の3経路すべて |
| E-47 | **HTML/JSへの埋め込みエスケープ**（LLM出力・貼付テキスト・現場メモ原文がそのまま `<script>` ブロックやHTML本文へ入る） | (なし) | `modExportHtml` は次の3規約を必ず守る。**(1) データの埋め込み形式**: JSONは「**1本のJS文字列リテラル＋`JSON.parse`**」の形でのみ埋める（`var DATA = JSON.parse("…");`）。JSONをそのままJSオブジェクトリテラルとして直書きすることを**禁止**する。文字列リテラル化の際に `modUtilText.JsStringSafe` を必ず通す（`</` → `<\/`、U+2028/U+2029、その他の制御文字をエスケープ）。**(2) HTML本文への差し込み**: テンプレートのテキストノード・属性値へ差し込む値は `modUtilText.HtmlSafe`（`& < > " '` のエンティティ化）を必ず通す。エスケープを通さない埋め込み経路が1つでもあれば出荷しない（17章 T-46 の出荷前検問で grep 検査する）。**(3) 文字コード**: 書き出しは `ADODB.Stream`（Charset="utf-8"・BOMあり）で行い、テンプレ先頭に `<meta charset="utf-8">` を含める（VBAの `Open`/`Print #` はCP932で書くため使わない） |
| E-48 | **HTML生成失敗**（ディスク書込失敗・出力先の権限不足・テンプレ組立失敗） | E0502 | S1〜S3のJSONは保持されるため再出力可。出力先が原因の場合はE-21のフォールバックを先に試す。失敗しても案件の `status` は下げない（出力は状態遷移に影響しない）。成功時は確定パスを案件一覧の `report_path` に記録する |

### 受信箱・ウォッチ系（E-40〜）
| ID | ケース | code | 対応 |
|---|---|---|---|
| E-40 | 受信箱の一括診断中の途中失敗 | E0701 | 診断済み分は保存、失敗分はundiagnosedのまま残す |
| E-41 | rejected判定でdrop_type未選択 / conditional_holdでrevive_tag・期日未入力 | (なし) | 保存ブロック（統制語彙の必須化） |
| E-42 | ウォッチ貼付テキストの著作権配慮 | (なし) | 記事全文の恒久保存をしない: ウォッチログには要約と出典のみ保存（本文はLLM処理後に破棄）。§2 NFR-S6 |
| E-43 | プロンプトインジェクション（貼付テキスト内の指示文） | (なし) | ■■■境界＋BLOCK_GUARD＋出力スキーマ固定＋ID実在検証の多層防御（攻撃が成功しても不正な成果物が通らない）。境界記号そのものの偽装はE-04の `■■■`→`[境界記号]` 置換で潰す |
| E-44 | 壁打ち履歴の肥大（リボン入力上限超過） | (なし) | 渡す履歴を直近 sparring_max_turns 往復に制限（全履歴はcase_data保存）。上限系エラー(E0204)時は「往復数を減らして再開」を案内 |
| E-45 | T2ドシエの収集結果に他社記事の全文コピー | (なし) | 利用ガイドで「要点＋出典」貼付を推奨。生成物への転載はNFR-S6のとおり出典言及に限定 |
| E-46 | 数式インジェクション（外部由来テキストの先頭 `=` `+` `-` `@`。貼付テキスト・LLM出力・受信箱body・ファイル名） | E0606 | セルは実行環境である前提で扱う（NFR-S7）。書き込み口 `SetCellSafe` で先頭式記号の無害化（`'`前置）・NULバイト除去・32,000字切詰め。**CSV出力**も同ガード（集計ファイルを開いた管理者側で発火するため）。**HTML出力経路**も同じ外部由来テキストを扱うため、`modExportHtml` では先頭式記号の無害化に代えて E-47 の `HtmlSafe` / `JsStringSafe` を必ず通す（HTMLでは式記号は無害だが、エスケープ漏れが同種の「外部由来テキストがそのまま実行文脈に入る」欠陥になるため、3書き込み口すべてを NFR-S7 の一元化対象とする）。発火時は E0606 を err_log に記録し、detail には**発火箇所（シート名・列名）のみ**を書き本文は記録しない（NFR-S3） |

## 2. セキュリティ設計

### NFR-S1: データ分類と投入可否マトリクス

| 分類 | 例 | ribbon投入 | 備考 |
|---|---|---|---|
| 公開情報 | HP・有報・プレスリリース | **可** | 本製品の基本入力 |
| 社内作成・顧客固有でないナレッジ | メニュー・型・判断基準・匿名化済み事例 | **可** | 登録要件: 顧客名・個人名を書かない |
| 自社保有の契約情報（現契約サマリ） | 種目・限度額・特約・保険料 | **条件付き（OQ-6）** | 確定まで**匿名化を既定**（E-31）。匿名化後は「どの企業か特定できない契約構造情報」として扱う |
| 顧客の非公開情報（商談で得た内部情報） | 財務詳細・工程・見積 | **不可（Phase 1）** | OQ-1確定まで。営業メモ欄に注意書き常設 |
| 個人情報 | 氏名・連絡先 | **恒久不可** | E-05で機械ブロック（走査対象は貼付欄・受信箱body・壁打ち発話・判断台帳 situation/key_reason・フィードバック customer_quote の5経路）。**唯一の例外がフィードバックの customer_quote** で、顧客の逐語引用がFR-11の要件のためブロックせず「警告＋伏字の差し替え案の提示」とする。伏字を断って保存されたものも本製品からLLMへは送らない（フィードバックは学習データの蓄積先であり、リボンへの投入経路を持たない）ため、投入可否としては引き続き**不可**である |

### NFR-S2: 認証情報（**キーはローテーション不可＝借用の共用キー1本**を大前提とする）
**前提（発注者確認 2026-08-28）**: 本PJが使えるAPIキーは全社共用の借り物1本のみ。**再発行・ローテーションの権限は現場になく、実質不可能**。したがって「漏れたら作り直す」が使えない＝**一度露出したら恒久被害**。キーの扱いは性善説の本製品の中で唯一、例外的に厳格化する。

- ribbon経路＝キー概念なし（**これが基本経路。キーが物理的に存在しないので漏れようがない**）。本製品は可能な限りribbon経路で完結させる。
- direct経路（開発・検証用）＝キーは `%APPDATA%\RPN\api_key.txt` に置き、**ブック・config・ログ・画面・リポジトリ・配布物のどれにも一切書かない**。ビルド処理はキーに触れない（環境変数からブックへ焼き込む方式も採らない）。
- **禁止（PoCの失敗に学ぶ）**: 難読化してブックに埋め込む方式（PoCのOBF1）は使わない。難読化は「漏れてないように見せる」だけで、鍵とアルゴリズムが同居すれば復元可能＝ローテ不能キーでは致命的。
- **配布物・リポジトリにキーを入れない**は、ローテ不能キーを守る唯一の構造的な壁。走査仕様は次の4項目を正とし、17章 T-46② はこれを参照して実装する（姉妹PJの実漏洩は「難読化blobが xlsm 内の sheet23.xml に埋まっていた」形であり、平文のキー形状文字列を素のgrepで探すだけでは原理的に検出できない）。
  1. **対象**: リポジトリの全trackedファイル ＋ ビルド成果物 `*.xlsm` / `*.xlsx` を `unzip -p` で**全パート展開したテキスト**（xlsx/xlsmはZIPであり、素のgrepではXMLパート内が見えないため展開が必須）。
  2. **検出パターン**: `sk-[A-Za-z0-9_-]{20,}` ／ 連続32文字以上の `[0-9a-f]` ／ 連続40文字以上のBase64様文字列 ／ 文字列リテラルに `OBF`・`難読`・`_KEY` を含む定数定義。
  3. **buildスクリプト検査**: `build/` 配下のスクリプトが `api_key.txt` または環境変数からキーを読む行が存在しないことを grep で確認する（「ビルド処理はキーに触れない」の機械的担保）。
  4. **検出時の出力**: ファイル名と行番号のみを出し、**キー値そのものは出力しない**（検問ログ自体が漏洩経路にならないようにする）。
- Git運用: 本製品リポジトリに `.xlsm/.xlsx` 成果物と `dist/` を**コミットしない**（`.gitignore` で除外し、`git check-ignore` で実効を確認）。万一混入したら、その時点で当該キーは「これ以上広げない」対応（リポジトリ非公開化・共有範囲の即時最小化）しか採れない＝**予防が全て**。

### NFR-S3: ログとマスキング
err/usage/run_log に入力・生成本文を記録しない（detailは最大400字のメタのみ）。本文は case_data / pf_json のみ＝利用者ローカルのブック内。共有フォルダに置くのはナレッジ（顧客固有情報なし）と成果物（Phase 1はHTMLリスクレポート、Phase 1.5以降はPPTも。いずれも利用者判断）のみ。個人名は全シート入力禁止（13章§4）。

### NFR-S4: マクロ配布統制
配布前に社内ルールに従い署名（OQ-2）。AI生成コードを利用者に実行させる仕組みは持たない（HTMLレポートもPPTも固定VBAのテンプレート組立であり、LLMにHTML・スクリプトを書かせない=10章FR-37）。

### NFR-S5: 監査可能性
全LLM呼び出しが run_log に残る（誰が・いつ・どの案件・経路・モデル・注入ナレッジ・検証結果）。LLM生出力は当該ラウンドの検証合格版を `sN_json` に、不合格版を `sN_json_failed` に保存する（E-06。完全な実行履歴はrun_log側で追う）。**HTMLリスクレポートの末尾**（Phase 1.5以降はPPT最終ページにも）に「本資料はAI支援により作成した骨子を人が確認・編集したものです」を自動挿入。リボンtoolN=`<config app_tool_prefix><step>`（既定 `リスク提案ナビ:step`）で管理側ログとも突合可能。

### NFR-S6: 外部コンテンツの扱い
ウォッチで収集した記事は要約＋出典のみ保存（全文の恒久複製をしない）。生成物への他社商標の使用は「出典として言及」に限定し、提案書には自社名義の記述のみを載せる。

### NFR-S7: 外部由来テキストの書き込み口の一元化（姉妹PJ B6BE7監査の教訓・docs/21 §5）
外部由来テキスト（ユーザー貼付・LLM出力・受信箱body・取込ファイル名）が外へ出る口は**3つ**あり、そのすべてを専用関数に強制する。防御関数が1本あっても素通り経路が3箇所残っていた実例があるため、「口ごとに1関数」を機械検査で担保する。

| # | 書き込み口 | 強制する関数 | ガード内容 | 参照 |
|---|---|---|---|---|
| ① | **セル**（シートへの書込） | `modUtilText.SetCellSafe` | 先頭式記号（`=` `+` `-` `@`）の `'` 前置・NULバイト除去・32,000字切詰め | E-46 |
| ② | **CSV**（ログ・集計の書出） | `modUtilText.SetCellSafe` と同一ガードを通す | 同上（集計CSVを開いた管理者側のExcelで発火するため） | E-46 |
| ③ | **HTML/JS**（HTMLレポート生成） | `modUtilText.HtmlSafe`（HTML本文・属性値）／ `modUtilText.JsStringSafe`（JS文字列リテラル） | `& < > " '` のエンティティ化／`</`→`<\/`・U+2028・U+2029・制御文字のエスケープ。JSONは「1本のJS文字列リテラル＋`JSON.parse`」形式でのみ埋める | E-47 |

**実装完了条件（17章 T-46① の検問と同一の定義。両章で一致させる）**: 検査対象は `src/**/*.bas` から `modUtilText`（防御関数の定義本体）を除外したもの。検出パターンは `\.Value2?\s*=` ／ `\.Formula\w*\s*=` ／ `(Cells|Range|Offset)\([^)]*\)\s*=`（VBAで最頻の既定プロパティ代入 `ws.Cells(r,c) = s` を取りこぼさないため）。定数・ヘッダの書込は行末に `' SAFE:const` の注記を必須とし、**注記の無いヒットのみをFAIL**とする（除外が実装者の裁量ではなく差分レビュー可能な痕跡として残る）。③については「HTMLへ差し込む値が `HtmlSafe` / `JsStringSafe` を経由しない埋め込みが grep でゼロ」を同じ検問に含める。

あわせて2点を設計原則とする: ①**クライアント側のデータはセキュリティ境界にしない**（本製品のナレッジブックは全員閲覧前提・顧客固有情報なし=NFR-S1で担保。「権限」「見せ分け」を配布ファイル内のフィルタで実現しない） ②**LLMが返したID（メニューID・ナレッジID等）は必ず提示済み候補とのホワイトリスト照合を通す**（既存のID実在検証=E-43がこれに該当。取得側でも再チェック）。

## 3. 確認事項（OQ。ブロック対象を明示）

| ID | 事項 | 先方 | ブロックするもの |
|---|---|---|---|
| OQ-1 | 顧客非公開情報のAI投入可否（社内規程） | 情シス/コンプラ | Phase 2の入力拡大のみ |
| OQ-2 | マクロ配布の署名要否・配布経路 | 情シス | リリース手順の最終化 |
| OQ-3 | AIリボンの利用上限値（**入念モード=1案件8〜12呼び出しの前提で確認**） | リボン担当部署 | 大量PoC時の計画のみ（厳しければ deep_transport=direct で回避可） |
| OQ-4 | direct経路（OpenAI API）の業務利用範囲 | 情シス | direct本番利用のみ（開発は非ブロック） |
| OQ-5 | VBAから外部Webサイトへの取得可否（プロキシ） | 情シス | ウォッチ半自動化のみ（Phase 1.5以降） |
| OQ-6 | **現契約情報（自社保有の契約データ）のリボン投入可否** | 情シス/コンプラ | **なし（匿名化既定で先行）**。可の確定後に匿名化を任意化 |
| OQ-7 | AIリボンの1呼び出しあたり入力上限（文字数/トークン） | リボン担当部署 | t2_max_context_chars・sparring_max_turns の最終値のみ（暫定値で先行） |
| OQ-8 | ~~信用調査データの利用可否~~ **解決（2026/8/25 グループ長回答）**: 帝国DBは社内業務利用可（企業検索）。収集レシピの finance/profile 観点の正式ソースに昇格。あわせて「**他社での付保状況は競争情報のため取得不可**」と確定 → 他社付保はヒアリングシートの定番質問に昇格（データで取れないものは聞く設計） | 解決 | — |
| OQ-9 | 帝国DB情報の**AIツール（リボン/ディープリサーチ）への入力可否**（社内業務利用とはライセンス上別論点）と、生成物（提案書）への転載範囲 | 調達/コンプラ | 帝国DB本文の直接貼付のみ（確認まで「要点を人手で要約して貼る」運用で先行可） |
| OQ-10 | **VIEW情報（社内の企業情報システム）のAI投入可否**（高橋FB②。財務インパクト表現 FR-43 の精度が上がる）。決算公告・EDINETは公開情報のため投入可（確認不要） | 情シス/主管部署 | なし（確認まで人手要約転記で先行） |
| OQ-11 | **社内既存資料の転用可否**: リスクコンサルティングポータルのリスマネパンフ・業種別リスクマップチラシ・法人版リスクマップ、提案書サンプル（商C企）、提案書ライブラリ（企金営開）を、シードナレッジ（リスクライブラリ・型・成功事例）およびHTMLレポートの参考表現として利用してよいか | 各主管部署 | シード装填の一部のみ（公開情報由来の装填は先行可） |
| OQ-12 | **社内「マルチモーダルチャット」の利用規約（顧客情報の投入可否）**。2026-08-28利用者確認で大半が判明済み: 正式名称「マルチモーダルチャット」/社内ポータルにホストされたChat型Webアプリ（UI基盤はChainlitと推定=画面の「Build with Chainlit」表記）/Excelアドインのボタンからブラウザ起動/モデルGPT-5.6Sol（リボンは5.5まで）/PDF・Excel等の添付可/**外部から呼べるAPI口なし**（製品パイプライン組込は不可で確定）/主管はリボン・ディープリサーチと同じHD系またはビジネスプロセスデザイン部と推定/仕様・アーキテクチャ非公開。**残る未確認は利用規約=顧客情報・個人情報の投入可否のみ**（ディープリサーチは投入不可が明文。それに準じる前提で運用） | 主管部署 | なし（確認まで公開情報のみ投入。位置づけ=営業の手動前処理ツール: 本製品が出力したファイルの添付・整形用。※社内ブラウザはWebからのファイルダウンロード不可のため「有報PDFをダウンロードして添付」は成立しない——添付できるのは自作ファイルとメール受領ファイルのみ） |
