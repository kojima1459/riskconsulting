# 10. プロダクト概要と要求・要件定義

**製品名**: リスク提案ナビ（コード名: RPN）
**版**: v2.5（W7・裁定書25「W7センターピン整合」: FR-35 の第2ラウンドを「フリート・GLTD等の各論」から**「採用した提案の種目に絞った深掘り」**へ改め〔種目特化AIは作らない。絞り込みの器は13章§2.1 `focus_line_ids`・15章§1.2c〕、FR-44 に**決算・財務の貼付欄**〔13章§2.11(a) 7本目 `input_finance`〕と**S1 `financials`**〔15章§2〕という投入先を明記した。以下は前版までの変更概要。v2.4: ニューリスク=エマージング確定: FR-05のS2出力に `emerging_risks`〔新種・新興リスク0～3件〕を追記し、FR-37のセクション一覧のニューリスクに注記＋「訪問で分かったこと」を追加。以下は前版までの変更概要。実装前監査72件の裁定を反映。UC1/UC2/UC9とプレイ・レジストリの出力をHTMLレポート系へ統一〈PPTはPhase 1.5〉、NFR-P1をHTMLレポート込みの目標へ、NFR-P2を「リボン呼出前の確定表示」方式へ、NFR-P3をティア連動の上限へ改めた。〈検証指摘の修正〉FR-07のS4出口を「S4_骨子シート表示＋ヒアリングシート生成」に確定し提案書のファイル化をPhase 1.5へ、UC1/UC2/UC9とPL-01/02の成果物記述をこれに整合、docs/08参照をD-1～D-9へ。v2.3までの変更経緯はdocs/20章）
**読者**: 実装担当（Opus/Sonnet エージェント）、レビュー担当、PJメンバー
**文書体系**: 本書(10)～17章が実装の正。19章（用語集・整合性チェックリスト）が全文書共通のID・enumレジストリを保持し、矛盾時は19章のレジストリ→各章の順で優先。

---

## 1. プロダクト概要

**営業の「現場の声」と商品部の「商品・座組の知識」をAIの上で毎日すれ違わせ、新しい提案（型）を生み出し、成功パターンを全員に横展開する二方向ナレッジエンジン。**

- 形態: `リスク提案ナビ.xlsm`（本体・利用者ローカル実行）＋ `ナレッジブック.xlsx`（共有・管理者編集）
- LLM: 社内AIリボン「リボンちゃん」経由（全社配布・キー不要）。開発用にdirect（OpenAI API）・mock経路を併設（14章）
- 構造: 共通シャーシ（構造化→突合→選択肢→成果物→記録）の上に**プレイ（作戦カード）**を載せる。Phase 1はPL-01～03、Phase 1.5でPL-04～06、Phase 2でPL-07/08（§5）
- 基盤実証: PoC「MyBookshelf」（github.com/kojima1459/notebook, branch `claude/internal-notebook-lm-chatbot-B6BE7`）のLLMゲートウェイ・config・ログ・テスト方式を移植（12章）

### 二方向エンジン（製品の心臓部）

```
【使うエンジン】営業・商品部が案件ごとに使う
  案件入力 → S1構造化 → S2リスク/ギャップ → S3提案 → HTMLリスクレポート
                                                    → S4_骨子シート・ヒアリングシート（提案書のファイル化はPhase 1.5）
【生むエンジン】商品部が月次・四半期で回す
  受信箱(投稿・現場の声・ウォッチ) ＋ 案件事実ログ
    → PL-03プリフライト(入口診断) / PL-06型生成(掛け合わせ)
    → 編集会で採択 → 型ライブラリ → 翌月から全員の使うエンジンに注入
```

**設計原則: 現場の声は「集めない」。** 営業は自分の得（準備が15分・提案が厚い）のために使うだけで、入力と商談事実が副産物として構造化されて貯まる。営業向けに入力専用の作業を課す機能を作ってはならない。

## 2. 背景・目的

1. 提案準備の時間（1日→15分）と質の底上げ（内田部長の春華堂原体験）
2. 属人化の解消: 80点までの素材は電子データにあるが、80→100点（勝ち筋・座組の引き出し・判断の理由）は共有されていない
3. 商品部と営業の知識非対称の解消: 商品部は現場を知らず（アイデアが机上化・投稿ボックス2年分1,680件の8割が受け側構造でドロップ）、営業は商品・座組を知らない（提案が定番化）
4. 型の量産: ヤモリ→じゃぱん21→WOTAのような横展開を、個人の偶然から仕組みの必然へ（商品×CSV×DXの実装）

## 3. ペルソナ

| ID | ペルソナ | 特徴 | 主要ニーズ |
|---|---|---|---|
| P1 | 営業・営業企画担当 | 非エンジニア。全種目の商品知識はない | 更新・新規の準備を15分で。玄人商品（費用利益等）も漏れなく検討台に |
| P2 | 商品部 UW/リスコン担当 | 種目の専門家。自Gの外は知らない | 生成物のレビュー・修正。現場の声と新しい型の種が届いてほしい |
| P3 | ナレッジ編集長（商品部・当面は開発者兼務） | 月次編集会の主宰 | AIの下書きを裁くだけで型・カタログ・判断基準が育つこと |
| P4 | 開発者（小島） | VBA・OpenAI APIを扱える唯一の要員 | PoC流儀での1人保守。仕様と実装の一致 |

## 4. ユースケース（プレイ別）

| ID | ユースケース | プレイ | 主アクター | 流れ（正常系） |
|---|---|---|---|---|
| UC1 | 新規訪問先の提案準備 | PL-01 | P1 | 案件作成(case_type=new)→HP貼付→一括実行→**HTMLリスクレポート（S1+S2+S3）＋S4_骨子シート＋ヒアリングシート**（15分。提案書のファイル化はPhase 1.5） |
| UC2 | 更新案件の診断と提案 | PL-02 | P1 | 案件作成(renewal)→HP＋現契約サマリ貼付→一括実行→ギャップ診断・アップ/クロスセル提案を載せた**HTMLリスクレポート（S1+S2+S3）＋S4_骨子シート＋ヒアリングシート**（提案書のファイル化はPhase 1.5） |
| UC3 | 生成結果の専門レビュー | 共通 | P2 | S1～S3シートを修正→当該Step以降のみ再実行 |
| UC4 | 商談後の事実記録 | 共通 | P1 | フィードバックシートに事実（展開・顧客の発言・結果）を選択式中心で記録（5分） |
| UC5 | アイデア・ネタの投函と診断 | PL-03 | 全員 | 受信箱に投稿→プリフライト診断→補強→判定（統制語彙） |
| UC6 | 月次編集会 | PL-05/06 | P3 | ウォッチ結果と型候補の下書きを採択/却下（30分）→ナレッジブック反映 |
| UC7 | 戦略案件の座組開発 | PL-04 | P2+P1 | 型・パターン・機構を注入した対話型セッションで座組仮説を作る |
| UC8 | UW判断の記録 | 共通 | P2 | 判断台帳に条件・選択・決め手・結果を記録（PL-07/08の学習データ） |
| UC9 | mock環境での動作確認 | 共通 | P4 | mock経路で全画面フロー・**HTMLリスクレポート生成（S1+S2+S3）**・S4_骨子シート表示・ヒアリングシート生成を社外環境で確認（提案書ファイル化の確認はPhase 1.5） |

## 5. プレイ・レジストリ（本製品の機能単位。全章共通）

| ID | プレイ | Phase | 1回のLLM呼び出し数(目安) | 出力 |
|---|---|---|---|---|
| PL-01 | 新規提案 | 1 | 4（S1～S4） | HTMLリスクレポート（S1+S2+S3）＋S4_骨子シート＋ヒアリングシート（提案書のファイル化はPhase 1.5） |
| PL-02 | 更新診断（アップ/クロスセル） | 1 | 4（S1～S4・更新ブロック注入） | ギャップ診断を載せたHTMLリスクレポート（S1+S2+S3）＋S4_骨子シート＋ヒアリングシート（提案書のファイル化はPhase 1.5） |
| PL-03 | 投稿プリフライト診断 | 1 | 1（PF） | 診断結果（生存見込み・予測棄却類型・組み替え案） |
| PL-04 | 壁打ち・座組開発（対話型） | **1** | 可変（対話。1発話=1呼び出し） | 仮説の反復精錬・座組仮説。気づきは受信箱へワンクリック送信 |
| PL-05 | 情報ウォッチ | 1.5 | 1～3（WT） | ニュースの仕分けとナレッジ更新案 |
| PL-06 | 型生成（掛け合わせ） | 1.5 | 3～10（FG） | 型候補（格付・成立条件・検証仮説つき） |
| PL-07 | BID戦略 | 2 | 未設計 | 条件設計・交渉シナリオ（判断台帳100件超で設計着手） |
| PL-08 | 高ロス対応 | 2 | 未設計 | 選択肢比較（増率/クローズ/縮小/伴走）＋類似案件の顛末 |

Phase 1の実装スコープは **PL-01～PL-04＋共通基盤＋記録系（フィードバック・判断台帳・受信箱の器）**。PL-05/06は運用（人力＋docs/08プロンプト）で先行し、Phase 1.5でツール化する。

### 入力ティア（ドシエ・ティア。案件属性 dossier_tier）

| ティア | 対象 | 収集 | 所要 | 期待水準 |
|---|---|---|---|---|
| **T1 クイック** | 量をこなす更新・新規 | 収集レシピ8項目（15章§2.0） | 15分 | 恥ずかしくない80点の初回準備（フロア） |
| **T2 フルドシエ** | 重要案件・戦略先 | 14観点（8項目＋SNS評判・競合・業界市況・マクロ・財務/付保コンテキスト・**拠点ハザード**）。収集はディープリサーチ社内アプリへの定型プロンプト（docs/08 D-1～D-9）＋S1が生成する案件別プロンプト（research_requests）で自動化し人はコピペ運搬 | 1～2時間（放置込み・人の作業15分） | 仮説を持って訪問する水準 |
| **T3 壁打ち** | 提案を研ぎ澄ます局面 | T1/T2のドシエ＋S1～S3結果を初期コンテキストにPL-04で反復 | 可変 | 三菱電機型（付加価値の天井） |

思想: **フロア（T1）は量と底上げとデータ収集のため、天井（T2×T3）が付加価値のため**。入力は多いほど有利（充足度診断が誘導）だが、収集コストをAI（ディープリサーチ）に転嫁することで「多く集める」を現実にする。

## 6. 要求→要件トレーサビリティ

| 要求ID | 要求 | 対応要件 |
|---|---|---|
| RQ1 | 営業: 更新・新規の準備を15分で終えたい | FR-01～10, FR-30, FR-31 |
| RQ2 | 営業: 全種目・座組を覚えずに厚い提案がしたい | FR-06, FR-07, FR-13 |
| RQ3 | 商品部: 現場の声・課題感を継続的に得たい | FR-11, FR-12, FR-20 |
| RQ4 | 商品部: 投稿の無駄打ちを減らし判定基準を見える化したい | FR-14～17 |
| RQ5 | PJ: 型を生み続け横展開したい | FR-18～21 |
| RQ6 | PJ: 将来のUW高度化（BID・高ロス）に備えたい | FR-22 |
| RQ7 | 会社: 情報セキュリティ事故を起こさない | NFR-S1～S7（16章） |
| RQ8 | 開発者: 1人で保守できる | NFR-M1～M3 |
| RQ9 | 利用者: キー・設定なしで使いたい | FR-23, NFR-U1 |

## 7. 機能要件（FR）

### 案件処理（使うエンジン: PL-01/02）
| ID | 要件 | 優先度 |
|---|---|---|
| FR-01 | 案件単位で入力・中間結果・状態・種別を管理。案件ID `C-YYYYMMDD-NNN`、`case_type` enum(new/renewal) | Must |
| FR-02 | 入力（共通・必須）: 企業名／業種（業種マスタ選択）／HPテキスト貼付。入力（共通・任意）: 有報リスク章／営業メモ。入力（renewal時・必須）: 現契約サマリ貼付。入力（renewal時・任意）: 前回更新メモ | Must |
| FR-03 | 案件コンテキストを選択式で保持: `channel`(wholesale/retail)／`kanji`(lead/non_lead/coins)／`bid`(yes/no)／`reins`(none/reins/captive)／`other_insurers`(自由記述) 。S2以降のプロンプトに注入し、run_log の層別軸とする | Must |
| FR-04 | Step1: 企業プロファイル構造化（renewal時は現契約の構造化 `current_coverage` を含む。newでは空配列）。Schema-S1 | Must |
| FR-05 | Step2: リスク仮説（**リスクユニバース10分類**・頻度×影響（3値＋1～5スコア）×根拠×確認点×**移転可能性(insurability)**×**損害規模の財務対比(loss_scale_note)**×**status(仮説ライフサイクル)**）。renewal時は付保ギャップ `gaps`（uninsured/underinsured/overlap）を併せて出力（newでは空配列）。**ニューリスク（エマージング＝サイバー・気候変動等の新種・新興リスク）`emerging_risks` を0～3件含む**（当てはまりが薄ければ空配列。一般論の羅列は禁止）。ナレッジ（リスクライブラリ）注入。Schema-S2 | Must |
| FR-06 | Step3: 提案3本。各提案に `proposal_kind`(upsell/cross_sell/scheme) を付与し、**実在の** menu_id/line_id/型ID にのみ紐づけ（VBA後検証）。ナレッジ（メニュー・種目・成功事例・型ライブラリ）注入。Schema-S3 | Must |
| FR-07 | Step4: 構成JSON→**S4_骨子シートへ表示（人手編集可）**＋ヒアリングシート生成（質問10問以内）。Schema-S4。**提案書のファイル化（HTML/PPT）はPhase 1.5**（高橋FB⑥: 編集可能形式は最後の最後。Phase 1のHTMLリスクレポートはS1+S2+S3から作る別成果物で、S4は読まない〈18章§1.1〉） | Must |
| FR-08 | 中間結果の人手編集と、当該Step以降のみの再実行。再実行時は下流結果を無効化 | Must |
| FR-09 | 「一括実行」と「Step個別実行」の両提供 | Must |
| FR-10 | 全Step実行を run_log に記録（経路・モデル・所要・注入ナレッジID・検証結果・実行者） | Must |

### 記録系（現場の声の副産物収集）
| ID | 要件 | 優先度 |
|---|---|---|
| FR-11 | フィードバックは**事実のみ**記録: `event`(next_appt/quote_req/won/lost/renewed/claim)／顧客の実際の発言（引用）／成約条件／事後ロス。解釈（なぜ刺さったか）の入力欄は設けない | Must |
| FR-12 | 案件の入力・中間・最終・事実記録を、案件IDで一気通貫に参照できる（生むエンジンの学習データ） | Must |
| FR-22 | 判断台帳: UW判断の事実（状況要約／選択=raise/close/restrict/keep/improve／係数メモ／決め手1行／結果／事後ロス）を1案件5分で記録。ツール外案件も記録可 | Must |

### 受信箱・プリフライト（PL-03）
| ID | 要件 | 優先度 |
|---|---|---|
| FR-14 | 受信箱: 誰でも投函可（テーマ＋本文＋出所 enum: member_post/field_voice/watch）。状態 enum: undiagnosed/diagnosed/adopted/conditional_hold/rejected/merged | Must |
| FR-15 | プリフライト診断（1コール）: 保険原理5問判定＋生存文法(a)～(d)適合＋カタログ・型・研究中テーマとの重複照合＋予測棄却類型（T1～T10）＋3手（経路/給付/主体）による組み替え案。Schema-PF | Must |
| FR-16 | 判定の統制語彙: `drop_type` enum T0～T10 必須選択（自由記述は補足）。条件付き保留には `revive_tag` enum(tech/regulation/partner/data/market)＋見直し期日 | Must |
| FR-17 | 同一テーマの重複投函は棄却せず関心度スコア（件数）として集計・表示 | Should |

### 生むエンジン（PL-05/06。Phase 1.5だがデータ構造はPhase 1で用意）
| ID | 要件 | 優先度 |
|---|---|---|
| FR-18 | 型ライブラリ・機構ライブラリ・パターンマスタ（P1～P15）・適用先候補・巡回リストをナレッジブックに保持（13章） | Must(構造) |
| FR-19 | 情報ウォッチ: 貼付された収集テキストをAIが仕分け（new_asset/pattern_example/target_candidate/rule_change、該当パターンID付与）。Schema-WT | Phase1.5 |
| FR-20 | 型生成: 機構×未解決リスク×器（パターン）の掛け合わせ候補を生成し、生存文法4条件でセルフ審査のうえS/A/B格付・検証仮説つきで提示。Schema-FG | Phase1.5 |
| FR-21 | 採択された型は型ライブラリ登録により、次回以降のS3注入対象に自動的に含まれる（追加開発なしで効く注入設計） | Must |
| FR-13 | S3の「該当メニュー・型なし」は新サービス候補としてナレッジブックへ自動記録 | Must |

### 基盤
| ID | 要件 | 優先度 |
|---|---|---|
| FR-23 | LLM経路 config切替: ribbon(既定)/direct/mock。ribbonはキー不要。directのみ外部ファイルのキー使用 | Must |
| FR-30 | 実行中の進捗常時表示・多重実行防止・失敗Stepからの再開 | Must |
| FR-31 | **入力収集標準と充足度ゲート**: 案件入力に収集レシピ（T1=8項目/T2=14観点。15章§2.0）を常設表示。S1が入力充足度（14観点のok/partial/missing＋ティア基準の総合high/mid/low＋追加収集の具体指示）を診断し、low時は警告表示（続行可・run_log記録）。「入力がザルならザルと指摘し、何をどこから足すかを返す」設計 | Must |
| FR-32 | **ドシエ・ティア**: 案件に dossier_tier(t1_quick/t2_full/t3_sparring)を持ち、収集レシピ表示・充足度判定基準・入力上限(config)をティア連動させる。T2の収集はdocs/08のディープリサーチ用プロンプト集で支援 | Must |
| FR-33 | **壁打ちモード(PL-04)**: 案件のドシエ＋S1～S3結果＋ナレッジ（型・パターン・機構・判断基準）を初期コンテキストに、AIリボンの会話継続(prevU/prevA)で対話できる壁打ちシート。履歴は案件に保存。任意の発話を「気づき」としてワンクリックで受信箱(field_voice)へ送信できる。壁打ちの結論は人がS2/S3シートへ反映（自動反映しない） | Must |
| FR-34 | **現場メモ（自由入力）**: 書式自由の「何でも欄」。S1が要約せず原文のまま field_insights（タグつき）に切り分け、S2/S3/S4・壁打ちへ原文パススルー（docs/09 §2） | Must |
| FR-35 | **マルチラウンド（伴走型3ステップ）**: 初回ラウンド＝全体像（エグゼクティブサマリー＋リスクマップ＋営業アプローチガイド＋ヒアリングシート）。訪問後にヒアリング回答欄（input_hearing_answers）へ回答を貼り、S1またはS2から再実行すると第2ラウンド＝**種目特化の深化提案**（仮説のconfirmed/rejected/new判定＋**初回ラウンドで採用された提案の種目に絞った深掘り**。絞り込みは案件一覧の `focus_line_ids` が持ち、15章§1.2c `BLOCK_ROUND2_FOCUS` が S2/S3 の user に入る。**種目特化AIは作らない**＝髙橋FB「STEP2 個別提案」への接続は、第2ラウンドの種目絞り込みで当面足りるかを確認中〔裁定書25 髙橋さんへの確認3〕。v2.5・裁定書25 S4）へ進む。充足度診断は回答分を反映して上がる。「AIが完成品を一括出力する」のではなく「対話で提案書が成長する」が製品の中核思想（部長FB） | Must |
| FR-36 | **入念モード（quality_mode=deep）**: S2/S3を生成→批判（別呼び出しの審査員/経営者ペルソナ）→改訂の3段で実行。T2案件は既定deep。批判・改訂の生出力も保存（docs/09 §4） | Must |
| FR-37 | **HTMLリスクレポート出力（主力出力・v2.3で昇格）**: S1+S2+S3のJSONを固定HTMLテンプレート（高橋PLプロトタイプ準拠の標準型: エグゼクティブサマリー3テーマ→企業理解（事実/推定/要確認タグ）→リスクユニバース10分類→**2軸リスクマップ（影響×頻度5×5・色分け）**→保険カバレッジ表（移転可能性・ギャップ・管理策）→**ニューリスク（新種・新興リスク。サイバー・気候変動等。第2ラウンドの status=new とは別物）→訪問で分かったこと（ラウンド更新。第2ラウンド以降のみ表示）**→提案ストーリー→ヒアリング質問→出典・免責）に流し込み、**自己完結HTML1ファイル**を固定VBAで生成（modExportHtml。LLM追加呼び出しなし・データはJS配列としてテンプレートに埋め込む）。デザインは差し替え可能なテーマとして分離。**紙面設計（大谷FB）: 冒頭のエグゼクティブサマリーのみA4 1枚相当の文字中心、以降は文字少なめ・グラフィカル**。**「当社だからできること／できないこと」を明示するセクションを置く**: できること=提案ストーリー＋未然防止メニュー、できないこと=transferability=hard・unmatched_risks・do_not_propose（理由と代替の打ち手つき）——できないことを正直に書くことが提案の信頼を作る | Must |
| FR-38 | **S4出力バリアント**: proposal（保険提案書・既定）/ alliance（協業提案書: 課題共有→座組図→三方のメリット→実証ステップ）。S3のscheme比率から推奨を表示し人が選択 | Must |
| FR-39 | 未然防止策: S2の各リスクに preventions（対策＋対応する実在メニューID）を必須出力。「検知・予防・行動変容→残余を保険」の会社思想を出力構造に埋め込む | Must |
| FR-40 | **引受目線（do_not_propose）**: S3が保険会社目線で「提案を控えるべき種目・リスク」を理由つきで出力し、レポートに明記（例: 会計不祥事係争中の先のD&O）。網羅性のため分析には残し、提案からは外す使い分け（高橋FB⑤） | Must |
| FR-41 | **追加リサーチプロンプト生成（research_requests）**: S1が充足度不足の観点ごとに、ディープリサーチ社内アプリへそのまま貼れる調査プロンプト文面（企業名・拠点等を埋め込んだ具体文）を生成。案件入力シートにコピー操作つきで表示（高橋FB③） | Must |
| FR-42 | **拠点ハザード連携**: 拠点住所×公開ハザード情報（浸水深・土砂・地震・津波）を第14観点（hazard）として収集・S1のlocations（address/hazard_note）に構造化し、S2の自然災害リスクの根拠に使う。収集はdocs/08 D-7（ディープリサーチ）＋重ねるハザードマップの手動確認（VBAからの外部API接続はしない。16章 OQ-5） | Must |
| FR-43 | **財務インパクト表現**: 入力に財務データ（純資産・売上）がある場合、S2が損害規模と財務体力の対比（loss_scale_note）を概算と明記のうえ出力し、S3のpitchに織り込む。数字の創作は検証で排除。保険料試算は引き続きスコープ外（「要見積」表示）（高橋FB②） | Must |
| FR-44 | **VIEW情報・決算公告の投入**: 決算公告（公開情報）は財務欄へ投入可。社内VIEW情報のAI投入可否は16章 OQ-10の確認完了まで人手要約転記のみ。**投入先は貼付欄の7本目「決算・財務」（13章§2.11(a) `input_finance`）であり、S1 が `financials`〔fiscal_year / net_assets / sales / operating_profit / source / note〕へ構造化する（15章§2。`source` の enum に `kessan_kokoku` と `view` を持つ）。読み取れない項目は「不明」とし推測で埋めない**（v2.5・裁定書25 S3） | Should |
| FR-45 | **企業ドシエファイル（1社1ファイル・v2.3.1）**: ツール本体（xlsm・共通配布）と企業データ（マクロ無し.xlsx・1社1ファイル）を分離。案件のラウンド履歴・S1～S3のJSON・事実記録・現場メモを企業ファイルに蓄積し、ツールから開いて続きから作業できる（担当15社=15ファイル。面談のたびに追記して育てる）。書き出し/取込はPoCのmodPack/modPackExport骨格を転用（modCompanyFile）、共有前にPII走査（modPii）。13章§2.8（常務・大谷FB: 成長型・自己改善型の器） | Must |
| FR-46 | **リテール（未上場）対応**: 有報が無い先でも、帝国DB要約＋決算公告＋HP＋D-7ハザード＋ヒアリングで同じパイプラインが回る（成英工業デモで実証済みの経路を正式仕様化）。S1は有報前提の観点（finance_risk）を決算公告・帝国DB・信用調査系公開記事で代替評価し、未上場では「取れない情報」を責めずヒアリングシートへ回す。リテールこそマルチラウンド（FR-35）が主戦場——初回は薄い入力で全体像、対話で育てる | Must |
| FR-47 | **PDF/Word直接取込（Phase 1.5）**: 有報PDF・決算公告PDF・会社案内パンフをテキスト化して貼付欄へ自動投入。PoCのmodExtractor系（PDF3経路フォールバック・Word相乗り）を移植（12章対応表）。**Phase 1から「人がPDFを開いて該当章をコピペ→貼付欄」を正式ルートとする**（S1が構造化するので体裁不要。コスト制約なし前提=14章。調査AIアプリは有報深部を読めず創作するため使わない=docs/08 1z実測）。FR-47はこのコピペ作業の省力化として位置づける | Phase1.5 |

### 明示的スコープ外（Phase 1）
Web自動取得／顧客の非公開情報の投入（※現契約サマリの扱いは16章 OQ-6 が確定するまで匿名化運用）／保険料試算・引受判断の自動化／PDF・Word自動抽出（**Phase 1.5でFR-47として実施**。PoC実証済みモジュールの移植）／ベクトル検索（**理由**: Embeddings自体はAIリボンの `GetEmbeddings` で実装可能〔社内IT・AI環境v1.1 §4.2・§4.5〕であり「埋め込みAPIが無い」ためではない。採らない理由は工数・32bit Office のメモリ・計算時間制約・本製品のナレッジ規模の小ささであり、判断そのものは据え置く）／PL-05～08のツール化／**Power Automate Desktop（PAD）による自動化**（DR貼付の自動化を含む。理由: 所定の配備端末は基本的にシンクライアント端末であり環境終了ごとに再インストールが必要で利用者に日常的に使わせる設計は現実的でない／DR は画面変更に弱い。社内IT・AI環境v1.1 §3.4・§8 #17）。**将来PADを取り込む場合の条件**（v1.1 #17・#20の逐語）: 「PAD なしでも手作業だけで完結できる手順を必ず残す」「フロー内に顧客情報・機密情報を直接記述しない」「配布はエクスポートしたフローを版番号付きで xlsm と同じ場所に保管する」。

## 8. 非機能要件（NFR）

| 区分 | ID | 要件 |
|---|---|---|
| 性能 | NFR-P1 | **一括実行（S1～S4＋HTMLリスクレポート）目標5分以内**（standardモード・T1想定。入念モードは1案件8～12呼び出しのため対象外）。リボン待機上限は `llm_wait_sec`(既定1200秒)/回 |
| 性能 | NFR-P2 | **リボン呼出中はVBAが制御を返さず画面更新できないため、「呼出の前に確定表示する」方式を採る**（PoC SetStage方式）: 呼出の直前にStep名・開始時刻・最大待ち時間・「画面が白くなっても処理は続いている」旨を書き切り、呼出中の無表示・画面ゴースト化を利用者が異常と誤認しないようにする。一括実行はStep間で必ずDoEventsを挟み、`ScreenUpdating=False` は必ずエラーハンドラで復帰させる（16章 E-50） |
| 性能 | NFR-P3 | 入力合計上限は**ティア連動**: `dossier_tier=t1_quick` は `max_context_chars`(既定40,000字)、`t2_full`/`t3_sparring` は `t2_max_context_chars`(既定100,000字)。超過は優先度の低い入力から打切り＋警告（打切り順と打切らない4欄は16章 E-03） |
| 信頼性 | NFR-R1 | LLM応答のJSON不正は修復リトライ1回→失敗時は人可読エラーと状態=error（16章 E-06）。**検証不合格の生応答は `sN_json` を汚さず `sN_json_failed` に退避**し、`sN_json` には検証合格した出力のみを格納する |
| 信頼性 | NFR-R2 | 全エラーを err_log に構造化記録（コード体系と個別コードの正は16章§1各表の `code` 列。19章§4に同内容を登録）。ローテーション `log_max_rows` |
| 信頼性 | NFR-R3 | 完了済みStepの結果は失敗時も保持し、失敗Stepから再開可能。再開の情報源は案件一覧の `last_ok_step`（検証合格済みの最大Step）と `failed_step`（直近失敗Step）であり、起動時の状態修復もこの2列と `sN_json` の突合で行う（16章 E-12） |
| 保守性 | NFR-M1 | PoC規約踏襲: 1モジュール30,000字以内／R3（`Application.Run`はゲートウェイのみ）／R4（UI層以外にExcelオブジェクトトークン禁止）／外部ライブラリ禁止 |
| 保守性 | NFR-M2 | 純ロジックは modTestsPure でテスト、**本体内mockトランスポート `modMockLlm`**（transport=mock）でLLMなし結合確認。`wintest/mock_ribbon/modMockRibbon.bas`（ニセリボンちゃん=.xlamスタブ）は `Application.Run` 配管そのものの検証用で別物 |
| 保守性 | NFR-M3 | モデル名・閾値・パス・機能フラグは全て config 駆動（ハードコード禁止） |
| 保守性 | NFR-M4 | プロンプト・スキーマは15章と一字一句一致（一致検査をテストに含む） |
| UX | NFR-U1 | 利用者の操作は「選ぶ・貼る・押す」のみ。設定作業ゼロ（ribbon経路） |
| UX | NFR-U2 | エラーは非エンジニア向け日本語＋対処方法つき |
| コスト | NFR-C1 | ribbonは社内利用枠内（LimitCheck準拠）。direct試算は14章 |
| セキュリティ | NFR-S1～S7 | 16章§2に定義（データ分類・キー管理・ログマスキング・署名・監査・著作権・**外部由来テキストの3書き込み口の一元化**〈セル/CSV=SetCellSafe、HTML/JS=HtmlSafe・JsStringSafe〉） |

## 9. 成功指標（KPI）

| 対象 | 指標 | 目標（Phase 1終了時） |
|---|---|---|
| 営業価値 | 準備時間削減率（自己申告計測） | 80%削減 |
| 品質 | ベテラン採点3軸（網羅性/具体性/実売性）平均 | 3.5/5.0以上（実在5～10社） |
| 信頼性 | S3実在チェックの幻覚ブロック率 | すり抜け0件 |
| 橋のKPI | 案件由来の現場の声レコード数／受信箱のプリフライト後生存率 | 記録が回り始めること（実数で報告） |
| 橋のKPI(1.5～) | 案件ログ・ウォッチ由来で型ライブラリに昇格した件数/月 | 月1件以上 |
