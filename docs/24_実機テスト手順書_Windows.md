# 24. 実機テスト手順書（Windows・実Excel）v5.0

v5.0変更概要（裁定書23 C-2「焼き付け配布」）: **利用者の初回設定を2つから1つに減らした**。v4.0まで§2.2で必須にしていた「VBAの信頼設定」は、**開発側が新しいビルドを1回開いて閉じる**作業（焼き付け。§8.1b）へ移し、利用者側では**§6.11の予備手順として残した**（禁止されている設定ではないが、会社が勧めている設定でもないため、ふだんは触らない）。利用者は受け取った2ファイルを**`D`ドライブの同じフォルダに置いて開くだけ**でよい（`D`ドライブは組織の設定で信頼済みのため、マクロの確認も出ない）。§1にファイルの置き場所、§6.11に「焼き付け前のファイルを受け取ったときの合図と連絡先」を足し、§6.1・§6.2・§6.7 を`D`ドライブ運用に合わせた（マクロ有効化の手順は`D`ドライブ以外に置いたときの予備手順として残してある）。

v4.0変更概要（W6第1弾: ナビ画面への統合を反映）: v3.1までの画面は `HOME`／`案件入力`／`操作ガイド` の3タブに分かれていたが、v3.2でこの3タブは廃止され、**`ナビ`（帯＋4区画）＋`使い方`の2タブへ統合**された。本書は旧タブ・旧ボタン名（`HOME`／`案件入力`／[① 調べる指示文を出す]〜[⑤ ヒアリングシートを出す]の5本キャプション）の記述を全面削除し、実装（`src/ui/modUINav.bas` の配置表・`src/ui/modUINavDraw.bas`・`build/build_rpn.py` の使い方タブ）から1字単位で引用し直した。対象タスクとT-14b・§6・§8の位置づけは変えていない。

v3.1変更概要（裁定書17 裁定A「配布物の見直し」）: 配布物を**本番版 `リスク提案ナビ.xlsm` とナレッジブックの2ファイル**に絞り（開発版 `リスク提案ナビ_dev.xlsm` は**開発専用**と明記）、旧§5「ニセリボンちゃん（T-14b）」を**開発側の §8.5 へ移した**（利用者の合格判定はT-48とT-41の2つだけになる）。

v3.0変更概要（裁定書14: W5「社内環境対応+UI/UX改善」）: 会社PCは**ターミナル・PowerShell・Python・git・CLIが全面禁止**という実機事実に合わせ、本文をゼロから書き直した。

## この文書は誰のためのものか

**発注者ご本人が、ご自分の会社のWindowsパソコンと本物のExcelで「リスク提案ナビ」を動かし、合格・不合格をご自分の目で確かめるための手順書**である。プログラミングの知識は不要で、**黒い画面（ターミナル・PowerShell）は一度も使わない**。

書いてあるのは次の3つだけである。

1. **押す場所**（ボタンの名前は画面に出ているとおりに書いてある）
2. **何が出たら成功か**（画面に出る文字を1字単位で示す）
3. **うまくいかないときの対処**（§6）

分からない言葉は `使い方`タブの「⑤ 困ったとき」（本書§6の対処表と対で読む）を見ればよい。**判断に迷ったら先へ進まず、その画面のまま開発担当へ連絡する**のが正しい。

対象タスク: 17章 **T-48**（ブック内テスト実行）・**T-41**（実機2社完走）の2つ。**T-14b（ニセリボンちゃん配管検証）は開発側の作業**であり、利用者の合格判定には含めない（手順は §8.5）。

---

## 1. 受け取り方

会社PCではファイルをダウンロードしたりインストーラを実行したりできないので、**開発担当からチャットで受け取ったExcelファイルを、私物のPCからメールで会社アドレスへ送って、会社PCで保存するだけ**にする。

### 1.1 受け取るファイル（2つ）

| ファイル名 | 中身 |
|---|---|
| `リスク提案ナビ.xlsm` | 本番版。これが普段使うファイルである |
| `ナレッジブック.xlsx` | 社内の知識をまとめた台帳（マクロなし） |

**この2つ以外は受け取らなくてよい。** 開発版（`リスク提案ナビ_dev.xlsm`）は**開発専用**で、本物のAIを呼ばず決まった答えを返すため、実案件に使うと事故になる。開発側の配管検証（§8.5 ニセリボンちゃん）のときだけ開発担当が使う。

**2つは同じフォルダへ入れる。** `ナレッジブック.xlsx` を `リスク提案ナビ.xlsm` と同じフォルダに置いておけば、起動時に自動で見つかる（設定をいじる必要はない。§3.1）。

**置き場所は `D` ドライブにする。** `D` ドライブの中に新しいフォルダを1つ作り（例: `D:\リスク提案ナビ`）、**2つのファイルをそこへ一緒に入れる**。`D` ドライブは会社の設定で最初から信頼されているので、**こちらで設定することは何も無い**（マクロの確認も出ない）。

> **OneDrive（オンラインの保存場所）には置かないこと。** OneDrive のフォルダに置くと、開くたびにマクロの確認が出る（§6.7）。

### 1.2 手順

1. チャットで受け取った2つのファイルを、私物のパソコン（Mac可）へ保存する。
2. 私物のパソコンのGmailを開き、上の2つのファイルを**そのまま（zipにせず）添付**して、会社のメールアドレスへ送る。

   > **zipにしないこと。** 会社のメールは圧縮ファイル（zip）の添付をはじく設定になっている（実機で確認済み）。xlsmファイルはそのままの形で送れば届く。

3. 会社PCでそのメールを開き、2つの添付ファイルを保存する。**`D` ドライブに新しいフォルダを1つ作り（例: `D:\リスク提案ナビ`）、その中へ2つとも保存する**（同じフォルダに入れておくと、ナレッジブックが自動で見つかる。§3.1）。OneDrive のフォルダには置かない（§1.1の注意）。

これで受け取りは完了である。次の§2から先は、**会社PCの中だけ**で完結する。

---

## 2. 開く前の設定（初回だけ）

`リスク提案ナビ.xlsm` を開く**前**に、次の設定を済ませておく。**初回の1回だけ**でよい。設定はこの1つだけである。

### 2.1 ファイルのブロックを外す

インターネットやメール経由で受け取ったファイルには、Windowsが「よそから来たファイル」という印を付けており、そのままでは中の機能が一部制限される。

1. 保存した `リスク提案ナビ.xlsm` を**右クリック** → [プロパティ]。
2. 「全般」タブのいちばん下に **[許可する]** というチェックボックスがあるので、チェックを入れる。
3. [OK] を押す。

**この手順は `リスク提案ナビ.xlsm` と `ナレッジブック.xlsx` の2ファイルとも**行う。本体だけ解除して `ナレッジブック.xlsx` を解除し忘れると、同じフォルダに置いてあってもナレッジを読めず、`ナビ`のナレッジ欄に「ナレッジブック.xlsx を本体と同じフォルダに置いて…」が出続ける。

---

## 3. 開く

1. §1で保存した `リスク提案ナビ.xlsm` をダブルクリックで開く。
2. 画面の上に黄色い帯で「セキュリティの警告」が出たら、[**コンテンツの有効化**] を押す。
3. 少し待つと、はじめてこのブックを開いた人向けの案内カードが画面の真ん中に出る（3枚）。カードの右下の[次へ]で読み進め、最後は[はじめる]で閉じる。あとから `使い方`タブの[ツアーをもう一度見る]で何度でも見返せる。
4. カードを閉じると **`ナビ`** の画面が出る。**最上段3行（コーチ帯）に `STEP 1/6` と進捗ドット、いま何をするかの1文が出ていれば成功**である。

**成功の合図**: `ナビ` が表示され、最上段のコーチ帯に **`STEP 1/6`** と **`●○○○○○`**（進捗ドット）、そして「会社名と本社の場所を入れてください。入れると、社内のディープリサーチに貼る文がすぐ下に出ます。」の1文が出ている。コーチ帯の下に①〜④の4区画（角丸のパネル）が縦に並んでいる。

### 3.1 ナレッジブックの場所（ふつうは何もしなくてよい）

**`ナレッジブック.xlsx` を `リスク提案ナビ.xlsm` と同じフォルダに置いてあれば、起動したときに自動で見つかる**。設定をいじる必要はない。

**成功の合図**: `ナビ` のナレッジ欄に「同じフォルダのナレッジブックを読み込みました。読込OK（メニューN/型N/事例N）」と出る。

ナレッジ欄に「**ナレッジブック.xlsx を本体と同じフォルダに置いて[ナレッジを読み直す]を押してください（探した場所: <kb_pathの現在値>）。置いてあるのに読めないときは、そのファイルを右クリック→プロパティ→[許可する]にチェックを入れて開き直してください。**」と出たときは、まず2つのファイルが別のフォルダにある可能性を疑う。同じフォルダへ移してから、コーチ帯の[**ナレッジを読み直す**]を押す。**同じフォルダに置いてあるのに出るとき**は、メールで受け取った `ナレッジブック.xlsx` がブロックされている（§2.1 のブロック解除がされていない）。文中の「探した場所」に出るパスが、実際に置いた場所と同じかどうかもあわせて確かめる。

**共有フォルダに置く運用のときだけ**（管理者向け・ふつうは不要）:

1. `ナビ` の画面のまま、シート見出し（画面いちばん下のタブの並び）を**右クリック**し、[**再表示**] を選ぶ。
2. 一覧から **`config`** を選んで [OK]。`config` というシートが開く。
3. `name` 列に **`kb_path`** とある行を探し、その右の `value` 列のセルへ、`ナレッジブック.xlsx` を保存したフォルダのフルパスとファイル名を貼り付ける。
   - パスの調べ方: エクスプローラで `ナレッジブック.xlsx` を右クリック → [プロパティ] → 「場所」の値の末尾に `\ナレッジブック.xlsx` を足したものが、貼り付ける値である。
4. `config` シートのタブを**右クリック** → [**非表示**] に戻す。
5. `ナビ` へ戻り、コーチ帯の[ナレッジを読み直す]を押す。ナレッジ欄に「読込OK（メニューN/型N/事例N）」と出れば成功である。

---

## 4. 自動テスト（T-48・所要は数分）

会社PCではターミナルが使えないため、開発側の検問と**同じ内容の検査**を、ブックの中の1ボタンで回せるようにしてある。**新しい版を受け取ったとき**と、**動きがおかしいと感じたとき**に押す。

### 4.1 やること

1. シート見出しの並びの中から緑色の「**使い方**」タブを開く。
2. 章「**⑥ 自己テスト(このブックが正しく動くかを自分で確かめる)**」まで読み進める（目次のリンクから飛べる）。
3. [**テストを実行**] ボタンを押す。
4. 次の確認が出る。

   > **テストを実行します。数分かかり、実行中は操作できません。記録シート（err_log）に検査の行が残ります。よろしいですか。**

   [はい] を押す。（[いいえ]なら何も起きず中止する。）
5. **数分間、Excelを触らず待つ**。終わると合否がポップアップで1行出て、下の「テストの結果」欄にも同じ内容と詳細が入る。

### 4.2 合格の読み方

ポップアップとテスト結果欄の1行目に、次の形の文字が出る。

```
全PASS（FAIL 0 / SKIP 0 / 純層 N = 期待 N / 層(b) M 本）
```

（`N` と `M` は実行のたびの実際の数字が入る。）

**合格はこの4つが同時に成立したときだけ**である（開発側のPowerShell検問と同一の判定基準）。

| 見るところ | 合格の値 | 意味 |
|---|---|---|
| FAIL | **0** | 失敗した検査が1つも無い |
| SKIP | **0** | 飛ばした検査が1つも無い |
| 純層 = 期待 | **N = N** | 中身の計算の検査が、決められた本数とぴったり一致する |
| 層(b) | **1本以上** | 本物のExcelでしか確かめられない検査が実際に走っている |

### 4.3 不合格のとき

次の形で出る。

```
NG（FAIL 3 / SKIP 0 / 純層 N = 期待 N / 層(b) M 本）
```

**自分で直そうとしない。** 次を行う。

1. テスト結果欄（`使い方`タブの下のほう。実行するたびに前回分は上書きされる）の**写真を撮る**か、そのままコピーする。
2. 開発担当へ送る。

期待本数がそもそも読み込めなかったときは「**期待本数が読めませんでした。開発担当へご連絡ください。**」と出る。これも自分で直さず開発担当へ連絡する。

---

## 5. T-41: 本物のAIで実在2社を通す（会社のパソコンで行う）

**この章は会社のパソコン（本物のリボンちゃんが入っているPC）で行う。**

### 5.1 準備

1. §3で開いた `リスク提案ナビ.xlsm` を使う。§3.1でナレッジブックの場所を教え済みであることを確かめる。
2. `ナビ` のナレッジ欄が「読込OK（メニューN/型N/事例N）」になっていることを確かめる。
3. `ナビ` の④区画の下に**赤い帯が出ていないこと**を確かめる。赤帯（「デモ／開発経路で動作中です」）が出ている間は本物のAIを使っていない。出ているときは開発担当へ連絡する。

### 5.2 画面の見取り図（帯＋4区画）

`ナビ` は上から次の順で並ぶ。

| 場所 | 中身 |
|---|---|
| 帯（最上段8行・固定表示） | `STEP n/6` ／進捗ドット／案件のプルダウン／対象の案件（表示）／いま何をするかの1文／[← 戻る][次へ →][ナレッジを読み直す][使い方を開く]（8行目まで固定され、下へスクロールしても消えない） |
| 区画① 会社のこと | 種別・会社名・業種名・本社の場所の入力欄と、社内ディープリサーチに貼る文8本（標準3本＋[＋ もっと調べる（あと5本）]で開く補助5本）と各文の[コピー] |
| 区画② 貼る | 6つの貼付欄（見出しは `── ① 調べた結果 ──`〜`── ⑥ ヒアリング回答 ──` の丸数字つき。`調べた結果`／`会社のホームページ`／`有価証券報告書のリスクの章`／`現場メモ`／`いまの契約`／`ヒアリング回答`）と各欄の[ここに貼る][中身を見る][消す]、そして[貼ったものを保存する]。各欄はプレビューの上に `┈┈┈ 先頭だけお見せします ┈┈┈` の1行を持つ |
| 区画③ 作る | [まとめて作る] |
| 区画④ 出す | [レポートを出す][ヒアリングシートを出す][下書きを見る] |

### 5.3 1社目: 新規案件（STEP 1〜6を順に進める）

新規案件は、帯の案件プルダウンで**何も選ばない（空のまま）**ことで始まる。空のままだと案件IDの欄に自動で **`(新規)`** と出る（採番は5-4の[貼ったものを保存する]のときに行われる）。

| # | STEP | 操作 | 成功の合図 |
|---|---|---|---|
| 5-1 | STEP 1/6 | 区画①に種別=**新規開拓**、企業名・業種・本社の場所を入れる | コーチ帯が「1本目の[コピー]を押して…」（STEP 2）へ進む。区画①の下に調べる文8本のうち標準3本が現れる |
| 5-2 | STEP 2/6 | 標準3本の[コピー]を**1本ずつ**押し、社内のディープリサーチへ貼って投げる（返ってくるまで各10分ほど。同時に2本は投げない） | 押すたびに「N本目（〇〇）を写しました。…」のトーストが出て、次に押す枠の太枠がずれる |
| 5-3 | STEP 3/6 | 返ってきた3本を、区画②の`調べた結果`へ[ここに貼る]で貼る。会社のホームページ等、分かる範囲の他の欄も貼る | [ここに貼る]を押すたびに「〈欄名〉に N字 保存しました。次の欄へ進んでください。」のトーストと、枠に先頭5行のプレビューが出る。状態行は「貼り付け済み　9,812字　（14:03 に貼りました）」の形で、**画面を描き直しても時刻は貼ったときのまま**（字数が変わったときだけ新しい時刻になる） |
| 5-4 | STEP 3/6 | 区画②の[**貼ったものを保存する**]を押す | 「案件 <案件ID> を保存しました。次は③の[まとめて作る]を押してください。」のトーストが出て、案件IDが採番される |
| 5-5 | STEP 4/6 | 区画③の[**まとめて作る**]を押す | 進捗の案内欄が更新され、ステータスバーに実行中の1行が出る |
| 5-6 | — | 待つ（**画面が白くなっても触らない**） | 進捗の案内が「① 会社の理解を作っています」→「② リスクの洗い出しを作っています」→「③ 提案の候補を作っています」→「④ 提案の骨子を作っています」と**段ごとに切り替わる**（1本のまま止まらない）。4つのシート（S1・S2・S3・S4）に中身が描かれる。あわせて画面の右上に白いカード（トースト）で「**できました。④の[レポートを出す]を押してください**」と出て、文の長さに応じて3〜9秒ほどで自分で消える |
| 5-7 | STEP 6/6 | 区画④の[**レポートを出す**]を押す | 「お知らせ」欄に「リスクレポートを出力しました: <パス>」が出る。画面の右上に白いカード（トースト）で「**レポートを出しました。次は④の[ヒアリングシートを出す]です**」と出る |
| 5-8 | — | 出たパスのHTMLファイルをダブルクリックしてブラウザで開く | **5×5のリスクマップ**と**カバレッジ表**が絵として描かれる。文字化けが無い |
| 5-9 | — | 区画④の[**ヒアリングシートを出す**]を押す | `ヒアリングシート` シートに切り替わり、質問が並ぶ。回答欄は空。画面の右上に白いカード（トースト）で「**ヒアリングシートができました。Ctrl+Pで印刷できます**」と出る |
| 5-10 | — | `ヒアリングシート` で Ctrl+P（印刷プレビュー） | A4縦1枚に収まっている |

### 5.4 2社目: 更新案件

1社目と同じ手順を、**種別=更新**の実在1社で行う。追加で次を見る。

| # | 見るところ | 成功の合図 |
|---|---|---|
| 5-11 | 区画②の`いまの契約` | 更新案件（種別=更新）を選ぶと欄が現れる（新規開拓では非表示）。分かる範囲で[ここに貼る]で貼る |
| 5-12 | `S1_企業プロファイル` の現契約ブロック | 現契約が行として構造化されている |
| 5-13 | `S2_リスク仮説` の付保ギャップ | 「入っていない」「金額不足」「重複」の観点で行が出る |
| 5-14 | HTMLレポートのカバレッジ表 | 付保ギャップが載っている |

### 5.5 最後に: 検証結果の集計

画面いちばん下のタブから **`run_log`** シートを開き（最初から見えている）、`validate_result` 列を数える。

| 値 | 意味 | 目安 |
|---|---|---|
| `ok` | 1回目の答えがそのまま合格した | 多いほどよい |
| `repaired` | 1回作り直させて合格した | 数件なら正常 |
| `failed` | 作り直しても合格しなかった | **0が望ましい**。あれば開発担当へ報告 |

**数え方**（ブックに数式を書き足さない方法）:

1. `run_log` シートの見出し行を選び、[データ] タブの [フィルター] を押す。
2. `validate_result` 列の矢印を押し、`ok` だけにチェックを入れる。
3. 画面右下のステータスバーに「N レコード中 M 個が見つかりました」と出る。その M が件数である。
4. 同じ手順で `repaired` と `failed` も数える。
5. 数え終わったら**フィルターを解除**する（`run_log` は見えたままでよい）。

**報告する内容**: 2社ぶんの `ok` / `repaired` / `failed` の件数と、`err_log` シートに増えた行（`err_code` と `logged_at` だけでよい。本文は記録されない）。

---

## 6. うまくいかないときの対処

### 6.1 ブックを開いたら「はじめにお読みください」だけが見えて、他の画面が無い

**原因**: マクロが有効化されていない。または、開発側の準備（§8.1b）が済んでいないファイルを受け取っている。

**対処**:

1. 画面の上のほうに黄色い帯で「セキュリティの警告」が出ていないか見る。出ていれば [**コンテンツの有効化**] を押す。ファイルを `D` ドライブのフォルダ（§1.1）に置いていれば、この帯はふつう出ない。
2. **英語で `Trust the VBA project, then reopen.` と出るときは §6.11** を見る（受け取ったファイルの準備がまだ済んでいない合図である）。

### 6.2 赤い帯で「信頼できないソース」と出て開けない、またはマクロが最初から無効

**原因**: §2.1の「ブロックを外す」設定がまだ済んでいない。または、ファイルを `D` ドライブ以外（OneDrive など）に置いている。

**対処**:

1. §2.1をやり直す。ファイルを右クリック → [プロパティ] → [許可する] にチェック。
2. それでも出るときは、2つのファイルを `D` ドライブのフォルダ（例: `D:\リスク提案ナビ`）へ移してから開き直す。**`D` ドライブに置くのが正しい置き方**である（§1.1）。
3. どうしても `D` ドライブ以外を使わざるを得ないときの予備手順は §6.7 にある。ただし**会社はマクロを有効にする設定を勧めていない**ので、まず `D` ドライブへ移すことを試す。

### 6.3 圧縮フォルダ（zip）の中から開こうとして開けない

**原因**: メールに直接添付されたxlsmファイルではなく、zipの中から直接開こうとしている。

**対処**: **zipのまま直接開かない。** 一度zipの外（デスクトップなど）へ取り出してから開く。そもそも本製品の配布経路ではzipを使わない（§1参照）。zip添付を受け取った場合は開発担当へ確認する。

### 6.4 `ナビ` に「ナレッジブックが見つかりません。管理者にご連絡ください。」と出る

**原因**: 本体ブックと同じフォルダにも、`config` の `kb_path` に書いたパスの場所にも `ナレッジブック.xlsx` が無い。

**対処**: `ナレッジブック.xlsx` を `リスク提案ナビ.xlsm` と**同じフォルダ**へ置き、Excelをいったん閉じて開き直す（起動時に自動で見つかる）。開いたまま直したいときは帯の[ナレッジを読み直す]を押す。共有フォルダに置く運用のときだけ §3.1 の後半で `kb_path` を書き直す。

### 6.5 「お知らせ」欄に「本日のAI利用枠の上限です」と出た

**原因**: 社内AIリボンの1日の利用上限に達した。

**対処**:

- **起動も画面操作も止まらない**。翌日以降にやり直す。
- 途中まで通ったStepの結果は残っている。`案件一覧` の `status` を見て、続きのStepから押し直せばよい。

### 6.6 レポートを作り直したら前のファイルが消えていないか心配になった

**消えない**。同じ日に同じ案件でもう一度出力すると `_2` `_3` と連番の別ファイルが作られる。前回のHTMLはそのまま残る。**企業ファイル**（`_企業カルテ.xlsx`）だけは1社1ファイルで**同じファイルへ追記**するのが正しい動作であり、連番は作られない。

### 6.7 開くたびに毎回マクロの確認が出るのが煩わしい

**原因**: ファイルを `D` ドライブ以外（OneDrive など）に置いている。`D` ドライブのフォルダは会社の設定で最初から信頼されているので、そこに置いてあれば確認は出ない。

**対処**: まず2つのファイルを `D` ドライブのフォルダ（例: `D:\リスク提案ナビ`）へ移して開き直す（§1.1）。**これが正しい置き方**である。

**予備手順（`D` ドライブ以外を使わざるを得ないときだけ）**: ブックを置くフォルダを1つ決めて「信頼できる場所」に登録する。**会社はこの設定を勧めていない**ので、`D` ドライブへ移せるならそちらを選ぶ。

1. [ファイル] → [オプション] → [トラストセンター] → [トラストセンターの設定]。
2. 左の [**信頼できる場所**] を選び、[新しい場所の追加] を押す。
3. [参照] でブックを置いているフォルダを選ぶ。
4. 「この場所のサブフォルダーも信頼する」にチェックを入れて [OK]。

> **注意**: 信頼できる場所に登録したフォルダのマクロは確認なしで動く。**そこには自分が中身を知っているファイルだけを置く**。

### 6.8 ほかのExcelファイルを開いていると動きがおかしい

**対処**: 本製品を使うときは**ほかのExcelファイルを全て閉じてから**、`リスク提案ナビ.xlsm` だけを開く。

### 6.9 赤帯（デモ／開発経路で動作中です）が消えない

**原因**: 練習用ファイル（`リスク提案ナビ_dev.xlsm`）を開いている。

**対処**: `_dev` が付かない `リスク提案ナビ.xlsm` を開く。それでも消えないときは開発担当へ連絡する。

### 6.10 §4の自動テストが不合格だった

**対処**: §4.3のとおり、テスト結果欄の写真かコピーを開発担当へ送る。**自分で直そうとしない。**

### 6.11 英語で `Trust the VBA project, then reopen.` と出る

**原因**: 開発側の準備（§8.1b）が済んでいないファイルを受け取っている。ふつう配られるファイルは準備済みなので、このメッセージは出ない。

**対処**: **まず開発担当へ連絡する**（「`Trust the VBA project, then reopen.` と出た」とそのまま伝えればよい）。準備済みのファイルを送り直してもらうのが正しい直し方である。

**予備手順（どうしても今すぐ動かしたいときだけ）**: このブックは、開いたときに自分で画面を組み立てる作りになっている。準備が済んでいないファイルでそれを行うには、次の設定が要る。**会社はこの設定を必須にしていないので、ふだんは触らなくてよい。**

1. Excelを開く（空のブックでよい）。
2. [ファイル] → [オプション] → [トラストセンター] → [トラストセンターの設定] → [マクロの設定] と進む。
3. **「VBAプロジェクト オブジェクト モデルへのアクセスを信頼する」**にチェックを入れて [OK] を押す。
4. Excelをいったん全部閉じてから、`リスク提案ナビ.xlsm` を開き直す。

---

## 7. 実機観察メモ（開発側が知りたいこと）

会社PCで実際に動かしたときにだけ見える挙動は、以下をメモして開発担当へ伝えると助かる。A〜Hは§5.3/5.4の手順で見るべき着眼点、それ以降は環境依存の挙動である。

| 観察項目 | 見るところ |
|---|---|
| A. コーチ帯の位置 | 最上段3行（`FreezePanes` で固定）がスクロールしても本当に消えないか。画面のズーム倍率（表示タブの拡大率）を変えたときにずれないか |
| B. 事前描画カードが見えるか | ③の[まとめて作る]を押した直後、進捗の案内欄（4行）が呼出の**前に**書き切られて見えるか。呼出中に画面が白くなっても、直前に書いた文字が読めるか |
| C. ステータスバー | LLM呼出中、ウィンドウがゴースト化しても画面いちばん下のステータスバーの文字が読めるか |
| D. [ここに貼る]がEDRで動くか | 会社のセキュリティソフト（EDR）の環境で、クリップボードの遅延バインド取り出しがブロックされないか。ブロックされたときは「貼り付けられませんでした。もう一度コピーしてから、[ここに貼る]を押してください。」の案内どおりCtrl+V直貼りへ切り替えられるか |
| E. [中身を見る]のメモ帳 | 区画②の各欄の[中身を見る]を押したとき、メモ帳が実際に開いて読めるか。メモ帳が無い/開けない環境での失敗の出方 |
| F. プレビュー5行の見え方 | [ここに貼る]のあと、枠に出る先頭5行のプレビューが読みやすいか（行が長い表の崩れなど） |
| G. 例文の消え方 | 現場メモの枠に何か書いた直後、4見出しぶんの例文がまとめて消えるか（実装は枠単位。11章§3.3.3(b)）。「この見出しだけ書いたら、この見出しの例文だけ消える」という期待とずれていないか |
| H. レポートが見本と同じ構成か | 出力したHTMLレポートが `docs/design/出力見本_春華堂統合提案_v0.1.html` と同じ節構成（SEC-01〜SEC-17）で描かれているか。特にSEC-17「攻めの保険活用」が出ているか |
| ナレッジブックの読込待ち | ネットワーク越し（社内共有フォルダ）にナレッジブックを置いた場合、読込に体感でどれくらい待つか（起動時・[ナレッジを読み直す]時の両方） |
| 初回ツアーカードの位置 | 画面のズーム倍率を変えたとき、案内カードが画面の中央からずれて見えないか |
| 自動テストの後始末 | §4の自動テストを初めて実行した直後、シートやボタンの見た目・動作に違和感が残らないか |
| 業種欄などの一覧の反応 | 業種欄のような一覧から選ぶ入力欄が、起動直後にどれくらいの時間で選べるようになるか |
| トーストの後始末 | ボタンを押してトースト（右上の白いカード）が出た直後にブックを閉じ、Excelが本ブックを勝手に開き直さないか |
| トーストが見えるか | ③[まとめて作る]の完了後に右上へカードが出て、6秒ほどで自分で消えるか |
| 幅の狭い画面 | 1366×768相当のノートPC・拡大率100%で、区画①〜④とその中のボタンが横スクロールなしに見えるか |
| ナレッジブックの自動発見 | 同じフォルダに置いて開くだけで「同じフォルダのナレッジブックを読み込みました」が出るか（静的な検問が無く、実機が唯一の検証手段である） |

---

## 8. 開発側だけが使う手順（利用者は読まなくてよい）

**この章より下は、この文書の読者が実行する必要は無い。** 開発担当がLinux環境でブックを組み立て、出荷前の検問を回すときに使う手順を要約として残してある（詳細は姉妹文書・開発リポジトリを参照）。

### 8.1 ブックの組み立て（ビルド）

Python・git を使う開発環境で、次の順にリポジトリを取得しブックを組み立てる。

1. `git clone` でリポジトリを取得する。
2. `python -m pip install openpyxl olefile` で部品を入れる。
3. `python build/build_rpn.py --dev` / `--prod` / `--kb` で3つのファイル（開発版・本番版・ナレッジブック）を組み立てる（`dist` フォルダにできる）。
4. 組み立てたファイルは**そのままでは配れない**。§8.1b の焼き付けを1回行ってから配る。

### 8.1b 焼き付け（新しいビルドを受け取るたびに1回・**Windowsで行う**）

**配る前に、開発側が会社PCで1回だけ開いて閉じる。** これを「焼き付け」と呼ぶ。焼き付けの済んでいないファイルを配ると、利用者側に英語で `Trust the VBA project, then reopen.` が出る（§6.11）。

**なぜ要るか**: 組み立てたばかりのブックは、部品（VBAモジュール）を`vba_src`シートの中に文字として持っているだけで、ブック本体にはまだ入っていない。**初回に開いた1回**でその部品がブックへ書き込まれ（`vba_src!E3` に `baked` が立つ）、以後の起動は部品を入れ直さずそのまま動く。だから利用者側では§6.11の設定が要らない（12章§2・裁定書23 C-1）。

**手順**:

1. **開発リーダーのPC（Windows）**で行う。事前に §6.11 の予備手順の設定（「VBAプロジェクト オブジェクト モデルへのアクセスを信頼する」）を1回だけ済ませておく。**この設定が要るのは焼き付けを行うこのPCだけ**であり、利用者には求めない。
2. `dist` にできた `リスク提案ナビ.xlsm`（配る本番版）を**ダブルクリックで開く**。
3. `ナビ` の画面が出るまで待つ（§3の成功の合図と同じ）。
4. **そのまま閉じる**。保存を聞かれることは無い（焼き付けの際にブック自身が保存する）。
5. 閉じたあとのファイルが**配布用**である。これを利用者へ渡す（§1）。

> **必ずWindowsで焼き付ける。** Macで開いて焼くと、部品を書き込むときに文字コードが混ざる（裁定書23 C-2）。
>
> **新しいビルドを受け取るたびに1回**必要である（焼き付けはファイル1つ1つに対して1回）。開発版 `リスク提案ナビ_dev.xlsm` を配るとき（§8.5）も同じ。
>
> **Macは製品としては非対応**である（クリップボード・メモ帳・ADODB・`Declare` がWindows前提。17章 Z-20）。Windowsで焼いたファイルをMacで開くと、画面の確認くらいはできる、という程度に留まる。

### 8.2 開発側の自動検問（`python3 tools/gate.py`）

出荷前に次の16ゲートを一括で回す。1つでも赤ければ配らない。

| コマンド | 何を見るか |
|---|---|
| `python3 tools/vba_lint.py` | 書き方の規約・CP932で表せない文字の混入 |
| `python3 tools/run_lo_tests.py` | 全部品が文法として通るか・純ロジックが動くか |
| `python3 tools/prompt_diff.py --strict` | 15章のプロンプト本文と実装が1字も違わないか |
| `python3 tools/validate_check.py` | 15章§11の検証ルールと実装・テストの件数一致 |
| `python3 tools/sheet_check.py` | 13章のシート定義と実物のブックの一致 |
| `python3 tools/ship_check.py` | 出荷前検問①②③（外部テキストの直書き・鍵の混入・成果物の除外） |
| `python3 tools/render_report.py`（`--faithful` も） | HTMLレポートの固定文とエスケープの逐語照合 |
| `python3 tools/enum_check.py` | 19章§3のenum表と実装の変換表の一致 |
| `python3 tools/caption_check.py` | ナビのボタン名（キャプション）が実装・使い方タブの早見表・13章§2.10の配置表・初回ツアーの文言で1字も食い違わないか |
| `python3 tools/t48_check.py` | 本文書§4の[テストを実行]の合否判定が**4条件（FAIL0／SKIP0／純層=期待／層(b)≧1）**のままか（17章T-48の規定文とも突合） |
| `python3 tools/gate.py` | **上のすべてをまとめて回す標準手段**（全16ゲート一括） |

### 8.3 Windows実機での自動テスト（開発側のPowerShell実行）

開発オーナーが自分のWindows PC上で `wintest\run_excel_tests.ps1` を実行し、`modTestsPure` と `modTestsExcel`（層(b)）を自動で回す。本文書§4の[テストを実行]ボタンは、**この検問とまったく同じ4条件（FAIL0/SKIP0/純層=期待/層(b)≧1）を、利用者がターミナル無しでブックの中から回せるようにしたもの**である。両者は互いの代わりにならず、どちらも出荷前に緑が要る。詳しくは `wintest/README.md` を参照する。

### 8.4 用語の言い換え（開発側の言葉）

| 出てくる言葉 | 意味 |
|---|---|
| リポジトリ | 設計書とプログラムの部品が入った一式のフォルダ |
| クローン（clone） | そのフォルダ一式を自分のPCへ丸ごと持ってくること |
| ビルド | 部品からExcelブックを組み立てること |
| ゲート / 検問 | 出荷してよいかを機械的に確かめる自動チェック |
| PowerShell | Windowsに最初から入っている「文字で命令する画面」（開発側専用。利用者は使わない） |

### 8.5 ニセリボンちゃん（T-14b・**開発側**が個人のWindowsパソコンで行う）

**これは開発側の検証であり、利用者は行わなくてよい**。会社PCではなく、個人のWindowsパソコンで行う。ターミナルは使わないが、VBE（VBAの編集画面）を手で操作する必要があり、社内の情報を扱わない検証用の作業だからである。**練習用の `リスク提案ナビ_dev.xlsm` を使うのもこの検証のときだけ**である。

#### 8.5.1 これは何を確かめるのか

本製品は、社内のAI「リボンちゃん」へ**12個の情報を決まった順番で**渡して答えをもらう。会社のPCでないとリボンちゃんは動かないので、**同じ形をした偽物（ニセリボンちゃん）**を個人PCに入れ、「渡す形そのもの」が正しいかを確かめる。AIの答えの良し悪しではなく、**渡し方**を確かめる検証である。

> **注意**: ニセリボンちゃんは**会社のパソコンに入れてはならない**。本物と名前が一部一致するため、取り違える原因になる。

#### 8.5.2 ニセリボンちゃんを作る（1回だけ・5分・VBE手動インポート＝ターミナル不要）

1. 個人PCでExcelを起動し、**空のブック**を開く。
2. `Alt` を押しながら `F11` を押す（VBEという編集画面が開く）。
3. 上のメニューから **ファイル → ファイルのインポート** を選ぶ。
4. 開発リポジトリの次のファイルを選ぶ（開発担当からチャットで個別に受け取る）。

   ```
   wintest\mock_ribbon\modMockRibbon.bas
   ```

5. `Alt` + `F11` でExcelの画面へ戻り、`F12`（名前を付けて保存）を押す。
6. **ファイルの種類**を「**Excel アドイン (\*.xlam)**」に変える。
7. **ファイル名**を次のとおりにする（「**リボンちゃん**」の6文字を必ず含める）。

   ```
   リボンちゃん(検証用).xlam
   ```

8. 保存先は既定のまま（`%APPDATA%\Microsoft\AddIns`）でよい。[保存] を押す。

#### 8.5.3 アドインを有効化する

1. Excelで [ファイル] → [オプション] → [アドイン] → 下の「管理: Excel アドイン」で [設定] を押す。
2. 一覧に出た **`リボンちゃん(検証用)`** にチェックを入れて [OK]。

#### 8.5.4 本番版を開いて確かめる

1. 個人PCへ持ってきた `リスク提案ナビ.xlsm`（本番版）を開く（§2の設定と §8.1b の焼き付けを個人PCでも先に済ませておく）。
2. `ナビ` の帯の案件プルダウンで何も選ばず（新規モード。`(新規)` と出る）、区画①に企業名などを適当に埋めて区画②の[貼ったものを保存する]を押す。
3. 区画③の[**まとめて作る**]を押す（またはシートタブを再表示して `S1_企業プロファイル` の[S1から再実行]を押す）。
4. 実行後、画面いちばん下のタブから **`run_log`** シートを開く（最初から見えている）。

**見るところ**:

| 列 | 期待する値 | 意味 |
|---|---|---|
| `transport` | `ribbon` | ニセリボンちゃん経由で呼べた（アドイン名の部分一致で見つけた） |
| `model` | config の `recommended_model` の値（既定 `gpt-5.5`） | モデル名が正しく渡っている |
| `latency_ms` | 0より大きい数 | 実際に呼び出しが往復した |

5. 続いて `case_data` シート（非表示。タブを右クリック → [再表示]）を開き、`data_key` が `s1_json_failed` の行の `content` を見る。ニセリボンちゃんはJSONを返さないので検証には落ちるが、**受け取った引数がそのまま書き戻されている**ので、渡した中身が正しいかを次の文字で確かめられる。

| 応答の中の文字 | 確認すること |
|---|---|
| `model=gpt-5.5` | モデル名が届いた |
| `tool=リスク提案ナビ:s1` | 呼び出しの名札が届いた |
| `temp=` / `maxTokens=` / `wait=` | 数値が届いた |
| `systemLen=` / `userLen=` | 文章の長さが届いた |
| `effort=` / `verbosity=` | 推論の調整値が届いた |

6. **AI利用枠が上限に達したとき（True側）の確認**:

   1. Excelで空のブックを開き、`Alt` + `F11` でVBEを開く。
   2. 左のツリーで `リボンちゃん(検証用).xlam` の下の `modMockRibbon` をダブルクリックする。
   3. `LimitCheck = False` と書かれた1行を `LimitCheck = True` に**書き換える**。
   4. `Ctrl` + `S` で保存し、Excelをいったん全部閉じる。
   5. `リスク提案ナビ.xlsm` を開き直す。

   **成功の合図**: ブックは**普通に開き**、`ナビ` 画面が出る。そのうえで「お知らせ」欄に「本日のAI利用枠の上限です」のようなAI利用枠の案内が出る。**「開かない」「エラーで止まる」は不合格**である。

   6. 確認できたら **必ず `LimitCheck = False` に戻して保存する**。

**結果の残し方**: 上の表の「期待する値」と「実際に見えた文字」を並べたメモを作り、開発担当へ送る。
